# Zone Snapper · 设计蓝图 v0.1

> 目标：在都市天际线2（游戏 1.6.2f1）里，玩家用**游戏自带的**多边形区域工具画区域时，节点自动贴合到道路/铁路/步道/已有区域边界，边线**沿网络描边**而不是直线连接，并且在点下去之前就能看见会贴成什么样（淡色实时预览）。
> 参照物：Paradox Mods ModId 148238「Subdivisions」(作者 Sidrus，已被作者下架，GitHub 仓库 `EjafiSoftware/Subdivisions` 也已 404)。**没有源码**，本蓝图依据其商店页说明 + 完整更新日志（存 `research/reference-mod-subdivisions.json`）与游戏反编译实证重建其机制，并按用户需求改进。
> 本文所有「机制」结论都带证据位置；证据分三级：**FACT**（反编译 file:line 或 `ilspycmd -t` dump 行号）、**INFERENCE**、**UNVERIFIED**。

---

## 一、调研结论（决定架构的 FACT：1～8 定架构，9～17 定「贴在哪里」，18～19 定「怎么跨帧持有」，20 定「拖拽与预览」，21 定「哪个相位真的每帧在跑」，22 定「人行道外缘那个数在哪」，23 定「下拉框选项的 key」，24～25 定「游标格与预览该不该画」，26 定「那些词在游戏里到底叫什么」，27～30 定第六轮的三件事：「粗虚线谁画的」「游戏自己过不过滤网络类型」「白名单的标记位在哪、宽度不是高度」，31～32 定第七轮那颗重置按钮（控件从哪来 + 清一颗快捷键要动三处），33～34 定第八轮的「路口凭什么是好几个节点、中心取谁」与「地下的数据到底在哪一格」）

1. **游戏里「手动放节点→闭合成一个区域」的工具只有一个：`Game.Tools.AreaToolSystem`**
   （`research/decompiled/Game.Tools/AreaToolSystem.cs`，下称 *ATS*）。它按 `AreaType` 区分：**Lot / District / MapTile / Space / Surface**（FACT：`ilspycmd -t Game.Areas.AreaType`）。
   → 用户要的「行政区域、产业区域、地皮贴图区域、垃圾场填埋区域」在这款游戏里**不是四个工具，而是同一个工具的不同 AreaType**：
   - 行政区域 = `District`
   - 地皮/地块 = `Lot`；地皮贴图区域 = `Surface`
   - 产业类区域（采矿/仓储/填埋这类特殊产业区）= `Lot` + `ExtractorAreaData`/`StorageAreaData`
     （FACT：ATS:3528-3531 的 `specialized_industry` 遥测分支；填埋区是否走这条路径标 **UNVERIFIED**，实机 T4 验）
   - `ZoneToolSystem` 是**网格刷/框选**（`Mode {FloodFill, Marquee, Paint}`，FACT：ZoneToolSystem.cs:26,384-388），`TerrainToolSystem` 是**笔刷**（FACT：TerrainToolSystem.cs:195 `brushing => true`），`WaterToolSystem` 是**拖半径**（FACT：WaterToolSystem.cs:231-233）——这三个**没有节点概念**，不属于本模组范围。
   - 另外三个「点链」型工具：`NetToolSystem`（道路，FACT：NetToolSystem.cs:5057、撤点 :6378）、`RouteToolSystem`（公交/线路，闭合判据与 ATS 同形，FACT：RouteToolSystem.cs:766、撤点 :1810）、`ObjectToolSystem`（`Mode {Stamp, Line, Curve}`，撤点 :3809）。它们是**开链不是闭合多边形**，列为 P4。

2. **节点表是托管字段 + 公开读写口**：`private NativeList<ControlPoint> m_ControlPoints`（FACT：ATS:2557），
   公开方法 **`public NativeList<ControlPoint> GetControlPoints(out NativeList<ControlPoint> moveStartPositions, out JobHandle dependencies)`**（FACT：ATS:2973），
   游戏自己的渲染器 `Game.Rendering/GuideLinesSystem.cs:2841` 就走这个口读。**mod 拿到的就是这个列表本体，可写。**

3. **列表不变量**：最后一格永远是「跟随鼠标的游标格」。FACT：`int index = math.select(0, m_ControlPoints.Length - 1, m_State == State.Create)`（ATS:3678、SnapJob :785）。

4. **左键提交节点 / 右键撤销上一步**：`OnUpdate` 里 `applyAction.WasPressedThisFrame() → Apply(...)`、`secondaryApplyAction.WasPressedThisFrame() → Cancel(...)`（FACT：ATS:3151-3157；`Apply` :3408、`Cancel` :3283 都是 **private 非 Burst** ⇒ 可 Harmony）。右键在 Create 态的净效果 = **弹掉一个已提交节点**（FACT：ATS:3330 `RemoveAtSwapBack(Length-1)` 删游标，随后 :3345 把新的射线点写回最后一格）。

5. **提交时的两道闸门决定投影布局必须怎么排**：
   - 最小节点间距：`num = distance(cp[Length-2], cp[Length-1])`，必须 `>= AreaUtils.GetMinNodeDistance(data)` 才受理（FACT：ATS:3509-3511；`GetMinNodeDistance = m_SnapDistance * 0.5f`，FACT：`ilspycmd -t Game.Areas.AreaUtils`）。
   - 贴合半径与间距都来自 **prefab 自己的 `AreaGeometryData.m_SnapDistance`**（FACT：`ilspycmd -t Game.Prefabs.AreaGeometryData`）。
   → **推论（本设计的关键不变量）**：描边点必须插在「它指向的那个手动节点」**之前**，投影形状恒为
   `[m0, T(m0→m1), m1, T(m1→m2), m2, …, mN, cursor]`
   这样 `[Length-2]/[Length-1]` 仍然是「上一个手动节点 → 光标」，游戏的最小间距判定和闭合判定都保持原语义，**不需要 patch 掉任何校验**。

6. **贴合半径天然分级**：`m_SnapDistance` 按 prefab 各不相同（区域类型越大值越大）⇒ 需求 7「行政区域贴合范围大、产业区域小」直接用游戏自己的 `m_SnapDistance × 倍率` 实现，不硬编码尺寸。
   最小间距同源于游戏自己的量：`AreaUtils.GetMinNodeDistance(areaData) = m_SnapDistance * 0.5f`，读不到 prefab 数据时退回类别表
   **Lot 8 / District 32 / MapTile 64 / Space 1 / Surface 0.75**（FACT：ATS:3458 与 `Game.Areas.AreaUtils` dump :410-421）。
   `PolicyKit.Resolve` 因此带第二个可选入参 `gameMinNodeDistance`，把我们的间距**抬到不低于游戏那道闸**（我们绝不比游戏更密）。

7. **`Apply` 是一台状态机，「左键事件」与「落了点」不是一回事，且第一击发生在 `State.Default`**（FACT：ATS:3408-3620）：
   - `State.Default` + 表里有游标 + `m_AllowCreateArea` ⇒ ATS:3476-3494：`m_State = State.Create`、`Clear()`、`Add(value)`（玩家那一格 = **第一个手动节点**）、再 `Add(raycast)`。
     ⇒ 我们的系统跑在 AreaToolSystem **之后同一帧**，读到的 `state` 已是 Create、`len==2`、`cps[Len-2]` 就是那一格。
   - `State.Create` 的最小间距 `if (num >= minNodeDistance2)` **没有 else**（FACT：ATS:3509-3511 → 3567）：
     距离不够时**一个节点都不加**、表长不变，而 Harmony 后缀照样跑 ⇒ 必须用「表长是否真的多了一格」当落点证据
     （`ProjectKit.GameAcceptedCommit`）。否则玩家挨着点两下（District 门槛 32 米，很常见）就会把上一个手动节点重复收进栈。
   - 闭合那一击：`m_State = State.Default`（FACT：ATS:3525）；`recreate != Entity.Null` 时再把 activeTool 交回 ObjectToolSystem（FACT：ATS:3533-3543）
     ⇒ **不需要我们自己判「区域建完了」**，入口闸 `state != Create` 就会在同一帧清会话。
   - `State.Modify` 的 Apply 在「没拖动」时会跳回 Create 并重新灌两点（FACT：ATS:3568-3592）⇒ 这是开局**接管**（`ProjectKit.ShouldAdopt`）的第二个正当来源。

8. **ECS 类型：查询/读写缓冲区一律写「元素类型」，绝不写 `DynamicBuffer<T>`**（FACT：`Unity.Entities/TypeManager.cs:2092` 只扫已加载程序集；
   `TypeManagerInternal.cs:186-198` 对拿不到 StableTypeHash 的类型直接 `return 0` ⇒ `GetTypeIndex` 抛 `ArgumentException: Unknown Type`）。
   构造出来的泛型 `DynamicBuffer<Game.Areas.Node>` **没有** StableTypeHash，元素类型 `Game.Areas.Node` 有（从游戏自己
   `AddBuffer<Game.Areas.Node>`（FACT：ATS:3433）扫出）。游戏自身全程用元素形式：查询 `ComponentType.ReadOnly<Game.Areas.Node>()`
   （FACT：`Game.Rendering/GuideLinesSystem.cs:2651`）、取用 `EntityManager.GetBuffer<Game.Areas.Node>()`
   （FACT：`Game.Serialization/RequiredComponentSystem.cs:1341`）。已上线的 AccessAnarchy 同形。
   真需要构造泛型时的官方兜底：`TypeManager.AdditionalComponentsRegistry.AddType(typeof(DynamicBuffer<Game.Areas.Node>), 2882194395u)`。
   **实机反证**：v0.1.0 在 `ZoneSnapperApplySystem.OnCreate` 写了 `ComponentType.ReadWrite<DynamicBuffer<Game.Areas.Node>>()`
   ⇒ OnLoad 当场断在 Harmony 之前 ⇒ 主系统带着空栈每帧抹掉玩家第一个节点。见开发笔记 §〇。


可贴合的世界几何（全部有公开可达路径）：
- 网络曲线：`Game.Net.Curve/Edge/Node/EdgeGeometry/Segment`（FACT：`Game.Net/Curve.cs:11`、`Edge.cs:8`、`Node.cs:10`）；最近点原语 **`MathUtils.Distance(Bezier4x3, float3, out float t)`**、`MathUtils.Position(curve,t)`、`MathUtils.Tangent(curve,t)`（FACT：Colossal.Mathematics dump :772/:581/:588）。
- 「P 半径 R 内所有路段」：**`Game.Net.SearchSystem.GetNetSearchTree(bool readOnly, out JobHandle)` → NativeQuadTree**（FACT：SearchSystem.cs:829，用法范式 ATS:1253-1269）；区域侧同理 `Game.Areas.SearchSystem.GetSearchTree`（FACT：Areas/SearchSystem dump，用法 ATS:3780/:1238）。
- 图遍历：公开 **`Game.Net.EdgeIterator`**（FACT：EdgeIterator.cs:34/105）+ 节点上的 `DynamicBuffer<Game.Net.ConnectedEdge>`（FACT：ConnectedEdge.cs:10）。
- 环形道路：组件 `Game.Net.Roundabout{float m_Radius}`（FACT：Roundabout.cs:8），中心/半径在 `EdgeNodeGeometry.m_Middle` + `m_MiddleRadius`（FACT：EdgeNodeGeometry.cs:12,20）；游戏的 Dijkstra 参考实现 `NetToolSystem.CreatePath`（FACT：NetToolSystem.cs:7172，public static）。
- 海岸线：游戏**没有**海岸线折线，现算湿/干网格质心（FACT：`NetToolSystem.cs:1811-1866 SnapShoreline`，阈值 `y > 0.2f`——这正是原模组说「iso-lines 会弯、要开 Anarchy」的原因）；网格数据 `WaterSystem.GetSurfaceData(out deps)`（FACT：WaterSystem.cs:764）。
- 让标记不陷进地里：`y = max(TerrainUtils.SampleHeight(...), waterSurface)`（FACT：NetToolSystem.cs:1876-1904 是正面教材，原模组 v1.2.0 修的就是这个）。
- 变化检测（需求 5 的命脉）：`Game.Common.Updated` 单帧脏标记 + **`Game.Net.UpdateCollectSystem` 公开成员 `netsUpdated` / `lanesUpdated` / `GetUpdatedNetBounds(out deps)`**（FACT：UpdateCollectSystem.cs:328/330/468）⇒ 事件驱动重描边，**不做定时全量重刷**。

预览绘制（如果要额外标记）：`Game.Rendering.OverlayRenderSystem`，公开 `Buffer` 上有 `DrawCircle/DrawLine/DrawDashedLine/DrawCurve/DrawText`（FACT：OverlayRenderSystem.cs:602/635、Buffer :133/:201/:233），跑在 `SystemUpdatePhase.Rendering`。**注意：游戏自己就按控制点列表画区域预览**（FACT：`UpdateDefinitions` → Burst job ATS:3793/1347-1461），所以把描边点插进列表 = 预览自动是描边形状，标记属可选增强。

### v0.1.2 补的 FACT：贴合「目标」到底由什么决定（第二轮 T4 反馈的根因）

前几轮把「贴路缘」实现成了「贴中心线」，就是因为下面这组事实没读到位。它们决定了**每一类区域该吸在哪**，
不是调参能替代的：

9. **游戏自己的吸附掩码表按 `AreaType` 分档**（FACT：ATS:3235-3275）：`District → Snap.NetMiddle`；
   `Lot / Surface / Space → Snap.NetSide | Snap.ObjectSide`；`MapTile → 无`。
   ⇒ 「行政区贴中心线、地块与表面贴边缘」不是玩家的一厢情愿，是**游戏自己的口径**；本模组照抄同一张表。
10. **隧道被排除在边缘吸附之外**（FACT：ATS:501-508），**高架没有**（`CompositionFlags.General.Elevated = 0x10000` 不参与任何门禁）
    ⇒ 需求「高架要能贴」天然成立，**不要**给高架加特判；隧道不贴路缘是与游戏一致的行为。
11. **缘线不是猜的**：`Game.Net.EdgeGeometry.m_Start/m_End` 两条缘折线（FACT：ATS:452-493 游戏自己贴路缘时采的就是这两条）。
    宽度 = `Edge → Game.Net.Composition → NetCompositionData.m_Width`（FACT：`ilspycmd -t Game.Net.Composition` / `Game.Prefabs.NetCompositionData`）。
    **读不到宽度时按 `PolicyKit.MIN_ASSUMED_ROAD_WIDTH = 3 m` 兜底**（玩家明确指定的口径）。
12. **建筑轮廓**：`Game.Objects.ObjectUtils.CalculateBaseCorners`（FACT：`Game.Objects/ObjectUtils.cs:651-663`）；
    `GeometryFlags.Circular` 那一类要排除；静态物体走 `SearchSystem.GetStaticSearchTree`（FACT：ATS 的 `Snap.ObjectSide` 分支）。
13. **`NativeQuadTree<TItem,TBounds>.Iterate<TIterator>(ref TIterator, int startNode = 0)` 没有「按框查」的重载**（FACT：`Unity.Collections/NativeQuadTree`），
    迭代器的 `Intersect(QuadTreeBoundsXZ)` 回调是**唯一**的裁剪手段。上一版 `AreaGather.Intersect` 直接 `return true`
    ⇒ 每次抓世界数据都遍历全城区域树；本轮补上 `m_Bounds` 判定（同一条纪律写在 Playbook 的「性能卡点」段）。
14. **拖拽改形时那张控制点表里只有光标**（FACT：`AreaToolSystem.State {Default, Create, Modify, Remove}`；
    `Modify` 期的预览环每帧在 Burst `CreateDefinitionsJob.Edit()` 里重建），
    落档时把 Temp 实体的 Node 缓冲**原样**拷进正式区域（FACT：`ApplyAreasSystem.cs:180-202`，:175 打 `Updated`）。
    ⇒ 「拖拽中实时预览」必须去改写那张 Temp 预览实体（补丁面扩到 job 内部，本轮不做）；
    **落档之后**读/写 `Game.Areas.Node` 就够 —— 这正是 `ZoneSnapperFollowSystem` 的立足点。
15. **拖的是哪一格是可知的**：`GetControlPoints(out moveStartPositions, out deps)`（FACT：ATS:2973）给出的
    `m_MoveStartPositions[0].m_OriginalEntity` + `.m_ElementIndex.x` = 被拖节点的**区域实体 + 节点下标**；
    `GetSourceArea`（FACT：ATS:1981-1995）在整个拖拽期间保持那张原始环不变。
    编辑器模式下位置是**局部空间**，`SubArea` 从属区域必须跳过，节点高度取不到时是哨兵 `float.MinValue`。
16. **弯道节点密度的硬界就是弦高公式**：`h ≈ L²/(8R)` ⇒ 允许的最大弦长 `L ≤ √(8·R·h)`；
    取 `h = 道路半宽`（读不到用 3 m/2）就等价于「相邻两点的连线不许越出道路边缘」（玩家第 ② 条的原话）。
    RDP 的判定本来就是垂距 ⇒ 把简化容差**封顶到道路宽度**（`TraceKit.ChordHardTolerance`）就是这条规则的现成实现，
    不需要另写一套曲线采样。
17. **「最外围」要相对谁算**：走廊 A* 跑三趟（左缘 / 中心 / 右缘），比的是「候选走线各中间点到**本区域质心**的平均平面距离」，
    取更大者（门槛 `> 1.02×` 是为了确定性：一样外围时保留先来者）
    ⇒ 区域在路网外侧时自动选外圈，且不会为省距离穿小巷、也不会绕大圈（`DetourTooLong` 仍是硬阀）。
    没有质心（还没闭合、点数不足）时**不启用外围比较**，退回默认最短路。
    代价：两块区域分居公共段两侧时各自相对自己质心算，理论上可能选出不同路线（写在开发笔记 §一 已知限制）。

### FACT 18（v0.1.3，第三轮实机用日志证出来的）：跨帧持有的锚点不能记「数组下标」

- `WorldSampler.Capture` 每次重抓都 new 一个 `WorldSnapshot`，`Edges/Nodes` 两个数组的顺序由
  四叉树遍历顺序 + 走廊盒子共同决定 ⇒ **同一实体的下标在两次抓取之间会变**（这不是理论边界，是常态：
  玩家每多点一下，走廊就长大一点，就重抓一次）。
- 于是「跨帧持有的锚点里存下标」= 一颗必炸的雷：描边读到的 `Edge` 要么越界（`TraceKit` 的越界防御直接
  `NotOnNetwork`），要么指向别条路（描出鬼线，再被自交闸拦下，仍是直线）。净效果 = **边线永远不贴**。
- 机器凭据：`snap=2907`（游标确实吸住了）对 `traceHit=17 / fallback=119`，且日志里明文有一行
  `闭合预览：…（闭合边描出 0 个点，回退原因 NotOnNetwork）`。整段日志零 `[ERROR]` ⇒ 不是崩，是这一条。
- **可迁移的口径**：跨帧持有的引用一律记「稳定身份 + 位置」，下标只是某一帧数组里的临时坐标。
  本模组用的稳定身份是 `WorldSampler.SignatureOf`（`Entity.Index + Entity.Version + 该实体的参考坐标`），
  重抓之间同一条路签名不变 ⇒ `Engine/AnchorRebind.cs` 靠它把锚点搬回新下标空间；
  签名找不到才退回「按位置重新吸附」，且**位置永远不许被重绑改动**。
  （Playbook 里也记了一条同形状的：Entity 数值跨会话不稳 ⇒ 存档侧要用名字而不是 Entity。）

工程与工具链事实（沿用知识库 Playbook）：`IMod` = `OnLoad(Game.UpdateSystem)` / `OnDispose()`；系统注册 `updateSystem.UpdateAfter<T,AreaToolSystem>(SystemUpdatePhase.ToolUpdate)`（FACT：`Game/UpdateSystem.cs:178`，AccessAnarchy 实机验证过）；工具切换通知 **`public Action<ToolBaseSystem> EventToolChanged`**（FACT：ToolSystem.cs:30）；Harmony 走 `PackageReference 2.2.2 + PackageDownload 2.3.3 + 在 RunModPostProcessor 之后换 dll` 的既有形状（见 BridgeTheLanguageGap 的 csproj 与 Playbook 步骤 7）。

### FACT 19（v0.1.4，第四轮实机用日志证出来的）：绘制期间的走廊必须**单调**，而且要罩住所有已落的角

- FACT 18 说的是「下标会变 ⇒ 靠签名搬回去」。第四轮的日志补了另一半：`stale=0`（越界一次都没有）
  却仍然 `resnap=45 / lost=25 / fb=NotOnNetwork×121` ⇒ 那些锚点**签名找不到、按位置也吸不到**。
  唯一的解释是：**那一条路根本不在这一帧的快照里**（`WorldSampler.Capture` 只遍历走廊盒子范围内的实体）。
- 为什么会不在：`CorridorBox(上一格, 光标, …)` 只由当前这两点决定，光标每动一下走廊就整体平移/缩放一次。
  玩家沿一条路点到第 4 格时，第 1 格贴的那条路可能已经掉出盒子 ⇒ 那一条边 `NotOnNetwork` ⇒ 退回直线。
  **玩家视角就是「同一条路有时贴有时不贴，很随机」**——它确实随机，因为它是光标位置的函数，不是路的函数。
- 修法（`PolicyKit.PlanStroke`）：`Raw = 上一帧 Raw ∪ 所有已落档角 ∪ 本次两端`，`Capture = Raw ⊕ 半径`，
  **只增不减**；到 `STROKE_LIMIT=2500 m` 才按中心收缩并警告一次。`EnsureWorld` 的闸同步改成
  「当前快照不包含端点才重抓」（原来是「不相交就重抓」——走廊一收缩就换快照，等于亲手制造 FACT 18 那个漂移）。
- **可迁移的口径**：任何「按当前操作范围局部抓取、又被跨帧持有的引用指向」的世界快照，
  抓取范围都必须随会话**单调增长**并覆盖全部被引用的对象；否则重绑做得再对，也只是把「找不到」变成「吸不到」。

### FACT 20（v0.1.4，实时预览逼出来的两条取证）：拖拽格号是公开的，overlay 也是

- `AreaToolSystem.GetControlPoints(out NativeList<ControlPoint> moveStartPositions, out JobHandle)`
  （FACT：ATS:2973）——**第二个出参就是拖拽期的起点表**，之前一直当 `out _` 丢掉。
  进 `State.Modify` 的门槛（FACT：ATS:3440）是
  `HasComponent<Area>(cp.m_OriginalEntity) && math.any(cp.m_ElementIndex >= 0)`，而游戏自己就按
  `m_Nodes[cp.m_OriginalEntity][cp.m_ElementIndex.x]` 取被拖格、按 ATS:845/846 的环绕算法取左右邻格
  ⇒ **`m_ElementIndex.x` 就是 `Game.Areas.Node` 缓冲里的格号**（这条一直卡着「拖拽只重描左右两条边」，见任务 #16）。
  Modify 态 `index = math.select(0, Length-1, state==Create)`（FACT：ATS:3678）⇒ 表里第 0 格就是跟着鼠标动的那一格，
  且已经过游戏自己的吸附（:3681）。
- 世界空间带颜色的绘制通道只有一个：`Game.Rendering.OverlayRenderSystem` 的公开 `Buffer`。
  `GetBuffer(out JobHandle)`（:602）、`AddBufferWriter`（:635）、`DrawCircle(Color, float3, float)`（:133）、
  `DrawLine(Color, Line3.Segment, float)`（:201）、`DrawCurve`（:233）；颜色是 `UnityEngine.Color`（alpha 有效，
  内部自转 linear），位置是世界空间 `float3`，宽/直径单位是米。每帧 `OnUpdate` 里把实例计数归零（:741-747）
  ⇒ 停止提交就是消失。注册必须 `UpdateBefore<自己, OverlayRenderSystem>(SystemUpdatePhase.Rendering)`
  （FACT：SystemOrder.cs:688 把它注册在 Rendering；UpdateSystem.cs:173 提供带锚点的 `UpdateBefore<,>`；
  模组用 `UpdateAt` 会**追加**在游戏之后 ⇒ 晚一帧）。
  ⚠ `SystemUpdatePhase` 枚举里 `Rendering` 排在 `ToolUpdate` **之前** ⇒ ToolUpdate 发布的东西最早下一帧才上屏。
  ⚠ 拿 `Buffer` 之后要先 `deps.Complete()`：GuideLines / AreaBorder / EffectRange 这些系统把没写完的 job 登记在它那里。
- 游戏侧 `ControlPoint` **没有颜色通道** ⇒ 「用更淡的颜色显示预览」在技术上只能走 overlay，
  不能借用游戏自己的控制点渲染。这一点决定了预览是**另一条通道**，也就顺带保证了它写不进任何存档。

### FACT 21（v0.2.0，第五轮「自动跟随从来没效果」的根因）：`ApplyTool` 那一拍**只在有提交的帧存在**

- `Game.ToolOutputSystem`（`ToolSystem` 的派生）在自己的 `OnUpdate` 里，只有当本帧的 `applyMode` 是
  `Clear` 或 `Apply` 时，才更新 `m_OutputUpdate` Mask（= `SystemUpdatePhase.ClearTool / ApplyTool`）
  （FACT：`Game/ToolOutputSystem.cs:20-30`）。`GameSystemTypeGroup.Tools` 虽然恒开
  （FACT：`Game/GameSystemTypeGroups.cs:41`），但相位掩码没开 ⇒ **挂在 `ApplyTool` 上的系统平时一帧都不跑**。
- 每个相位由谁驱动：`ToolSystem.OnUpdate` 驱动 PreTool/ToolUpdate/PostTool
  （FACT：`Game/ToolSystem.cs:251-261, 308-327`）；`SystemUpdatePhase` 的枚举顺序是
  …Modification5 → ModificationEnd → PreSimulation → PostSimulation → GameSimulation → EditorSimulation →
  **Rendering** → PreTool → PostTool → **ToolUpdate** → ClearTool → **ApplyTool** → … → Serialize → Cleanup
  （FACT：`Game/SystemUpdatePhase.cs`）。
- 「路网变了」这件事的位是在 **Modification5** 置的：`Game.Net.UpdateCollectSystem.netsUpdated`
  （FACT：`UpdateCollectSystem.cs:389-413`；注册相位见 `SystemOrder.cs:192`）。
  ⇒ 想「每帧看路网有没有变并跟着改写区域」，系统必须在 **ToolUpdate**（或更早），在 ApplyTool 里连窗口都没有。
- 这条同时解释了为什么 `ZoneSnapperApplySystem`（补写闭合边）**必须**留在 ApplyTool：
  区域的 `Created` 标记在帧末 Cleanup 相位就被回收（FACT：`Game.Common/CleanUpSystem.cs:24-53`），
  只有提交那一帧既拿得到 `Created` 又拿得到 ApplyTool。两条约束方向相反，所以两个系统注定分处两个相位。

### FACT 22（v0.2.0，反馈 3「人行道外缘不是车道路缘」的数据侧）：整幅宽在 `m_Width`，不对称量在 `m_MiddleOffset`

- 游戏自己烘焙道路两侧边线的那一行就是答案：
  `float2 o = m_Width * float2(0.5f, -0.5f) + m_MiddleOffset;` →
  `OffsetCurveLeftSmooth(centre, o.x)` / `OffsetCurveRightSmooth(centre, -o.y)`
  （FACT：`Game.Net/GeometrySystem.cs:526-530`），写进 `Game.Net.EdgeGeometry` 的
  `m_Start.m_Left / m_Start.m_Right / m_End.m_Left / m_End.m_Right`（同文件 :398/:405 用
  `Cut(leftStartCurve, leftEndCurve, …)` 与 `Cut(leftEndCurve, leftStartCurve, …)` 分别写入 ⇒
  **每条缘线的上半段在 `m_Start`、下半段在 `m_End`，要各拼一条才完整**）。
- `AreaToolSystem.cs:452-493` 的 `Snap.NetSide` 消费的就是这四条曲线 ⇒ 我们只有走同一条数据才可能吸到同一条线。
- 旁证（口径来源，不是我们自己猜的语义）：`Game.Zones/BlockSystem.cs:195` 拿
  `m_Width - m_MiddleOffset * 2` 当"建筑可占地宽度"的起点 ⇒ `m_Width` **含人行道与隔音屏**，
  `m_MiddleOffset` 是"中线两侧的不对称量"（单边人行道、中央分隔带、隔音墙都会让它非零）。
- ⚠ **`Game.Prefabs.EdgeGeometry` 只有 `Segment m_Start, m_End; Bounds3 m_Bounds` —— 没有 `m_StartWidth`**。
  早期一处笔记写的"用 `EdgeGeometry.m_StartWidth` 取路缘"是错的，本轮按深查报告改掉了。
- 落地：`GraphEdge.SetWidthsFromPrefab(m_Width, m_MiddleOffset)` →
  `LeftHalfWidth = |w/2 + mid|`、`RightHalfWidth = |w/2 − mid|`（Engine 层，可离线断言；回归壳 W 段）。
  只取 `w/2` 的症状正是玩家那句「这不是人行道边」：不对称断面上内侧那条缘线整体偏 `mid` 米。

### FACT 23（v0.2.0，反馈 5 的「贴合模式」下拉框）：枚举选项的 key 模板不归 `ModSetting` 那六个方法管

- 枚举属性走 `WidgetType.EnumDropdown`，每个选项的本地化 key 是
  `"Options." + enumType.Name.ToUpperInvariant() + "[" + 成员名 + "]"`
  （FACT：`Game.UI.Menu/AutomaticSettings.cs:846-855` 的 `GetEnumMemberAccessor`（prefix `"Options"`）、
  :906-913 `text = enumType.Name.ToUpperInvariant() + "[" + names[i] + "]"` → `new EnumMember(value, text)`、
  :1131-1138 属性类型分派）。
- 所以「五个选项的文案」注册的 key 长 `Options.TRACEMODE[Smart]`，与
  `Options.OPTION[id.Name.SnapMode]`（标题）是两套；`LocaleKit.EnumKey` 与游戏侧 `ZoneSnapperSetting.ModeKey`
  共用同一模板，`CheckTemplatesOnce` 也把它对账（模板变了不会变成满屏 key，而是**安静地退回英文**，
  所以不钉死就等于没有）。
- 面板上枚举项**没有"这一项的说明"那一格** ⇒ `mode.*` 五行只注册标题，回归壳 L4 按这个口径放过它们。

### FACT 24（v0.2.0，拖拽目标识别）：跟着鼠标那一格的 `m_ElementIndex` 恒为 `(-1,-1)`

- Modify/Create 态每帧都会用新的射线命中替换游标那一格（FACT：`AreaToolSystem.cs:3661-3671`），
  替换出来的 `ControlPoint` 只有位置与 `m_OriginalEntity`，`m_ElementIndex` 是 `(-1,-1)`；
  索引选择见 :3678（Create 态取 `Length-1`，其余取 `0`）。
- ⇒ 「哪一格是被拖的那个**已存在节点**」不能按"表里第 0 格"认，也不能拿第一个 `m_ElementIndex.x >= 0` 就当数：
  必须取**带 `HasComponent<Area>` 且 `math.any(m_ElementIndex >= 0)`** 的那一格（游戏的进门条件，FACT：ATS:3440-3445）。
  本项目据此把拖拽目标识别写成两条路（带 Area 的条目 → 按位置就近认环上格），
  并把两条各自的命中次数分开计数（日志 `dragIdx=` / `dragPos=`）—— 如果实机上第二条远超第一条，
  说明 `m_ElementIndex.x` 在真机上并不是 `Game.Areas.Node` 的格号，FACT 20 的那半条结论要重读。

### FACT 25（v0.2.0，预览的"该不该画"判定不需要补丁）：`activeTool` 与 `state` 都是公开的

- `ToolSystem.activeTool` 是公开属性（FACT：`Game/ToolSystem.cs:84-100`）；
  `AreaToolSystem.State` 枚举 `{Default, Create, Modify, Remove}` 且 `state => m_State` 公开可读
  （FACT：ATS:2605，枚举本体 :2594 起）。
- ⇒ 渲染端可以自己判「区域工具此刻是不是活着、处在会用到控制点表的那两个状态里」，
  不需要第三个 Harmony 补丁：`activeTool == areaTool && state ∈ {Create, Modify}` 才画预览。
  这是反馈 1「退出工具后预览还留在地上」的第二道闸（第一道在生产端 `Idle()`，第三道是 `Time.frameCount` 新鲜度）。

### FACT 26（v0.2.0，反馈 9「本地化必须用游戏官方用词」）：官方词条在哪、能抄到的是什么、以及**上一版哪三条出处是假的**

**取证工具**：`scripts/extract-official-terms.py`（解析 `.loc` 并输出对照表）与 `scripts/probe-official-locale.py`
（按 key 片段 / 值片段全库检索，用来回答"游戏里到底有没有这个词"）。产物是
`research/locale-official-terms.md`（12 语言逐格原文 + 命中次数）与 `research/locale-probe*.txt`。
**这一节里每一个词都能在那份表里找到行号；找不到行的词一律不许写进文案。**

- 这份安装（1.6.2f1）**没有**明文 `StreamingAssets/Locale/l10n_*.txt` —— 全盘 `find -iname 'l10n_*'` 零命中，
  v0.1.x 那条"游戏语言目录下确有 l10n_zh-HANS.txt"的 FACT 是错的（结论碰巧没错：语言码确实是 `zh-HANS/zh-HANT`，
  但凭据要换成下面这两条）。官方文案在 `Cities2_Data/Content/Game/Locale.cok`（23,507,610 B），
  而它**就是一个普通 ZIP**：成员 `<lang>.loc` + `<lang>.loc.cid` 共 24 个，全部 `STORED`（未压缩）。
  `.loc` 的布局（本轮实测）：`varint 版本(1) + varint(0) + 3×(varint 长 + UTF-8 串)`（语言名/语言码/语言名）
  `+ varint 记录数`，之后是 `记录数 × [varint 长][lockey][varint 长度][UTF-8 值]`；第一条是空 key（跳过），
  基础游戏 **11027 条记录 / 11026 个非空 key**。DLC 各有一份同结构的 `Content/<包名>/Locale.cok`
  （本机 12 份：BridgesAndPorts / CityStations / DowntownMarketSet / DragonGate / LeisureVenues /
  MediterraneanHeritage / ModernArchitecture / …），**每份都是这 12 种语言**。
- **实际出货的 12 种语言**：`en-US de-DE fr-FR es-ES it-IT pl-PL pt-BR ru-RU ja-JP ko-KR zh-HANS zh-HANT`
  —— 没有 `es-MX`，繁简码也不是 `zh-CN/zh-TW`。我们注册的 12 份字典与之完全一致（L2/L6 各钉一半）。
- **能抄到的只有工具名**。命名空间实测只有 `Assets.* / Common.* / Tools.TOOL_MODE[*] / Toolbar.BRUSH_* / Achievements.* / Chirper.*`，
  工具标题在 `Assets.NAME[...]`（去掉尾部的"工具/Tool"就是名词）：
  `District Area` → 市辖区创建工具 / 行政區創建工具 / **特区作成ツール** / Создание района / Strumento creazione quartiere；
  `Extractor Lot` → Specialized Industry Area Tool / 专门产业区工具 / 特殊工業功能區域工具 / **特化産業エリアツール** / 특화 산업 구역 도구；
  `Surface Area` → Surface area tool / 表面区域规划工具 / 地面區域劃分工具 / 地表エリアツール / **표면적 도구**；
  `Map Tiles` → Map Tiles / 地图区块 / 地圖區塊 / マップタイル / Клетки карты / Pola mapy / Quadranti della mappa。
  页签那一层的说明在 `Assets.SUB_SERVICE_DESCRIPTION[Districts|ZonesExtractors|Surfaces]`
  —— ⚠ 上一版写的 `SubServices.NAME[...]` 这个 key **不存在**（0 命中），真实前缀是 `Assets.SUB_SERVICE_*`。
- **抄不到的：Snapping 这个词本身**。`SNAPPING` 在 12 种语言的 key 与值里 **0 命中**；en 值里 `Snapping` **0 次**；
  zh-HANS 值里 `吸附` **0 次**、`对齐` 只有 **1 次**（`Assets.NAME[UXSnapSFX]` = "UX对齐音效"，一个音效名）。
  `ToolOptions.*` 与 `Toolbar.SNAPPING_TITLE / ELEVATION_TITLE / UNDERGROUND_MODE_TITLE` 这些 key **全部不存在**
  —— 4.28 GB 的 `Blob.cok` 里也扫不到「快速对齐 / Snapping」的明文，说明那层工具选项 UI 文案在压缩资源里，本机读不到。
  ⇒ **上一版按那张表做的三处改动全部撤回**：① 页名 `tab.snap` 的 zh-HANS 从「对齐」改回**「吸附」**（与全表另外 15 处
  「吸附」一致，也不再声称"照抄官方"）；② 两条 `.desc` 里"游戏里这一档叫「快速对齐道路中央 / 快速对齐路边」"删掉，
  改成不引用游戏 UI 标签的说法；③ 四条滑杆标题里的动词（de Einrasten / fr Accrocher / ru Привязка …）标注为**本表自译**。
  回归壳 L6 现在有两条守卫断言守着这件事：整张表里不许再出现「快速对齐 / 快速對齊」，
  且 `tab.snap` 繁简两列必须都等于「吸附」（即"我们不拿无证词撑场面"）。
- 另外几个"游戏里没有"的概念（本轮实测确认，不是推测）：`curb` / `kerb` 在 12 种语言里 **0 次**（`sidewalk` 有 31 次，
  zh-HANS 一律译「人行道」）⇒ 玩家要的"人行道外缘不是车道路缘"只能我们自己说清；
  也没有"高架道路/隧道"的独立标签，游戏给的是 `Common.ACTION[Enter|Exit|Leave Underground Mode]`（地下视图 / 地面视图 / 离开地下）。
  ⚠ 别把 `Game.Areas.AreaType`（None/Lot/District/MapTile/Space/Surface/Count）与 `Game.Zones.AreaType`（住宅/商业/工业）混：
  前者的 `Space` 在 UI 里是**空域**（`Assets.NAME[Helipad Airspace - Small]` = 直升机坪空域），不是外层空间 —— 反馈 6 删那一档时核对到的副作用。
- **与本模组用词的取舍**：玩家点名要的两个名字优先 —— 「节点吸附距离」（反馈 3 原话）与「城市边界线」（反馈 6 原话）保留玩家词，
  官方名放在同一格的括号里（`城市边界线（地图区块）`，12 种语言每格都带着游戏的 Map Tiles 原词，L6 逐语言钉了 12 条）。
  L6 段（**31 条**）钉的就是这张"能抄的抄、抄不到的标出来"的对照表。

### FACT 27（v0.2.1，反馈 2「两个叠在一起的粗虚线长方形」）：区域边框的虚线只由游戏自己画，且只对地图瓦片；线宽正比于 `m_SnapDistance`

- `Game.Rendering/AreaBorderRenderSystem.cs:378-395`：chunk 带 `Game.Areas.MapTile` 且**非编辑器模式**时 `dashedLines = true`，
  其它情况一律 false ⇒ 游戏里唯一画虚线边框的对象就是地图瓦片。
- 同文件 `:544-552` `DrawEdge` → `m_OverlayBuffer.DrawDashedLine(color, color, 0f, styleFlags, line, geometryData.m_SnapDistance * 0.2f, num * 0.55f, num * 0.45f)`，
  其中 `num` 由 `geometryData.m_SnapDistance * 1.25f` 量化 ⇒ **线宽与虚实段长都正比于 `m_SnapDistance`**。
- **推论**：本模组把滑杆倍率写进 prefab 的 `m_SnapDistance` 之后，若不归还，城市边界那种粗虚线方块会按倍率一直变粗
  （这就是第六轮反馈 2「粗虚线」那一半的来源）。长方形**本身**是游戏画的瓦片边框 —— 本模组没有任何创建区域实体
  或画虚线的代码（全仓库 `dash` 零命中；对既有实体的写入点只有四处，见开发笔记 §〇 ③）。
  ⇒ v0.2.1 新增 `RestoreAllPrefabSnaps`：借了要还（离开创建态 / 关总开关 / 系统销毁三个时机全额还原）。

### FACT 28（v0.2.1，反馈 9「白名单」的取证边界）：游戏自己的区域吸附 job **不按网络类型过滤**

- `Game.Tools/AreaToolSystem.cs:440-508` 的 `NetIterator.Iterate` 只查 `CheckComposition`（`:501-508`，
  唯一排除的是 `CompositionFlags.General.Tunnel`）⇒ 凡是 net 搜索树里的边**都参与**吸附，步道/水道/内部路一视同仁。
- `:651-663` 的 `Snap.ObjectSide` 分支对**任何非圆形**静态物体都贴边（只查 `Game.Objects.GeometryFlags.Circular`；
  `BuildingData` 仅用于把包围盒换成 `m_LotSize * ±4`）⇒ 树、摆件、桥墩在游戏那里也是合法目标。
- ⇒ 本模组的「只认道路/轨道、只认建筑」白名单是**我们自己的口径**，不是抄游戏的；抄游戏的只有
  「中心线 vs 外沿」那一半（`AreaToolSystem.cs:3235-3275` `GetAvailableSnapMask`，即 FACT 12）。
  白名单误杀/漏杀的责任全在我们自己，所以判据必须能离线断言（T3 O8 段）。

### FACT 29（v0.2.1，反馈 9 白名单用的标记位）：SubOwner / OnWater / HasLot 确实存在，且游戏自己就在用

- `Game.Net/GeometryFlags.cs:32-33`：`SubOwner = 0x1000000`、`OnWater = 0x2000000`，
  挂在 `Game.Prefabs/NetGeometryData.cs:30` 的 `m_Flags` 上。
- 游戏自己判「这条 net 归别人所有」就是 SubOwner + Owner 一起查（`Game.Pathfind/LaneDataSystem.cs:525`、
  `Game.Pathfind/ParkingLaneDataSystem.cs:399`）；沿 `Game.Common.Owner` 往上走到建筑的那套走法见
  `Game.Net/ReferencesSystem.cs:701-705`（`while (TryGetComponent(owner, …) && !m_BuildingData.HasComponent(owner)) owner = componentData.m_Owner;`）。
- 建筑侧的「占一块地」标记是 `Game.Objects/GeometryFlags.cs:29` 的 `HasLot = 0x100000`。
- ⇒ 白名单三条判据（埠头=OnWater、内部路=SubOwner 位或 Owner 链走到建筑、真建筑=`BuildingData` 或 `HasLot`）
  的游戏侧证据就是这三个位；「判不出来时按**不是**内部路处理」（fail-open）是**我们的取舍**，不是游戏的口径。

### FACT 30（v0.2.1，反馈 8 的 Lift 误判源头）：`m_ElevatedWidth` 是**宽度**，不是高度

- `Game.Prefabs/NetGeometryData.cs` 里 `m_ElevatedWidth`（`:34`）与 `m_DefaultWidth`（`:32`）并排、都是**宽度**（float）；
  高度是另一对 `m_DefaultHeightRange` / `m_ElevatedHeightRange`（`Bounds1`，同文件 `:18-20`）。
- ⇒ 旧代码把 `m_ElevatedWidth` 当 `Lift`（高度）用是错的：会让「双向六车道高架」的 Lift 变成 24 米，
  进而让 `TraceKit.HeightChanged` 在平地上判出高度切换、白钉一个节点。
  v0.2.1 已改成只在组合确实带 Elevated 位时用 `m_ElevatedHeightRange.max`。

### FACT 31（v0.2.1 出机前追加）：选项页那颗「按钮」是怎么来的，以及 set-only 按钮属性的地雷在哪

- 控件类型只由「属性类型 + 特性」决定：`bool` 且带 `[SettingsUIButton]` ⇒ `BoolButton`；
  再加 `[SettingsUIConfirmation]` ⇒ `BoolButtonWithConfirmation`
  （FACT：`Game.UI.Menu/AutomaticSettings.cs:1051-1060`。这一支排在 `canRead/canWrite` 那两条判断**之前**
  ⇒ 有没有 getter 都还是按钮；反过来，普通 bool 要 `canRead && canWrite` 才成 `BoolToggle`）。
- 点击时游戏只做一件事：`itemData.property.SetValue(itemData.setting, true)`
  （FACT：同文件 `:1169-1191` 的 `AddBoolButtonWithConfirmationProperty` 里那个 action）
  ⇒ setter 只会被递 `true`（不读值、不事后翻回 false），所以 setter 自己写 `if (!value) return;` 是必需的。
- 确认框正文的 key：特性带 `confirmMessageId` 时查 `"Options.WARNING[" + confirmMessageId + "]"`，
  且用 `LocalizedString.IdWithFallback(id, confirmMessageValue)`（FACT：同文件 `:1146-1159` +
  `Game.Settings/SettingsUIConfirmationAttribute.cs:9-15`，参数顺序 **id 在前、兜底文本在后**）。
  ⇒ 这一条与 `ModSetting.GetOptionWarningLocaleID`（`Game.Modding/ModSetting.cs:318-321`）拼出的串**逐字相同**，
  我们的 `LocaleKit.WarningKey` 就是它；反射那一圈注册不到 `Options.WARNING[...]`，必须自己补一行。
  ⇒ 缺词条的后果**不是**显示 key，而是永远显示兜底文本（与下拉框那条 `TRACEMODE[...]` 的事故不同），
  所以实机判据是「弹窗文案跟着语言变不变」。
- 判据来源（游戏自己的两颗按钮，两种写法）：`resetSettings` 是 set-only（FACT：`Game.Settings/GeneralSettings.cs:138-146`），
  `autoSaveNow` 带 getter（FACT：同文件 `:102-118`）。工具链那一页的按钮同理（`ModdingSettings.cs:36-93`）。
- **地雷**：设置基类的 `GetHashCode()` 遍历**所有** public 实例属性并直接 `propertyInfo.GetValue(this)`，
  **没有** `CanRead` 保护（FACT：`Game.Settings/Setting.cs:62-71`）；同文件 `Equals` 在 `:54` 反倒有保护。
  ⇒ set-only 按钮属性是「按钮能用、按 getter 取值的那条路炸」的形状。我们的 `ResetAllSettings` 因此带一个恒 false 的 getter：
  按钮不读值 ⇒ 语义不变；恒 false ⇒ 万一被编码进存档、读回来递的是 false，被 `if (!value) return;` 吃掉，
  不会出现「加载设置时凭空全量重置一次」。
  没上 `Colossal.Json.Exclude`（第三方模组 Traffic Tool Essentials 为此加的最后一道保险）：本工程不引用 `Colossal.Json`，
  为这一颗按钮新开引用不值。⇒ 门禁 **T2c** 静态扫源码：任何 `[SettingsUIButton]` 属性没有 getter 就红。

### FACT 32（v0.2.1 出机前追加）：清掉一颗快捷键要动**三处**，少一处就是假的

- **① 存档里那份 `ProxyBinding`**：它是**结构体**（FACT：`Game.Input/ProxyBinding.cs:12`
  `public struct ProxyBinding : IEquatable<ProxyBinding>, IJsonWritable`）。
  ⇒ `binding.path = ""` 只改副本；游戏的写法是「改副本再整个塞回属性」：
  `WithPath` 用 `path = newPath; return this;`（FACT：同文件 `:714-718`），
  注册的 watcher 用 `property.SetValue(this, newBinding)`（FACT：`Game.Modding/ModSetting.cs:210-216`）。
  ⇒ 持久化走的是字段不是属性：`m_Path` 带 `[Include]`（FACT：`ProxyBinding.cs:255-256`），
  而 `path` 属性本身是 `[Exclude]`（FACT：`:503-515`，set 里顺手 `ResetConflictCache()`）
  ⇒ 赋回属性 = 会进盘。结构体也不可能为 null，注册时那句 `binding.actionName`（`ModSetting.cs:157`）才不判空。
- **② 输入映射表里那份真绑定**：动作早已 `InputManager.instance.AddActions(...)` 注册（FACT：`ModSetting.cs:207-208`），
  只改存档的话那颗键在**本次会话里照样触发**。入口是 `InputManager.instance.SetBinding(副本, out _)`
  （FACT：`Game.Input/InputManager.cs:942-959`）。空 path 能不能递进去看 `canBeEmpty`：
  `SetBindingImpl` 只在 `string.IsNullOrEmpty(path) && !canBeEmpty` 时拒（FACT：`:997-1001`），
  而模组的 composite 没人改过这项，默认 true（FACT：`Game.Input/CompositeInstance.cs:14`、`:88-97`）。
  ⇒ 递空值的写法照抄游戏自己解冲突时的解绑：`resolution2.path = string.Empty; resolution2.modifiers = Array.Empty<ProxyModifier>();`
  （FACT：`Game.UI.Menu/InputRebindingUISystem.cs:687-691`）。`SetBinding` 内部 `action.Update()`（`:1012`）
  会触发 ①里那条 watcher 回调 ⇒ 游戏把有效值再刷回属性，两头自动对齐。
- **③ 那颗动作的开关 `shouldBeEnabled`**：`set true` 时建的是
  `new InputActivator(ignoreIsBuiltIn: false, ..., InputManager.DeviceType.All, enabled: true)`
  （FACT：`Game.Input/ProxyAction.cs:322-347`）。**没有 path 又开着**的激活器 = 按任意键都触发
  （Playbook §3.2 记着的「幻影触发」事故本体）。内置动作那一支直接抛错，只有模组动作能自己开关（`:334-337`）。
  ⇒ `Unbind` 之后按 `action.bindings` 里还有没有可用键决定关不关（判据与 `ZoneSnapperMod.HasBindableBinding` 同源：
  `Engine/InputKit.IsBindablePath`）。这一条是这颗按钮唯一可能弄坏玩家键盘的地方，所以清单里写成优先回报项。

---

### FACT 33（v0.3.0，第八轮反馈 5「路口一堆节点」）：游戏自己怎么认"这是路口"，以及为什么一个路口在我们快照里是 2~5 个节点

- 游戏给接头打的标是**接入的不同 net 实体数**：`GetNodeFlags` 里 `int num3 = 0` 起头，
  每条接入边累加不同的 net 实体，`if (num3 >= 2) handednessFlags.m_General |= CompositionFlags.General.Intersection;`
  （FACT：`research/decompiled/Game.Net/CompositionSelectSystem.cs:469`、`:516`、`:748`；
  `:758-770` 还给"一条路岔出去"那种 T 形补同一个标）。
  ⇒ 我们的 `JunctionKit.MIN_SEED_DEGREE = 3` 是它的等价形式：一个接头接住 ≥3 条**合法**边 ⇔
  进来的那条 + 至少两条别的方向 ⇔ num3 ≥ 2。**度数只数白名单地面网络**（步道/水道/埠头/内部路/地下都不数），
  否则"两条人行道交叉"也会被判成路口（那正是第八轮反馈 1 那一类误吸的来源）。
- 一个路口在游戏的数据里**不是一个点**：
  `Game.Net/Node.cs:10` 是 `public float3 m_Position` —— 一个节点一个位置，没有"这堆节点属于同一个路口"这一层。
  而同一块交叉口会同时存在：主网络节点、副网络节点（`Game.Net/SecondaryLane.cs`）、重叠的 SubNet 节点（`Game.Net/SubNet.cs`），
  以及 `Game.Prefabs/NetGeometryData.cs:31/:38` 那两个 `m_NodeOffset` / `m_MinNodeOffset` 把一条长路拆成的多段节点。
  ⇒ 同一路口在我们的快照里常是 2~5 个 `GraphNode`，位置差几米（人行道那一格最偏）。
- **为什么必须归并而不是"取度数最大的那个"**：走线原来直接采各条边自己的折线端点
  （游戏把边线一路采到**它自己那个节点**上），于是"拐在哪一格"取决于当时用的是哪条边、邻接表谁先进来 ⇒
  玩家看到的是同一条边界每次重算都可能拐在不同点（他截图里那一圈黄点）。归并 + 统一到中心点之后，
  形状只由"这个路口"决定，与遍历顺序无关（回归壳 J10 还钉了幂等：同一对端点重描两次逐格全等）。
- **中心点只取主接头的平均**（度数 ≥3 的那些），副网络那一格不参与平均 —— 把它算进来等于把中心往路边拽几米，
  而那几米就是这条反馈本身的形状。高度取"最高度数那一格"的，不取平均：高架与地面平均出一个两边都不是的值没有意义。
- 归并半径 `MERGE_SPAN = 15` 米是**我们自己的数**（游戏没有"路口半径"这个概念）：
  下界要盖住"四车道 + 两侧人行道"的交叉口内部那段（整幅路宽可到 25~30 米），上界不能吃下一个街区的两个相邻路口。
  偏保守 ⇒ 宁可少并（两个路口各一个中心，走线仍然稳定），不能多并（两个路口并成一个，边界从一个点跳出去，肉眼可见地错）。

### FACT 34（v0.3.0，第八轮反馈 3「地下物体一律不算」）：地下的数据在哪、游戏自己的阈值是多少、以及我们为什么用另一个

- 网络那一侧：`Game.Net/Elevation.cs:8-10` 一条边两端各一个高程（`m_Elevation.x` / `.y`）；
  `Game.Net/NetUtils.cs:594` 那条映射把组合标位翻成碰撞语义：`Elevated → CollisionMask.Overground`、
  `Tunnel → CollisionMask.Underground`；`Game.Tools/NetToolSystem.cs:6078` 与 `:6240` 的
  `requireUnderground` 用的判据是 `elevation < 0 && (elevation <= -3 * ElevationLimit || LoweredIsTunnel)`。
  ⇒ **我们只借数据、不借这个阈值**：那是"这一格能不能连进去"的连通性判据，不是"玩家看的目标在不在地下"。
  采样时把**最低的那一头**存进 `GraphEdge.LowElevation`，引擎层统一用 `Underground = Tunnel || LowElevation < -0.5`
  （下沉式立交/明挖段的地面本来就在路面以下，-0.5 米只是"别把采样噪声当挖深"）。
- 建筑那一侧：`Game.Buildings/BuildingFlags.cs:6-14` **没有**"地下建筑"这个位（别指望有现成标记）。
  可用的是物体自己的落点高程：`Game.Objects/Elevation.cs:8-10`、`ObjectUtils.cs:262-286`（算轮廓时取的就是
  `transform.m_Position` 的 xz 与 y），以及 `Game.Objects/RaycastJobs.cs:228` 用高程分地上/地下那一条查询通道。
  ⇒ 判据 = `transform.m_Position.y < -0.5` 的建筑**在采样阶段就不进快照**（与摆件/树同一待遇：
  留在快照里再过滤也会白占 `OBJECT_LIMIT` 名额，一片地下停车场就能把整条街的真建筑挤出去）。
- 游戏自己贴道路时**也排除隧道**（FACT：`Game.Tools/AreaToolSystem.cs:501-508` 的 `CheckComposition`），
  但它那一条只在路缘吸附那一支；上一版我们据此做成"路缘档禁、中心线档允许"，
  第八轮反馈 3 把中心线档那一半也收了 ⇒ **三类工具的吸附、描边、路口归并**现在都不认地下的东西。

---

## 二、架构

```
┌ Engine/（纯 BCL，一个游戏类型都不碰 → 可离线 T3 断言）
│   GeoKit        点/线/折线基础数学、贝塞尔采样、圆/弧、CurveBounds、CurvesAgree（形状相等判定）
│   WorldModel    世界数据形状：AreaTier/NetKind/SnapKind/AnchorCode/Graph*/WorldSnapshot + IsoKit/ScalarField（海岸等值线）
│   SnapKit       候选打分与优先级、半径分级、贴合结果、FreeNode（吸不上就原位不动）
│   TraceKit      走廊局部图 + 三档侧向 A*(外沿/中心/缘线)、同边切段、环形绕行、逐边定侧、
│                 类型变化钉节点、转角剪角、弦高封顶、失败回退直线
│   SimplifyKit   Douglas-Peucker(自适应容差) + 去重 + 共线合并 + 自交检测
│   CurveKit      两点之间的「弯曲连接」（Catmull-Rom/圆弧拟合 → 少量点）
│   ProjectKit    投影纯函数 f(手动栈, 每边描边, 光标) → 控制点序列 + 右键语义 + 写表四道闸
│   PolicyKit     开关/半径倍率/简化度 → 具体数值；档位↔吸附掩码表；MIN_ASSUMED_ROAD_WIDTH=3；
│                 PlanStroke(一次绘制内的单调走廊规划)
│   AnchorRebind  跨帧锚点的「稳定身份 → 当前快照下标」重绑（认得出就搬下标，认不出才重认，位置永不动）
│   FollowKit     已提交/已落档的那一圈重描：Rebuild(整圈) 与 Around(拖拽节点左右两邻边)
│   PreviewKit    实时预览：Stroke(最后一格→游标) 与 Drag(被拖格→左右邻格)，含四道闸与预算闸；只读，不写任何状态
│   LocaleKit     12 语言文案的唯一来源（标题/说明/快捷键动作名）
│   StoreKit      自管 JSON：缺键安全读、读不全拒绝写盘（Playbook §3.2）
│   InputKit      鼠标射线/视野盒（不依赖游戏输入系统类型）
├ GameSide/（唯一的类型转换层：float3 ↔ Engine 几何、quadtree 抓取、区域实体列举）
│            SnapperState（volatile 镜像 + 日志口）、FallbackTally（回退原因计数）、
│            PreviewFeed（预览信箱：ToolUpdate 发布 → Rendering 消费，两边都在主线程，发布出去的对象只读）
├ Systems/ZoneSnapperSystem.cs      ToolUpdate、UpdateAfter<AreaToolSystem>；绘制中的手动栈投影 + 预览发布
├ Systems/ZoneSnapperApplySystem.cs ApplyTool、UpdateAfter<ApplyAreasSystem>；补写「闭合那条边」
├ Systems/ZoneSnapperFollowSystem.cs ApplyTool、UpdateAfter<ZoneSnapperApplySystem>；
│                                   已提交区域跟随 + 拖拽落档后的重描（预算 2 块/帧、视野盒 320 m）
├ Systems/ZoneSnapperPreviewSystem.cs Rendering、UpdateBefore<OverlayRenderSystem>；
│                                   只把 PreviewFeed 里的几何画成淡色线/圈，异常只关掉预览
├ Patches/（Harmony：Apply / Cancel 两个 postfix，只用于「玩家刚提交/刚撤销」这两件事的通知）
└ Setting.cs（官方 ModSetting 声明式页 + 快捷键标签页 + 12 语言）
```

**为什么这样切**：Playbook 的硬规则 14——「想离线测某个方法，它所在的类必须一个游戏类型都不碰」。贴合打分、图搜索、简化、弧拟合、投影不变量是本模组全部难点与全部 bug 温床，把它们关进纯函数层，就能用 T3 断言（数百条）在不开游戏的情况下迭代；游戏侧只剩「抓数据 + 填列表」。

**为什么只 patch 两个方法**：Playbook 优先「数据编辑」，其次 Harmony。控制点列表本身是公开可写的 ⇒ 投影、描边、撤点修正、重算全部走数据；Harmony 只承担「事件通知」，不改变游戏行为。关掉模组或异常时，最坏结果是「这一次没插描边点」，不会让游戏工具卡死。

---

## 三、核心机制：手动栈 + 幂等投影

模组自己持有一份权威状态（只在 `State.Create` 期间存在）：

```
manual[]   玩家真正点下的节点（含其吸附来源 kind/entity/t）
edge[]     edge[i] = 描边结果（点列 + 该走廊的几何签名/脏标记）
cursor     当前鼠标格（游戏自己维护，我们不抢）
```

每帧（且仅当**手动栈变了 / 光标动了 / 网络脏了**三者之一）执行：
`ProjectKit.Build(manual, edge, cursor)` → 重写 `GetControlPoints()` 那个列表，形状严格为
`[m0, T(m0→m1)..., m1, ..., mN, cursor]`。

这样四件事同时成立：

- **需求 1（不加按钮）**：跟随 `activeTool == AreaToolSystem` 自动生效，`EventToolChanged` 负责清理与重建；没有任何入口按钮。
- **需求 3（右键删的是手动节点）**：`Cancel` postfix 里 `manual.RemoveAt(last)` 再重建投影 ⇒ 游戏的「弹一格」和我们的「弹一个手动节点」永远对齐，描边点跟着整条边一起消失。
- **需求 5（自动跟随）**：投影是 `f(手动栈, 世界几何)` 的纯函数，几何变了就重算受影响边（`GetUpdatedNetBounds` 脏矩形与走廊求交），**画的过程中**区域形状会自己跟上道路变化。**已提交区域**与**拖拽落档之后**的跟随见 §4.5（v0.1.2 的 `FollowKit` + `ZoneSnapperFollowSystem`）。
- **需求 9（两点弯曲 / 少节点）**：见下节。

**这条链有一个隐含前提：栈里的锚点必须一直说得出「我挂在哪」。** 锚点除位置外还带
`Kind/GraphNode/Edge/Arc/Side`，其中两个是**快照数组的下标** ⇒ 快照一重抓就必须随它一起搬家，
否则投影里每一条边都会退回直线（v0.1.2 实机正是这样：`snap=2907` 而 `traceHit=17`）。
这一段单独成层：`Engine/AnchorRebind.cs`，靠 FACT 18 的稳定身份重绑，**位置永远不动**；
`EnsureWorld` 在 `MarkAllStale()` 之前调用它。开局接管与自愈收进来的那些格也走同一口径
（位置照单收下、锚点照认 —— 只认不挪，就不违反「不擅自改玩家存好的几何」）。

闭合处理：游戏自己会把游标吸到 `cp0`（FACT：SnapJob ATS:811-818，条件 `Length>=4` 且距离 < `snapDistance*0.5`），我们保持 `manual[0]` 在列表第 0 格即可，不额外改判定。**最后一条边（mN→m0）的描边只在提交那一刻计算**（避免把描边尾巴插到 `[Length-2]` 破坏间距判定）：提交后立即对生成的区域实体做一次数据层重写（`ZoneSnapperApplySystem`，ApplyTool 相位同帧，见 §4.5）。

### 写表前的四道闸（v0.1.1 实机教训；全部是 `ProjectKit` 里的纯函数，T3 钉死）

投影权力很大（我们写的就是游戏自己的那张表），所以每一道闸的默认动作都是**什么都不写**：

| 闸 | 判据 | 挡住的是哪类事故 |
|---|---|---|
| `Broken` | OnLoad 有任何一环没起来（补丁 0 个 / 主系统没注册） | v0.1.0 本体：装坏的模组每帧抹掉玩家的第一个节点 |
| `GameAcceptedCommit` | 表长 > 我们上一帧写进去的槽位数，才算「这一击落了点」 | 游戏的最小间距 `if` 没有 else（FACT：ATS:3509-3511）⇒ 重复收同一个手动节点 |
| `ShouldAdopt` / `MissingTail` | 栈空而表有点 ⇒ 原样接管；被挡 15 帧 ⇒ 收尾巴 | 编辑/重建路径（填埋区、特殊产业用地）整类被排除在贴合外 |
| `MayWrite` | **要减点就出示证据**：表里每一格都能在「我们上一帧真正写进表的那份投影」里对上（eps 容游戏二次吸附），否则这一帧不写 + `desync++` | 一切「事件漏收 / 表被别人清过」的最坏后果；**同时保住需求 3**（右键撤销必然要减点，第一版「只加不减」的闸会把删掉的角留在表里并触发错误自愈） |

口径：**半死不活比完全不工作有害得多**。四道闸任何一道触发，玩家得到的都是原版手感 + 一行可判读的日志，
而不是「区域工具点不动」。数字口径写在统计行里（`slots=` / `list=` / `rejected=` / `shrink=` / `desync=` / `adopted=`）。

---

## 四、描边与贴合的规则（需求 2、4、8 + 第二轮 T4 的 ②③④⑤ 条）

### 4.1 先定「贴在哪里」：按类别分档，抄游戏自己的掩码表

| 档位（`AreaTier`） | 覆盖的玩家工具 | 吸附目标 | 两端点不都在这条网络上时 |
|---|---|---|---|
| `Centerline` | 市辖区（`District`） | **道路中心线** | **整条边不贴合**，保持玩家画的直线（玩家第 ③ 条） |
| `Lot` | 专门产业区、垃圾场填埋区 | **道路边缘 / 同一建筑外轮廓** | 同上：跨越道路或建筑 ⇒ **拒绝贴合**（这两类不许跨路，第 ③ 条） |
| `Surface` | 表面区域规划、地皮表面区 | **道路边缘 / 同一建筑外轮廓** | 允许吸到对侧，但**跨越那一段保持直线**（第 ③ 条后半） |
| `Coast` | 上面几类 + 海岸开关打开时 | 海岸等值线（`IsoKit`） | 无网络命中才走这一档 |
| `Free` | 玩家关掉总开关 / 类别未覆盖 | 不贴合 | — |

对应 FACT 9～12。**隧道不贴缘、高架照贴**（FACT 10）；`MapTile` 与公园/散步区等点链工具本轮不覆盖（玩家口径）。

### 4.2 同档内谁赢（权重表；第五轮反馈 5 之后**没有**「优先级开关」与 Tab 切换了）

`PolicyKit.KindWeight`（评分 = 距离 × 权重，取最小）：

```
0.7  网络节点/交叉口        （路口本来就是玩家想点的那个角；不再是可调加成，也不许 >1）
0.85 已有区域边界·同类      （反馈 4 要的能力：两个手动点都贴在同类边界上时贴它的节点）
0.95 道路/轨道外沿（人行道外缘档）
1.0  中心线档 / 建筑轮廓档
1.25 已有区域边界·异类
1.3  城市边界线
4.0  自由（不吸）——只在该别的都够不着时才是结果
```

- **档位由类别决定，不是由权重决定**：`KindAllowed(cfg, tier, kind)` 才是闸
  （市辖区收 `NetCentre`、产业区/表面区收 `NetSide|ObjectSide`，抄游戏的 `GetAvailableSnapMask`，FACT 12）。
  外沿类收不到中心线候选 ⇒ 不会出现"路缘拿不到就退到路中心"那种把半条马路划进地块的结果。
- **原模组 Tab 快捷键那一档（优先边界 ↔ 优先网络）整条删除**：三个开关互相没说清谁赢，
  换成 4.8 的五档「贴合模式」。已有区域边界从此**永远允许吸**。
- 隧道不贴缘、高架照贴（FACT 10）；`MapTile` 与公园/散步区等点链工具本轮不覆盖（玩家口径）。

### 4.3 落点吸不上就不许挪（两条硬规矩）

- `FollowKit.AnchorAt`：半径内找不到候选 → **原位不动**（`SnapKit.FreeNode(p)`），绝不「随便找条最近的路贴上去」。
  理由：跟着玩家点的位置走是原版行为，擅自横跳是改玩家的意图。
- 描边失败（`TraceFallback`：`NotOnNetwork` / `SameAnchor` / `NoPathWithinBudget` / `DetourTooLong` /
  `WouldSelfIntersect` / `WouldDegenerate` / `NodeBudget` / `CrossesObstacle`）→ 那一条边退回直线，
  其余边不受影响。原因分开记在每条边的 `TraceResult.Fallback` 与 `FollowResult.EdgeFallbacks` 里；
  **统计行目前只有一个总数 `fallback=`**，实机要看分类分布时再加一行计数（本轮没加）。

### 4.4 描边规则（一条边怎么从 A 走到 B）

- 同一网络曲线的两个节点 → 直接取该贝塞尔的 `tA..tB` 子段（精确且零搜索）。
- 不同网络 → **走廊内局部图 A\***（节点=`Game.Net.Node`，边=该档对应的折线长度，代价=长度+转角惩罚）。
  **外沿档跑三趟**（左缘 / 中心 / 右缘，FACT 17），按「到本区域质心的平均距离」挑**最外围**那条：
  既不穿小巷，也不绕大圈（`k×直线距离` 的绕行硬阀保留）。
- **逐边定侧**（`SideForEdge`）：一条路径经过多条边时，每边各自选「锚点所在侧优先，否则离质心近的一侧」，
  不许整条路径共用一个侧 ⇒ 修掉「绕到路对面再回来」的蛇形。
- **类型/高度变化处必须有节点**（`AddNodePin` + `MarkCorners`，第 ② 条与反馈 4）：在**真正走过的那串边**上
  比较相邻边的 `NetKind` 与 `LayerBits`（道路→铁路、普通道路→公交专用道），再比 `Elevated`/`Lift`
  （地面↔高架、两条不同标高的架开路），变了就在交叉那颗网络节点上钉一个点；锚点两侧走过的边也纳入比较。
- **转角剪角**（`JunctionTrim`，第 ② 条的「严丝合缝」另一半）：过交叉口时两条边在路口各留**该侧自己的**半幅路宽
  不吃进去（`HalfWidth(ge, side)` 读 FACT 22 的 `Left/RightHalfWidth`），
  边界才不会被拉成穿过路口的一根直线；直穿（`cos ≥ 0.8`）与同一条边不剪；`TrimToward` 允许剪到整段消失 ⇒ **幂等**
  （重描两次与重描一次的剪法一致，这是第 ⑥ 条「不许越跟越坏」的前提）。
- 同一环形网络上两点 → **绕行使区域包住街区**：在环的两侧弧里选让多边形面积不退化的一侧（需求 8）。
  ⚠ 第五轮反馈 7 之后**环岛自己例外**：两端落在环上（`ea.OnRing || eb.OnRing`）时不再搜「街区最外围」那两趟，
  沿环岛那条中心线走就是玩家要的（旧行为是绕到环岛外侧那一圈房前路上）。
- **交点必放节点、没有交点就不放**（反馈 4 的核心口径）：`MarkCorners` 在 `Finish` 里、
  **在 `SimplifyKit.FitToBudget` 之前**登记 `TraceResult.Pins`。判据两条：
  ①单格转角 ≥ `tuning.CornerDegrees`；②连续同向偏转的**累计**越过阈值（直线接缓弯时头几米方向差很小，
  但它是"直线与曲线交汇处"）。阈值 = `模式基准角 × (1 − 0.8 × 曲线精细度)` ⇒ 默认档 22°、精细度 100% 时 4.4°。
  顺序反过来就是"简化先把角点抹掉，再按抹完的点说这里没有交点"。
- **节点密度**：贝塞尔密集采样 → `SimplifyKit` RDP，容差 = `路宽 × 该档比例`（比例由**曲线精细度**在
  0.25→0.02（市辖区）/ 0.08→0.01（外沿档）之间插值）再夹到该档上下限，并**封顶到道路宽度**
  （`ChordHardTolerance`，逐条边按真实路宽取最小；宽度读不到按 3 m，FACT 11/16/22）
  ⇒ 「相邻两点的连线不越出道路边缘」这条硬界，弯越急点越多，直线路段自然只剩 2 个点（第 ② 条）。
  手搭的 `ChordTol<=0` 不再等于「不封顶」——它退回档位默认值，防止玩家把外沿档调成穿心直连。
- **单边节点上限不再是设置项**（反馈 7 要"无限"）：只剩 `ModConfig.HARD_NODE_CAP=4000` 这道存档侧硬保险 ——
  一条边走出几千个点意味着走廊或选路已经不对了，与其把这么大的点表塞进游戏的三角化，不如这一条边退回直线。

### 4.5 三种工作模式（第 ③ 条 + 反馈 8）

1. **依次点节点直到闭合**：`ZoneSnapperSystem`（每帧投影手动栈）+ `ZoneSnapperApplySystem`（提交那帧补写闭合边）。
   每对相邻手动节点检查是否都落在本档的吸附目标上，是则描边，否则保持直线。
2. **拖拽已存在的节点**：游戏的表里此时**只有光标**（FACT 14），所以我们不看「先后两下」。
   落档之后由 `ZoneSnapperFollowSystem` 处理，走的是 `FollowKit.Rebuild` = **整圈重描**。
   第五轮反馈 4 把定义钉死之后这是**正确行为**而不是待修的近似：
   「区域创建完成后所有自动节点都视为手动节点」⇒ 重描的输入就是那一整圈手动节点，没有"哪些是我们吸的"要区分。
   （旧笔记里"落档只重描左右两条边"的任务 #16 因此作废；`FollowKit.Around` 仍用于**拖拽过程中的预览**。）
   代价如实写进 README：拖一个角时，其余落在吸附距离内的角点也会被重新吸附。
   **目标识别**不再靠 `Updated` 组件（拖拽过程中根本没有），改成从公开面拿：
   `GetControlPoints(out moveStart, out _)` 里**带 Area 且 `m_ElementIndex ≥ 0`** 的那一格给出区域与格号，
   拿不到就按位置就近认环上格；两条路各自的命中数进日志 `dragIdx=` / `dragPos=`（FACT 20/24）。
   相位：v0.2.0 起本系统跑在 **ToolUpdate**（`UpdateAfter<跟随, 主系统>`）——挂在 ApplyTool 时它只在
   "有提交的那一帧"才被调度，这就是反馈 8「按了重置键还是没法调整贴合区域」的根因（FACT 21）。
   **v0.2.1 更新（第六轮反馈 6）**：落档不再走整环 `Rebuild`——整环自检在密点环上几乎必判自交，
   拖拽落档一个字节都写不进；现走 `TryDragRetrace` + `FollowKit.LocalChain`/`SpliceLocal` 局部拼接
   （只重描被拖格的左右两条边，其余格坐标逐个原样，左右邻居写回**原来**的坐标），见 P3d 与 T3 X9e。
3. **路网变了自动跟随已提交区域**（反馈 8）：同一个系统每帧看 `UpdateCollectSystem.netsUpdated`
   （Modification5 置位 ⇒ 只有 ToolUpdate 前后还读得到，FACT 21）与玩家按「重算」键，
   重描视野盒内的已提交区域。默认关 + 标题写「实验性」（它改的是存档）。
   **不写坏的三道闸**：`GeoKit.CurvesAgree(ring, newRing, tol)` 形状本来就一样 ⇒ 一个字都不写；
   重描结果自交/退化 ⇒ `followReject` 计数 + 区域保持原样；改成功的批次压进单级退回栈
   （`UndoEntry{区域, 改写前的节点表}`，`UNDO_LIMIT=24` 批），玩家用「退回上一次自动跟随」一键还原
   （退回只覆盖本局内存里的批次，跨存档不做 —— 那需要 P3 的存档组件）。
   改写成功后：那块区域的边界描一圈绿色、被挪动的节点闪烁约 4 秒后消失（`FollowFlashFeed` → 预览系统的
   `DrawFlashes`，上限 8 块）。这就是反馈 8 对"显示贴合提示"的重定义。
   **v0.2.1 更新（第六轮反馈 5）**：`netsUpdated` 那一路**不再写存档**，只在去抖后打一行提示
   （`路网/建筑变了：已提交区域不会自动改…`）；写盘的唯一入口是玩家按「重算贴合区域」键。
   开关改名为「允许「重算贴合区域」键改写已保存的区域」、**默认从关改成开**（关着那颗键什么都不会做），
   12 语种标题/说明不再自称「自动跟随（实验性）」。拖拽落档那一路不受这个开关管。

### 4.6 实时预览：第三种「还没发生」的通道（第四轮反馈 ②）

玩家要的是**点下去之前**就知道会贴成什么样。两个入口、一条通道：

| 场景 | 预览内容 | 来源 |
|---|---|---|
| 移动下一个要放的节点 | 最后一格 → 游标那条边的描边结果 + 将被插入的自动节点 + 游标落点圈 | `PreviewKit.Stroke`（复用这一帧已经算出的 `cur`，不重算第二遍） |
| 拖着已存在的节点没松手 | 被拖格 → 左右邻格那两条边的描边结果 + 这一格松手后会落在哪 | `PreviewKit.Drag` → `FollowKit.Around`（与松手后重描同一段代码） |

四条硬口径（都是被前面的轮次教出来的）：

1. **不写进游戏的控制点表**：那张表的形状是「`[…, 最后手动格, 游标]`」，倒数第二格↔游标要过
   `GetMinNodeDistance` 校验（FACT：ATS:3509-3511）⇒ 预览点占进去会把玩家最后一次点击静默吃掉；
   而且左键会把表里**全部**点收进区域。预览只能活在另一条通道上。
2. **淡色是技术必然**：`ControlPoint` 没有颜色通道 ⇒ 走 `OverlayRenderSystem.Buffer`（FACT 20）。
   淡青=将要沿路走出的边界，淡黄=将要真的落进区域的节点；不透明度由玩家调（默认 35%）。
3. **口径必须与落档一致**：同一个头尾、同一份快照、同一套上下文 ⇒ 预览给出的 `Fallback` 与真正落档那次
   描边的 `Fallback` 必须相同（T3 的 P4 钉的就是这条一致性，而不是钉「结果一定好看」）。
   闭合那一帧额外要求把游标补进环尾再报 `RingEdge` —— 否则自交判定会拿一条不存在的弦去比（P4 的注释）。
4. **每帧都算，但要能认怂**：`BUDGET_MS=4` 的预算闸（上一帧整步超预算 ⇒ 这一帧沿用上一帧的预览），
   加上 `preview-off / mod-off / no-world / snap-off / budget` 几道闸门，跳过原因进 `pvSkip=` 账。
   ⚠ `Rendering` 相位排在 `ToolUpdate` 之前 ⇒ 预览最坏晚一帧上屏；这是刻意的取舍，不是 bug。
5. **消失是三件事，不是一件**（反馈 1）：信箱（`PreviewFeed`）只要没人 Clear，渲染端就会把**最后一帧**
   的几何一直画下去。所以
   ①生产端：`ZoneSnapperSystem` 所有"这一帧什么都不做"的提前返回统一走 `Idle(why)`（会话 + 拖拽快照 + 信箱一起撤）；
   ②消费端：`activeTool == AreaToolSystem && state ∈ {Create, Modify}` 才画（FACT 25，不需要补丁）；
   ③消费端还有一道 `Time.frameCount` 新鲜度闸 `STALE_FRAMES=4`（防"连生产系统都不再跑"那种情况——那时 ① 根本没机会执行）。
   ②③命中时打一行 `预览已按「工具不在/几何过期」作废`（每会话只记一次）。

### 4.7 「两个节点弯曲连接」→ 第五轮换成「节点续接」

`Game.Areas.Node` 是**折线**（`float3 m_Position; float m_Elevation`，FACT：Areas/Node dump），游戏数据结构里**没有曲线段**，区域三角化与边界渲染都按折线处理 ⇒ 「两个节点之间的真曲线连接」在数据层做不到。

- v0.1.x 给的替代是"自由点之间可选弧化"（`CurveKit` 生成 n 个中间点）。**第五轮反馈 7 明确不要这一档**，
  整条删除（连同 `CurveFreeEdges/CurveSegments/CurveBulge` 三个设置项）；`CurveKit` 目前作为已测纯几何留在 Engine 里，
  产品代码不再调用它（见开发笔记 §一 限制 10，下一轮决定删或留）。
- 取而代之的是**「节点续接」**（`TraceKit.Continuation`）：两端之间**没有连续**的自动贴合路径时，
  能贴的那一段照样贴出自动点，剩下的用直线（= 手动路径）接上。玩家原话拆成四条硬要求，逐条有断言（T3 T8 段）：
  ①**至少一端**在道路/轨道/建筑边缘上（两端都自由 ⇒ 不续接，就是手动路径）；
  ②**绕行距离仍受规则束缚**：每走一步检查「已走 + 剩下那段直线 ≤ 两端直线 × 绕行上限」，
    且 `Cost` 里连那条直线一起算（续接不许借直线抄近路）；
  ③**角度仍受规则束缚**：接头处转角 > 75° 就地断，且只许朝自由端**前进**（距离没在减少就是在绕远）；
  ④**中断处那一格必须是真节点并钉住**（"续接的节点可以是手动节点，也可以是续接路径上的节点"里说的就是它）——
    锚点在边中间、第一个路口就被闸拦下时循环里那次 `Pins.Add` 不会执行，这个洞是离线壳 T8 抓到的。
  还有一条品味闸：贴出来不足 6 米就整条不续接（断在半路又立刻接直线 = 边界上无缘无故的小折角）。
- 交付口径不变：**「不是真曲线，但弯曲效果与节点数的比值优于原模组」**，写进 README 与选项页说明，不吹「曲线」。

### 4.8 贴合模式（反馈 5：取代整个「贴合优先级」板块）

一个模式 = 一组**具体数字**（`PolicyKit.ModeKnobs`），可断言、可复述，避免"三个开关谁赢没定义"：

| 模式 | 绕行上限 | 转弯代价 | 换类代价 | 搜最外围 | 允许换网络 | 拐点角 | 一句话 |
|---|---|---|---|---|---|---|---|
| 智能（默认） | 3.0 | 0.35 | 6 m | ✓ | ✓ | 22° | 什么都要，按权衡走 |
| 路径最短 | 1.6 | 0.08 | 1 m | ✗ | ✓ | 22° | 只求沿路最短，不为外围绕远 |
| 节点最少 | 4.0 | 0.35 | 6 m | ✓ | ✓ | **40°** | 只有明显拐点才放点 |
| 交点最少 | 4.0 | **1.20** | **24 m** | ✓ | ✓ | 22° | 宁可在同一条路上多走，也少拐弯 |
| 不切换网络 | 2.0 | 0.35 | 400 m | ✗ | **✗** | 22° | 两端各自沿自己那条路走，绝不跳路 |

- 「不切换网络」精确等于旧版那个"允许跨网络类型描边"开关关掉（T3 T2 段有等价性对账）。
- 曲线精细度**不是模式**：它只动两件事——拐点阈值（×(1−0.8×精细度)）与弯道弦高容差，不碰选路代价。
- 模式**不许**动吸附距离（O5 段有这条断言）：一个管选路，一个管能吸多远，混在一起玩家就说不清了。

### 4.9 术语（反馈 4 定的四条，全项目统一口径）

| 术语 | 定义 | 出处 |
|---|---|---|
| 手动节点 | 玩家左键点下的格；**区域创建完成后自动节点一律转为手动节点** | 反馈 4，影响 4.5 的第 2 条 |
| 自动贴合路径 | 两个手动节点之间那一串自动点 | 反馈 4 |
| 手动路径 | 首尾手动、中间**无**自动点 ⇒ 直线 | 反馈 4（= 4.7 里"续接剩下的那一段"） |
| 交点 | 自动贴合路径上的**折线处**：直线↔曲线交汇处、曲率变化处、拐过弯的接头 | 反馈 4；判据见 4.4 的 `MarkCorners` |
| 切换处 | 从一条网络跳到另一条（含**不同高度**）的位置 | 反馈 4；判据见 4.4 的 `AddNodePin`/`HeightChanged` |
| 吻合 | 同一段路上两块区域的节点位置**逐格相同** | 需求 3/4；由"确定性判据不含区域身份"保证（X10） |

### 4.10 第八轮的三件结构性改法（接入点 / 路口中心点 / 跨越判在结果上）

**根因先记清楚，因为它解释了四条不同的反馈**：第六轮把「玩家点的那一格不由模组吸附」落地之后，
手动栈里端点的 `Kind` 基本是 `Free`；而 `TraceKit` 的 0b) 闸要求**两端都贴在能描的东西上**才肯描边
⇒ 正常描边（A* 那条）几乎永远走不到，唯一还能产出网络点的只剩 `Continuation`（节点续接）那条贪心支路。
于是：关掉续接 = 自动贴合整个失效（反馈 2 前半）；开着续接 = 能连通的两端也被强行断在路口（反馈 2 后半）；
"太远就直连"（反馈 6）、"产业区几乎看不到路径"（反馈 8）是同一道闸在别的形状上的表现；
"预览与落档不一致 / 路径不显示"（反馈 4）里也有一部分是它。

1. **接入点模型（`TraceKit.Trace` 公开入口 + `Attach` + `Lead`）**
   端点**一动不动**；模组在端点自己的吸附半径内另找一个**接入锚点**（与玩家点格时**同一个** `SnapKit.FindSnap`，
   所以"吸上来在哪"与"接进来在哪"不会两套口径），走线 =
   `玩家端点 → 接入点 → 沿网络（经路口中心）→ 接入点 → 玩家端点`。
   - 接入点是**自动节点**，按这一档的规则吸（中心线档吸中心线、路缘档吸人行道外缘/建筑轮廓）
     ⇒ 正是反馈 10 那句"只有自动贴合路径上的节点按规则吸附"。
   - 前导线（玩家端点→接入点那两根）过**同一道跨越闸**：它自己横穿马路/建筑时这个接入点不用（退回不接）。
   - 短于格间距的前导线不加（否则直线上凭空多两个几乎重合的自动点 = 第六轮反馈 1 那个症状换个来源）。
   - `Continuation` 退成兜底：只有那一格附近真没有本档该贴的东西时才轮到它。日志里 `attach` 与 `cont` 是此消彼长的一对。
   - 判据不止看 `Kind`：**`StuckOnTarget` = 档位认它 且 它报的坐标真的在那条几何上**
     （`AnchorGap` 用所属折线/路口中心量距离，门槛与 `Lead` 加前导线的门槛同一个数 `max(MinSpacing,0.5)`）。
     这一步是必要的而非锦上添花：反馈 3/10 之后"坐标不许改、但 `Edge/Arc/Side` 要用重锚结果"是常态输入，
     只看 `Kind` 会让走线沿路走完后**凭空**斜着接到那一格（没有节点、也没人管那一跳跨了什么）。

2. **一个路口只有一个代表点（`Engine/JunctionKit.cs` + `WorldSnapshot.NodePoint` + `TraceKit.ClampToJunctions`）**
   取证与取值理由见 FACT 33。落到机制上是四步：度数只数白名单地面网络 → 短边并查集归并 → 组内最高度数 ≥3 才算路口 →
   中心点只取**主接头**平均、高度取最高度数那一格。取用口两处：吸附候选与 A*/续接的接头坐标一律 `NodePoint`，
   交付前把"正好站在成员节点上"的顶点再夹到中心点。
   **顺序不能反**（玩家的 original wording 是"然后再去判断这个点是否需要设置节点"）：
   中心点先算出来，占不占一格仍由 4.4 的 `MarkCorners`/第六轮"直线不补点"决定 ⇒
   直穿一个四岔路口时那个中心点不会被强行钉成一格（回归壳 J11 钉这条）。
   **实机凭据**：夹的那一格数在 `TraceResult.Clamped` 里（本来就在中心 ⇒ 不计），由 GameSide 累进统计行 `junc=` ——
   归并的两个常数（`MERGE_SPAN=15` 米、`MIN_SEED_DEGREE=3`，见 FACT 33）是我们自己定的，
   真图上那种带人行道/辅路的路口跨度可能比 15 米还宽 ⇒ 画过路口 `junc` 一直是 0 就是调常数的信号，不是结构错了。

3. **"跨越"这件事的否决权从端点挪到结果**（反馈 8 与 9 的区别就在这里）
   - 删：`a`、`b` 两点连线的 `WouldCross` 一票否决（连同函数本身）。玩家那一笔压到别的区域/马路，
     不再构成"这一格干脆不贴合"的理由 —— 他要的是"贴出来的路径干不干净"。
   - 留三处，各管一段：**前导线**（`Lead`）、**交付折线逐段**（`Rejects → ResultCrosses`，只对路缘档生效：
     两端分居同一条路两侧时走线会从一条缘线直穿到另一条，等于把半条马路划进地块 = 反馈 8 那句"贴到道路中间去"）、
     **绕行比**（比例闸，见下）。
   - 路缘档还有一道前置禁令：**绝不沿中心线描边**（`TraceSide` 给不出侧时先用区域质心定侧，
     质心也判不出来就不描、留给直线）。宁可直连，也不把边界画到车行道中间。

4. **绕行预算 = 纯比例**（`DetourBudget = 直线 × MaxDetourFactor`）。
   比例是**尺度无关**的 ⇒ "距离"根本进不了判据，这正是反馈 6 要的"节点间无论距离多少都要尝试自动贴合"；
   四分之一矩形弯路的 1.414 在默认档 3.0 / `Shortest` 1.6 下都放行（T10）。
   中途加过的绝对余量（`straight + 60`）已删：它只对短弦松绑，而短弦本来就不撞闸，
   结果只是允许 20 米的两点绕到 80 米 —— 那是玩家不想要的"绕一大圈"。**误诊记录留着，防止下一轮又去调预算。**

5. **尖刺接缝（两条规则打架的产物）**：手动节点不许挪 + 走线自己长接入点 ⇒
   把一格从路缘正上方拽开时，左右两条走线会在**同一个坐标**各接进网络一次，环变成 `…X → P → X…`。
   这种环游戏存得下来但三角化不出东西（= "路径不显示"那一族，与第六轮"两个叠放矩形区块"同源）。
   ⇒ `FollowKit` 两处（整圈重描 + 拖拽）只留贴合内容多的那一侧，另一侧退回玩家那笔直线；计数进日志 `despike=`。
   ⚠ 同处还改了 `Around` 的 `Valid` 判据：老写法要求"被拖那格吸到了东西"，第八轮之后那格本来就不该吸
   ⇒ 正常拖拽会被判成无效、`SpliceLocal` 交回空表、松手后一个字节都不写（第六轮反馈 6 的复发形式）。

6. **地下物体一律出局**（反馈 3；取证见 FACT 34）：`PolicyKit.IsTargetNet` 第一行挡 `Underground`（三类都挡，
   上一版"市辖区可以贴隧道中轴"那条例外被撤），`ObjectAllowed` 挡地下建筑，`JunctionKit.Mergeable` 挡地下边；
   建筑那一侧在采样阶段就不进快照（别白占 `OBJECT_LIMIT`）。高架**不受此限**（第五轮反馈 4 仍成立）。

---

## 五、优先级与分期

| 期 | 内容 | 覆盖需求 | 状态 |
|---|---|---|---|
| **P0** | 贴合（网络/交叉口/区域边界/瓦片边界）+ 手动栈投影 + 右键语义 + 选项页开关 + 快捷键 + 半径分级 | 1,3,4,7 | v0.1.1 上线本地；T4 第二轮发现「贴错对象」 |
| **P0b** | **按类别分档的吸附目标** + 跨越规则 + 外沿选路 + 类型变化钉节点 + 弦不越路缘 + 转角剪角 | 第二轮 ②③④⑤ | **v0.1.2 已实现**，离线 859 条断言，待 T4 第三轮 |
| **P1** | 描边：同曲线切段 + 走廊 A* + 简化 + 自交回退 + 闭合边提交后重写 | 2,4 | v0.1.1 已实现（写表四道闸守着的） |
| **P2** | 环形/环岛绕行、`max(terrain,water)` 高度 | 8 | 已实现。**海岸线贴合与描边 v0.1.4 已整条删除**（玩家实测看不出效果、代价最高）；等值线 `IsoKit` 只留作回归证据 |
| **P3** | 绘制过程中的**自动跟随** + **已提交区域跟随** + **拖拽落档后重描** | 5、第二轮 ⑥、第五轮 ⑧ | v0.1.2 起有代码但 `follow=0`；**v0.2.0 找到根因并搬相位**（ApplyTool 只在提交帧存在，FACT 21），另加高亮闪烁 + 单级退回键。仍待实机确认（开发笔记 §二 第三步） |
| P3b | 移动中 / 拖拽中的**实时预览**（淡色显示点下去会贴成什么样） | ③ 的第二半 + 第四轮 ② | **v0.1.4 已实现**（`PreviewKit` + `PreviewFeed` + `ZoneSnapperPreviewSystem`，走 `OverlayRenderSystem`，不写任何数据）。**没有**去改 Temp 预览实体的节点缓冲——那条路要把 Harmony 补丁面扩进 Burst job，风险高于收益 |
| P3c | 手动节点进存档（`ISerializable` mod 组件），让跟随只重描我们吸过的点 | ⑥ 的完整版 | 未做，见开发笔记 §一 下一步 P3 |
| P3d | 落档后的重描从「整圈」换成「只重描左右两条边」 | ③ 精确化 | 第五轮曾**作废**（反馈 4：创建完成后自动点也算手动点）；**v0.2.1 复活并落地**：整环自检在密点环上几乎必判自交 ⇒ 拖拽落档一个字节都写不进（第六轮反馈 6），现改走 `TryDragRetrace` + `FollowKit.LocalChain`/`SpliceLocal` 局部拼接（只换左右两条边、其余格坐标逐个原样、左右邻居写回原坐标），离线断言 X9e |
| **P6** | **第五轮口径重写**：三个工具开关 + 每类节点吸附距离 + 贴合模式五档 + 交点/切换处规则 + 节点续接 + 城市边界线独立板块 + 12 语言官方用词对账（含繁体独立列） | 第三～七、九条 | **v0.2.0 已实现**；**v0.2.1 按第六轮反馈修正完毕**（白名单 / 按键才写盘 / 拖拽落档局部拼接 / 邻接取齐 / 叠置按最低 / 滑杆写进 prefab 并归还 / 快捷键页那颗重置按钮），离线 **1208** 条断言全绿，待 T4 第七轮 |
| **P4** | 扩展到 `NetToolSystem`/`RouteToolSystem`/`ObjectToolSystem(Curve)` 的点链贴合 | 2 的延伸 | 玩家口径：本轮**不做**（那几类没必要贴合） |
| P5 | 标记配色图例、`OverlayRenderSystem` 描边可视化、非中英语言校对 | 6 的打磨 | 语言文案 v0.1.1 已补齐 12 种，待实机定案 |

**关于需求 5 / 第 ⑥ 条的现状**：QUESTION 1（改 `Game.Areas.Node` 后游戏是否重算三角化/四叉树/归属）仍只有实机能定案，
所以跟随被写成「默认关 + 形状相同不写盘 + 结果可疑就整块不写」的形状；QUESTION 2（mod 组件进存档）暂未依赖 ——
现在的跟随**不需要**跨会话记忆，代价是它会把整圈按当前路网重算，而不只是重吸我们吸过的那几个点。

---

## 六、门禁与验收（不开游戏能测的全先测）

- **T3 断言集**（`tests/t3/`，零游戏 DLL 引用、零联网、跑完 < 5 秒）：
  投影不变量（含 `[Length-2]=最后手动节点` 的形状断言）、**写表四道闸各自的正反例**（P2b `MayWrite` 减点要出示证据、
  P2c `MissingTail` 自愈尾巴、P2d `GameAcceptedCommit` 落点证据、R 段右键对齐）、RDP 简化在容差内且保形、
  同曲线切段方向选择、环绕行不退化面积、自交检测回退、优先级打分与平局、半径分级（不同 `m_SnapDistance` → 不同搜索半径）
  与**不低于游戏最小间距**的钳制（O4）、`StoreKit` 缺键安全 + 读不全拒写、任何 `public string` 选项不返回 null
  （Playbook §3.2 那条 `ArgumentNullException`）、`LocaleKit` 的 L1–L6（key 模板 / 12 语言行数齐 / 取词绝不返回 key 本身 /
  **官方用词对账：只钉从 `Locale.cok` 取得到证的词，并反向守卫"没取证的词不许冒充官方"**）。
  **本轮新增 X1–X10**（`Tests.Targets.cs` + `Tests.Follow.cs`）：逐边定侧、线段与折线相交（含「折点正好压在弦上」）、
  描边沿线不穿心、最外围选路、道路↔铁路变化处必有节点、建筑轮廓档、弦高与曲率封顶、`AnchorCode` 编码、
  `FollowKit.Rebuild`/`Around` 四段、`GeoKit.CurvesAgree` 形状判定（含**重描三次幂等**）。
  本轮再加 **X11–X11d 共 40 条**（`tests/t3/Tests.Rebind.cs`）：同一条路在两份快照里下标不同（0↔1）时
  「不重绑就会拿错边」「重绑之后描边恢复」「每个分支位置都不动」「认不出才重认」「连做两次账不再涨」
  「接管/自愈补的锚点也走同一口径」。
  v0.1.4 加 **X11e**（`PolicyKit.PlanStroke` 的单调走廊：连推三次角点，后一帧的抓取框必须包住前一帧整框，
  且覆盖所有已落档角；超上限时收缩并置 `Clamped`）与 **P1–P7**（`tests/t3/Tests.Preview.cs`，预览：
  含两端契约、四道闸门、闭合那一帧与落档口径一致、拖拽两段接在同一格上、以及「预览改不到手动栈/存档环/快照」）；
  海岸线那几条老断言换成「删除后不抛、不再吐 `-(2e6+s)` 编码、已删档吃 default 权重」的守护断言。
  v0.2.0 再加 **O5–O7 / T8 / T9 / W 段**（五档模式旋钮、曲线精细度→交点阈值、每类档位的合法吸附目标、节点续接八条契约、
  转角与高度变化钉节点、左右半幅宽算式）与 **L6**（31 条官方用词对账：13 格工具名 + 12 格 Map Tiles 原名 +
  两处玩家词优先 + **两条"没取证的词不许冒充官方"守卫**，见 FACT 26）。
  v0.2.1 再加 **X9e / O8 / O9 / T10 / T11**（拖拽落档的局部拼接、目标白名单与跨越判据共用同一份名单、
  叠置平局按最低高度、与邻接区域边界取齐 + `weld=` 计数、路缘档中心线禁令）与 **L4 新三条**
  （FollowCommitted 标题/说明改口径、12 语种不再挂「实验性」）。
  出机前追加「重置所有设置项」那颗按钮时再加 **L4 五条**（名单 25 → 26、`Options.WARNING[...]` 模板逐字、
  说明与弹窗正文各钉一条、繁简 `重置/重設` 必须不同形）。
  **v0.3.0（第八轮）再加 J 段与 T12 段 + 四处改判**：
  新增 **J1–J11**（`tests/t3/Tests.Junction.cs`：一个路口只归并出一个中心点、中心点=主接头平均、
  成员升序与两次建表全等、20 米外的两个路口不许并、直路/纯步道/两腿在地下的那种"路口"根本不成立、
  高架照旧参与、走线在路口只拐在中心点且幂等、**直穿路口时那个中心点不占一格**、
  那一格是"本来就在中心"还是"被挪过来的"由 `TraceResult.Clamped` 分开数（J9 期望 0 / J10 期望 1 ⇒ 统计行 `junc=` 的来源））
  与 **T12**（接入点模型：两端都自由也照贴、关掉续接后同一条走线逐格全等、
  四分之一弯路绕行比恰为 1.414 仍放行、跨街区对角线仍沿网络、附近真没路才直连）。
  改判的四处旧断言都在注释里写了「是哪一轮反掉的」，不留两个口径并存：
  T7/T8（自由端不再等于"只能走续接"）、X3（路缘档的自由端/中心线锚点现在会长出**缘线上**的接入点，
  且走线里不许有中心线上的点）、X9c/X9e（被拖那一格停在玩家松手处 + 左右只留一个接入点）、
  X11（陈旧边下标现在由 `StuckOnTarget` 兜住，但把吸附关掉就立刻退回老缺陷 ⇒ 重绑那一层不可撤）、
  O8（隧道/下沉段三档都不能吸不能描，高架照给，地下建筑不给）。
  ⇒ 当前规模 **1274 条**，`node scripts/verify.mjs` 全绿。
- **T1** `dotnet build -c Release` 0 错误；**T2** 扫部署目录 DLL（`#US` UTF-16LE 双 parity 找字面量、`#Strings` latin1 找符号）：
  **32 个类型名**（v0.3.0 加 `JunctionKit` 与 `Junction`）+ **27 条实机哨兵字面量**
  （v0.3.0 新增 `attach=` / `despike=` / `under=` / `junc=` / 「个在地下（第八轮反馈 3）」，
  这五间账就是这一轮五个修法的机器凭据）
  + **反向哨兵** `吸附距离已写入 prefab` / `吸附距离已还原 prefab`（那套借与还已经删干净，粘回来就红）
  + 版本号双 parity + mtime；**T2c** 源码级扫按钮形状（带不带 getter 由「有没有确认弹窗」决定，FACT 31 那两条分支正好相反）；
  **T2b** 断言部署的 `0Harmony.dll` sha256 == NuGet 缓存 2.3.3 那份。
- **T4 实机清单**（交用户执行）见 `开发笔记.md` §二（**第九轮，v0.3.0**：〇~九步，第一步就是"关掉续接还得贴"）；
  本轮**只本地部署**，不执行任何 `PublishNewMod/NewVersion/Update`。

## 七、已知未验证项（实机前不得当事实用）

1. 填埋区/垃圾场是否真的走 `AreaToolSystem` 的 `Lot`（`ExtractorAreaData/StorageAreaData`）路径。
   v0.1.1 已把「编辑/重建」这一类整个接上（删掉了 `recreate != Entity.Null ⇒ 返回` 的闸，改为开局接管），
   但**它到底是哪条入口**（ObjectToolSystem 的 recreate 还是信息面板 ActionsSection）仍未实机确认 —— 只影响日志读法，不影响覆盖面。
2. 提交后重写区域 `Node` 缓冲的传播完整性（QUESTION 1）：**v0.1.2 起这条从「一次补写」升级成「每帧可能重写若干块已提交区域」**
   ⇒ 实机要看的是重写之后三角化/边界渲染/区属归属/信息面板有没有异常，以及有没有隐形地块。
3. mod 组件能否进存档（QUESTION 2）。本轮不依赖它，但**没有它就分不清「哪些点是玩家手动放的」** ⇒ 跟随只能整圈重描。
4. `GetControlPoints()` 返回的 `JobHandle` 与我们写入的先后关系：设计是 `deps.Complete()` 后在主线程改写；ATS 自己的 `SnapControlPoints` job 是否在同一帧内还会再写这张表（FACT 层面看是在 `Apply/Cancel/Update` 内调度并已返回，需实机确认无竞争与无卡顿）。
5. 大区域（几百条街走廊）描边的单帧耗时；预算上限与分帧策略待实机数字。
6. 描边后多边形的三角化在游戏侧是否偶发失败（`AreaFlags.Complete` 不满足 ⇒ 无法闭合提交），需要实机统计。
7. **模组侧能否真的 `GetBuffer<Game.Areas.Node>()`**：它请求的是 `DynamicBuffer<T>` 那条类型索引（见 FACT 8），
   而 v0.1.0 已经证明**在 OnLoad 时刻**这条索引对模组程序集不存在。补写发生在游戏同一帧建好区域**之后**，
   索引应当已在，但这只有实机能定性 ⇒ 补写系统带「连续 3 次异常即本局停用 + 留一行证据」的闸。
   若实机报停，需求 5 的已提交区域跟随要改走 `AdditionalComponentsRegistry.AddType` 或 `GameAPI` 的写入口。
8. Harmony 的 `Apply`/`Cancel` 两个 postfix 在 1.6.2f1 上是否真的打上（上一版根本没走到那一步）：看 `[Hook] 补丁已安装 2/2`。
9. **选项页文案是否真的不再是 key**（本轮第一优先）：`LocaleKit` + key 模板都齐了，但 `id = assemblyName.modNamespace.modClassName`
   这一串在实机上到底拼出什么，只有开游戏看得见 ⇒ 判据是清单第一步那一格。
10. **`AreaToolSystem.State == Modify` 在实机上是不是「拖角点」那一态**（第 ③ 条第二种模式的入口判定）：
    判错的最坏后果是多跑一次重描，不是写坏存档。
11. **跟随重描在真路网上的幂等性**：离线只在人造路网上证过「三遍不变」，实机判据 = 同一区域连按三次 F6 边界一模一样。
12. **外沿档的每帧代价**：每格多两次缘线探针 + 建筑轮廓收集，`lastMs=` 变贵是预期的；三位数就要看预算与分帧策略了。
13. **两块区域分居公共路段两侧时的「最外围」不一致**（FACT 17 的已知代价）：需求 ④ 在这一种情形下离线证明不了。
14. ~~重绑在真路网上是否够用~~ —— 第四轮已答：`stale=0` ⇒ 重绑本身在工作；真正没解决的是「路根本不在快照里」，
    即 FACT 19。**v0.1.4 的验收核心因此变成**：`resnap=` / `lost=` 在画的过程中基本不涨、`fb=` 不再以
    `NotOnNetwork` 为主、`traceHit > fallback`。若 `resnap=` 仍然大 ⇒ `SignatureOf` 依赖的 `Entity.Version`
    在同一次绘制里就会变，那这套重绑只能算兜底，得换成「按位置重认 + 边稳定性」的口径。
15. **`闭合边已补写：…` 五轮一次都没出现** ⇒ 「模组写 `Game.Areas.Node`」这条通路至今未被实机证实
    （也没被证伪：日志里没有 `连续 3 次无法读写…`）。**第六轮清单第二步专门点一次完整闭合盯这一行**；
    它不出来，§4.5 的跟随与拖拽重描全都谈不上。
16. **淡色预览是否真的上屏**（v0.1.4 新增的唯一「必须靠眼睛判」的行为）：判据是
    `预览已上屏：淡色折线 N 段、节点 M 个` + `pv=` 在涨 + 玩家看得见。三种失败形状（渲染端 / 生产端 / 颜色太淡）
    的读法见开发笔记 §一 未验证 3b。
17. **预览与落档的一致性在实机上是否真的对得上**：离线钉的是「同一头尾 ⇒ 同一个 `Fallback`」（T3 P4），
    但预览与真实提交之间隔着游戏的吸附（ATS:3552-3557 会在我们写完后再挪一次点）⇒ 玩家眼里可能变成
    「淡线说贴、点下去没贴」。第五轮第三步专门验这一条。
18. **闭合边的绕行仍会被侧向过滤挡住**（离线已观测，任务 #19）：方形「三个角 + 游标回起点」这种形状，
    绕外圈那两条边落在弦的同一侧之外 ⇒ `ViaGraph` 搜不到 ⇒ 判 `WouldDegenerate` 退回斜线。
    这是**描边选路**的问题、不是预览的问题（预览只是如实报出来），第五轮请顺便确认实机表现。
19. **`desync=135`（第四轮实测；第三轮是 30）**：走廊单调化之后重抓频率大幅下降，这个数字应当跟着掉。
    不掉就要查「谁在我们的槽位上写了别的点」——目前只能排除高度差
    （`P3.DistanceTo` 是 XZ 平面距离，游戏改高程不会把证据打脏）。
    同一条链路上的 `pop=105 / adopted=9 / 自愈`反复触发 也要一起看。
20. **跟随搬相位之后到底跑不跑**（v0.2.0，反馈 8 的全部赌注）：FACT 21 的读法如果哪里错了
    （比如 `ToolOutputSystem` 之外还有别人在维持 ApplyTool 掩码），实机症状与之前一模一样：`follow=0`。
    判据只有一行：改一条路之后日志有没有 `跟随重描：区域节点 X → Y 个`。
21. **每侧独立半幅宽在真 prefab 上给出什么数**（v0.2.0，反馈 3）：`m_Width` 含不含人行道这件事
    FACT 22 有旁证但没有逐 prefab 实测。判据：在一侧有宽人行道、另一侧只有车道的路上，
    专门产业区的节点应当两侧各贴各的边线；如果两侧贴到同一条线上，就是 `m_MiddleOffset` 没读到（或被别的组件覆盖）。
22. **五种贴合模式的手感排序**（v0.2.0）：`ModeKnobs` 那张表里的数字是**我们从离线断言倒推的**，
    不是从实机调出来的。哪一档"最像我要的"只有玩家能答 ⇒ 第六轮清单第六步第 3 条就是问这个。
    （默认值不动：`radius=100%` 与 `曲线精细度=0` 是按反馈的字面口径设的。）
23. **续接会不会"贴出玩家没想要的折角"**（v0.2.0）：6 米下限与 75° 上限是防这个的两道闸，但阈值本身没在实机看过。
    判据是 `cont=` 在涨而边界上不出现孤立的小折角；出现了就调 `CONT_MIN_LENGTH`/`CONT_MAX_TURN_DEG`，别改引擎结构。
24. **白名单在真实存档上是否误伤**（v0.2.1，反馈 9）：O8 段钉的是离线口径，真路网上「内部路」判据
    **fail-open 的那一半**（SubOwner 位没有、Owner 链又走不到建筑 ⇒ 按"不是内部路"处理）到底漏排多少、
    有没有反过来误杀真路，只有实机能答。判据：日志 `建筑轮廓白名单已生效：这一片 N 个静态物体不是建筑…` 的
    N/M 两个数 + 玩家肉眼（尤其厂区与滨水地段）。
25. **`weld=` 是否真的在涨**（v0.2.1，反馈 7）：`AdoptBorderNodes` 的 2 米阈值与 cos30° 平行判据全是离线数字，
    实机的邻接边界顶点密度够不够、公共段双缝消没消，未证。判据：挨着已有区域画第二块时统计行 `weld=` 从 0 变正。
26. **prefab 吸附距离写入/还原是否被游戏别处覆盖**（v0.2.1，反馈 2/3）：我们写的是 `AreaGeometryData.m_SnapDistance`
    （FACT 27：游戏用它画瓦片边框虚线的粗细），还原时机只有我们自己的三处（离开创建态 / 关总开关 / 系统销毁）；
    游戏若在别处重写这个数（如 prefab 重载），`s_prefabBase` 缓存的原值就过期了。判据：`吸附距离已写入 prefab` 与
    `吸附距离已还原 prefab：实体 #N（tool-left-create）` 成对出现，且城市边界粗虚线复原。
