# Zone Snapper 开发笔记（迭代视角 · 新会话先读「本轮修订」）

模组：Zone Snapper v0.3.0 · 游戏 1.6.2f1 · **只本地部署，未发布**（用户明确要求）
工程根：`D:/WorkSpace/CS2MOD/ZoneSnapper`（源码目录 = 开发工作区 = 未来的 git 仓库，单目录，照 Playbook 步骤 2 的「更优决策」）

上位知识：`D:/WorkSpace/CS2MOD/都市天际线2模组开发标准流程.md`（Playbook v1.0）
设计蓝图（机制链 + 全部反编译证据位置）：`设计文档.md`
本文件只记**本模组专属**的结论与台账；跨模组可复用的东西才进 Playbook。

---

## ☆☆☆、本轮修订（v0.3.0，2026-09-25）—— T4 第八轮反馈：接入点模型 / 路口一个中心点 / 地下排除 / 跨越只判结果

**反馈十条 + 两张截图**，收尾一句仍是「需求可以按你的理解排优先级，开发好本地测试，不要上传到官方平台」。
这一轮的价值不在修了十条，而在挖出**一个缺陷能同时解释四条**——所以先看下面那条根因，再看逐条落点。

### 根因（一条）：手动节点不许吸附之后，正常描边这条路被自己关掉了

第六轮按反馈把「玩家点的那一格不再由模组吸附」落地（第八轮反馈 3/10 又强调一次，带三个感叹号），
于是端点的 `Kind` 基本都是 `Free`；而 `TraceKit` 的 0b) 闸要求**两端都贴在能描的东西上**才肯描边
⇒ 正常描边几乎永远走不到，**唯一还能产出网络点的支路只剩「节点续接」**。
玩家看到的四件事是同一个缺陷的四个侧面：

| 他说的 | 实际发生的 |
| --- | --- |
| 反馈 2「关掉节点续接后预览和自动贴合直接不生效」 | 续接就是唯一的网络来源，关掉=没有自动点 |
| 反馈 2「开着续接变成强制续接，不管有没有连通的路都强行在交叉口断掉再直连」 | 能连通的两端也被丢进续接那条**贪心单跳**支路（每步只挑「离自由端更近」的一条边，转角一超 75° 就断，从来不走 A*） |
| 反馈 4「路径不显示 / 完全不贴合 / 放置与预览不一致」 | 同一道闸在不同形状上的表现 |
| 反馈 6「下个节点离得太远就干脆直线连接，即使有贯通的路」 | 同上；**不是**绕行比拦的（老口径里那条绝对余量是误诊，见下） |
| 反馈 8「产业区/表面区几乎看不到贴合路径」 | 同一道闸 + 路缘档多一条「中心线锚点不算在路上」的判据 |

**修法是一个模型，不是十条补丁**：端点**一动不动**，模组在它附近另找一个**接入锚点**，
走线 = `玩家端点 → 接入点 → 沿网络（经路口中心）→ 接入点 → 玩家端点`。
接入点是自动节点，按这一档的规则吸 —— 正是反馈 10 那句「只有自动贴合路径上的节点按规则吸附」。
代码在 `TraceKit.Trace` 的公开入口（`Attach` + `Lead` 两个私有助手），续接退回**兜底**。

### 判据统一：`StuckOnTarget` = 档位认它 **且** 它的坐标真的在那条几何上

只看 `Kind` 不够了。第八轮之后「`Kind` 说贴在路缘、坐标却在街区里 8 米」是**正常输入**
（跟随/拖拽要拿重锚出来的 `Edge/Arc/Side` 选边选侧，坐标又必须还回玩家那一个）。
这种端点若被当成「已经在路上」，走线会沿路走完后**凭空**斜着接到那一格：那一跳既没有节点，
也没人管它跨了什么 —— 预览与落档就可能各算各的（反馈 4）。
⇒ `AnchorGap`（点到所属折线的距离）+ `StuckOnTarget`（差超过一格间距就该长接入点），
`0b)` 闸、公开入口的 attach 判据、`Continuation` 的「哪一端在路上」三处共用同一个数：`max(MinSpacing, 0.5)`。

### 反馈 5（两张截图）：路口先只有一个点，再谈它要不要占一格

截图里那一圈黄点是**同一个路口的多个成员节点**：游戏给一个接头的标是「接住 ≥2 条别的 net 实体」
（FACT：`Game.Net/CompositionSelectSystem.cs:516` 数 `num3`、`:748` 打 `CompositionFlags.General.Intersection`），
而主网络节点 + 副网络/人行道节点 + 重叠 SubNet 节点 + 按 `NetGeometryData.cs:31/:38` 两个节点偏移拆出来的分段点
在快照里是 2~5 个各自带坐标的 `Game.Net/Node`（FACT：`Game.Net/Node.cs:10` 一节点一位置）。
老写法的拐角 = `world.Nodes[i].Pos`，挑中哪个取决于邻接表顺序 ⇒ 「偏心的节点拐弯」。

⇒ 新增 `Engine/JunctionKit.cs`（纯几何，离线壳 J 段直接断言）：
- 度数只数**白名单地面网络**（隧道/下沉/埠头/内部路/步道都不算，`Mergeable`）；
- 只把**短边（≤15 米）**两端并查集合并 ⇒ 宁可少并不多并（并错=两个路口变成一个，边界从一个点跳出去）；
- 组内最高度数 ≥3 才算路口；中心点**只取主接头**（度数 ≥3 的那些）的平均，副网络那些偏几米的成员不参与平均
  —— 这条正是截图里「偏上偏下」的成因；高度取最高度数那一格的（高架与地面平均出一个中间值没意义）。
- 取用口在 `WorldSnapshot.NodePoint(i)`：属于路口 ⇒ 给中心点。吸附候选、A* 代价/启发、续接、`CloserEnd` 全改走它。
- 交付前再夹一次：`TraceKit.ClampToJunctions` 把「正好站在某成员节点上」的顶点挪到中心点，并去掉并出来的重合点。
  ⚠ 只按「坐标恰好等于某个节点」认（毫米级），不按「离路口多近」认 —— 后者会把弯道最后那一格无辜拽到中心上。
- 反馈 5 的第二半（「然后再判断这个点是否需要放节点，毕竟直线不需要」）由**既有契约**承担：
  J11 断言「直穿过一个四岔路口 ⇒ 中心点不占一格」，用的是第六轮反馈 1 那条直线不补点的规则，没有新开关。

### 反馈 3：地下物体一律出局，三类都算（这一条把上一版的口径反掉了）

`PolicyKit.IsTargetNet` 第一行加 `if (ge.Underground) return false;`，`Underground = Tunnel || 最低端高程 < -0.5`。
- 之前是「市辖区可以贴隧道中轴、只有路缘档禁」——玩家这一轮明确否掉：「所有区域工具…都不包括建在地下的所有物体」。
- 数据侧：`Game.Net/Elevation.cs:8-10` 的 `m_Elevation` 两端高程取**最小**存进 `GraphEdge.LowElevation`
  （游戏的 `requireUnderground` 用的是另一个阈值 —— 那是「这一格能不能连」的判据，FACT：`NetToolSystem.cs:6078/:6240`，
  我们只借数据不借阈值）；`NetUtils.cs:594` 那条 Elevated→Overground / Tunnel→Underground 的映射是这套位的出处。
- 建筑那一侧在**采样阶段**就不进快照（`baseY < -0.5` ⇒ 与摆件同一待遇，不占 `OBJECT_LIMIT` 名额），
  因为留在快照里再过滤会被一片地下停车场挤掉整条街的真建筑。日志行「建筑轮廓白名单已生效…N 个在地下」是它的凭据。
- 路口归并也跟着排除（地下那段本来就不是地面路口的同一个「点」）。

### 反馈 9：跨越的否决权从「你的连线」挪到「要走线自己」

删掉 0) 那道 `WouldCross(a,b)` 一票否决（连同 `WouldCross` 函数本身）⇒ 「中间有别的区域所以不给建」没了。
但反馈 8 的「路径会贴到道路中间去」说明跨越这件事**必须**还有人管 ⇒ 判据挪到结果上，共三处各管一段：
- `Lead`：接入点那两根前导线不许横穿马路/建筑；
- `Rejects → ResultCrosses`：**交付折线逐段**查 `SnapKit.CrossesObstacle`，且只对路缘档生效
  （中心线档本来就沿中心线走，游戏自己也让市辖区跨路）；
- 绕行比：见下面那条。
X2 的断言因此从「期望 CrossesObstacle」改成仍然期望 CrossesObstacle —— 但**理由换了**：
不是「你那两点跨了马路」，而是「贴出来的这条线自己跨了马路」。这个区别就是反馈 8 与 9 的区别。

### 反馈 6：绕行预算退回**纯比例**，中途加的绝对余量删掉

`DetourBudget = straight × MaxDetourFactor`，没有 `+ 60` 那种绝对余量（本轮一度加过，又删了，注释留着讲为什么）。
理由是**比例是尺度无关的**：800 米的弯路和 80 米的同形状弯路绕行比一模一样 ⇒ 距离根本进不了判据。
四分之一矩形弯路（两直角边各 L，弦 L√2）比例 1.414，默认档倍率 3.0、`Shortest` 1.6 ⇒ 本来就放行（T10 钉住）。
反馈 6 的真门槛是**端点**，不是绕行比 —— 拿预算去兜它只会让 20 米的两点被允许绕到 80 米（玩家不想要那种圈）。

### 反馈 4「非常不稳定」里查出来的另一个形状：尖刺接缝

玩家那格不动 + 左右两条走线各自接进网络，如果两侧接的是**同一个坐标**（把一格从路缘正上方拽开 8 米就是这个形状），
环就变成 `…X → P → X…`：同一个位置被访问两次，零宽度的裂缝。游戏存得下来，三角化出来的就是看不见的区域
（这一族对应他说的「路径不显示」，也与第六轮那个「两个叠放矩形区块」同源）。
⇒ `FollowKit` 两处（整圈重描 `Despike` + 拖拽 `Around`）：接缝重合时**只留贴合内容多的那一侧**（点数比大小，平手留左侧 —— 纯粹为了确定性），
另一侧退回玩家自己那笔直线。计数进日志 `despike=`，实机能看出这两条规则有没有在他眼前打架。
顺带把 `Around` 的 `Valid` 判据改了：老写法要求「被拖那格吸到了东西」，
第八轮之后那格本来就不该吸 ⇒ 一次正常拖拽会被判成无效、`SpliceLocal` 交回空表、松手后一个字节都不写
（这正是第六轮反馈 6「放置后还是在原位」会复发的形式）。

### 反馈 1：快捷键页「重置」下面加「关于」板块

三行：模组版本（读 `ZoneSnapperMod.kVersion`，恒有值 ⇒ `StringField` 不会炸）、作者 `yuexian`、三颗跳转按钮
（请我喝杯咖啡 / 论坛页面 / RAINBOW 官网）。按钮是 set-only + `[SettingsUIButtonGroup]`（三颗共用一行），
论坛那颗的说明**照实写**「本模组还没发布，现在能去的是 Access Anarchy 的论坛页」——
门禁 L4 有一条断言专门盯这句话，防止哪天我们把它写成「本模组论坛」（虚假陈述）。
链接用 `UnityEngine.Application.OpenURL`（游戏自己的 Modding 页同一套，FACT：`ParadoxBindings.cs:694-709`），
点击会写一行「关于页已打开链接：X」——浏览器没弹出来时，这行是唯一能区分「没接上」和「被系统拦」的凭据。

### 反馈 7：GPT 那份算法文本没能拿到

分享链接 `chatgpt.com/share/…` 两种取法都被沙箱拦了（WebFetch 与浏览器点击都不行），**没有**照着它改。
本轮的算法是按游戏数据 + 上面的根因分析重写的。你把那段正文贴进来我再对着比一轮，别让我猜它写了什么。

### ☆☆☆ 的账

- T3 断言 **1208 → 1274**：新增 **T12**（接入点模型：两端都自由也照贴、关掉续接逐格全等、四分之一弯路绕行比 1.414、
  对角线跨街区仍沿网络、附近没路才直连）、**J 段**（J1~J11 路口归并与中心点夹取）、
  O8 按新口径重写（隧道/下沉段三档都不给、高架照给、地下建筑不给）、
  X3 重写（路缘档自由端长出**缘线上**的接入点；中心线锚点被重挂后走线里没有中心线上的点）、
  X9c/X9e 重写（拖出去那格停在原地 + 只留一个接入点 + 弯道自动点全在缘线上）、
  X11 改写（陈旧边下标现在被 `StuckOnTarget` 兜住，但关掉吸附立刻退回老缺陷 ⇒ 重绑那一层不可撤）。
- **段号 T10 被第六轮那两段占了**（`weld=` 的邻接取齐 / `T11` 的中心线禁令），所以接入点那一段叫 **T12** —— 
  台账里两套编号并存过一轮，是升账时没先查旧号造成的，改号比改断言内容便宜。
- **收尾时补的那间账 `junc=`**（`TraceResult.Clamped` → `SnapperState.JunctionClamps`）：反馈 5 那条
  「路口只拐在中心那一个点」原本只有肉眼凭据，现在 J9/J10 两条断言把计数本身钉住
  （本来就在中心 ⇒ 0 格；偏心那格被挪过来 ⇒ 1 格），实机才有「归并吃没吃到数据」的可判读数。
- 旧断言里**被撤销的行为**该删就删（T7「自由端必须走续接」、T8③④「两端自由=不贴」、
  X9c「被吸回路缘 x 回到 94」），改判的每条都在注释里写了是哪一轮反掉的，不留「两个口径都对」的悬案。
- 版本 0.2.1 → **0.3.0**（横幅 / `PublishConfiguration.xml` / 商店页 changelog 三处一致，门禁 [0] 钉）；
  仍不发布：`<ModId>` 不填、`AccessLevel=Private`，没跑过 PublishNewMod/NewVersion/Update，不打 tag、不 push。
- 门禁新增哨兵：`attach=` / `despike=` / `under=` / `junc=` / 「个在地下（第八轮反馈 3）」；
  反向哨兵（不许回来的东西）：`吸附距离已写入 prefab` / `吸附距离已还原 prefab`。

---

## ☆、v0.2.1 出机前的追加（2026-09-24）：快捷键页下面那颗「重置」按钮

**要的东西**（用户原话）：「测试前再加个新功能，在快捷键设置下面加一个重置按钮，点击后重置所有设置项回归默认值。」
版本号**不动**（还是 0.2.1，本轮从没上过机），但部署产物变了 ⇒ 认 mtime 与哨兵字面量，别拿版本号当新鲜度。

### 这颗按钮本身：三步，缺一步就是假的

1. **声明**（`ZoneSnapperSetting.cs`）：`bool` 属性 + `[SettingsUIButton]` + `[SettingsUIConfirmation(id, 兜底文本)]`
   ⇒ 游戏给的是 `BoolButtonWithConfirmation`（FACT：`Game.UI.Menu/AutomaticSettings.cs:1051-1060`），
   点击时它只递一颗 `true` 进 setter（FACT：同文件 `:1169-1191`），不读值、不用我们事后翻回 false。
   放在快捷键页新分组 `KeysReset` 里 ⇒ 位置就是「四颗快捷键下面」。
2. **重置**：`SetDefaults()`（游戏自己的复位入口，抽象方法，`Game.Settings/Setting.cs:163`）
   + `m_system.ForceReset()` 丢会话缓存 + `ApplyAndSave()` 落盘（= `Apply()` + `SaveSpecificSetting`，FACT：`:151-155`）。
   顺序照的是游戏「全部恢复默认」那条链：SetDefaults → ApplyAndSave（`SharedSettings.cs:121-130`）。
3. **四颗快捷键单独清**：见下面那条 ③，**这一环最容易以为做完了其实没做完**。

### ③ 的账：解一颗键要清**三处**（这次真挖出来的东西全在这）

- **`ProxyBinding` 是结构体**（FACT：`Game.Input/ProxyBinding.cs:12`）。
  ⇒ 原来那版 `ClearBinding(ProxyBinding b) { b.path = ""; }` 是**纯废码**：改的是取属性时的副本，函数一返回就丢。
  （顺带：那行 `if (binding == null) return;` 拿结构体跟 null 比，本来就编译不过——T1 会拦，不是靠肉眼。）
  ⇒ 现在 `Unbind()` 改副本、**把副本赋回属性**，写法照游戏自己的 `WithPath`（`:714-718`）与
  watcher 的 `property.SetValue(this, newBinding)`（`Game.Modding/ModSetting.cs:210-216`）。
- **映射表里那份真绑定**：只改存档的话，那颗键在**本次会话里照样触发**。
  ⇒ 走 `InputManager.instance.SetBinding(副本, out _)`，`path=""` + `modifiers=Array.Empty` ——
  这三行是照抄游戏解冲突时的解绑口径（FACT：`Game.UI.Menu/InputRebindingUISystem.cs:687-691`）。
  空值不会被拒：`SetBindingImpl` 只在 `!canBeEmpty` 时才拒空 path（FACT：`Game.Input/InputManager.cs:997-1001`），
  而模组 composite 的 `canBeEmpty` 默认就是 true（FACT：`Game.Input/CompositeInstance.cs:14`、`:88-97`）。
- **动作开关 `shouldBeEnabled`**：它只在 OnLoad 按「有没有绑到键」算过一次（`ZoneSnapperMod.cs:263-271`）。
  清空绑定后不摁回 false ⇒ 一颗 `DeviceType.All` 的激活器挂在**没有 path** 的动作上 = **按任意键都触发本模组**，
  正是 Playbook §3.2 记着的那次「幻影触发」事故（FACT：`Game.Input/ProxyAction.cs:322-347`）。
  ⇒ `Unbind` 里查 `action.bindings` 还有没有可用键，没有就 `shouldBeEnabled = false`。判据与 `HasBindableBinding` 同源。
  这条不是可选项：它是这颗按钮**唯一可能弄坏玩家键盘**的地方。

### ④ 顺带挖到的地雷：set-only 按钮属性会踩自己的 `GetHashCode`

游戏那两颗按钮里，`resetSettings` 是 set-only（FACT：`Game.Settings/GeneralSettings.cs:138-146`），
`autoSaveNow` 却带 getter（FACT：同文件 `:102-118`）—— 游戏没解释为什么两套。理由在我们这边：
`Setting.GetHashCode()` **无 CanRead 保护**地遍历每个 public 实例属性调 `GetValue(this)`
（FACT：`Game.Settings/Setting.cs:62-71`；对照同文件 `:54` 的 `Equals` **有**保护，一前一后恰好是那条判据）。
⇒ `ResetAllSettings` 带一个恒 false 的 getter：按钮不读值（`BoolButton` 那条分支不建 accessor），
但恒 false 又保证「万一被编码进存档、读回来递 false」被 `if (!value) return;` 吃掉，
不会在**加载设置时**凭空全量重置一次。第三方证词同向：Traffic Tool Essentials 的注释里记着
set-only 按钮让设置解码中途抛异常、**排在它后面的所有键**（他们的快捷键）静默不加载。
没加 `[Colossal.Json.Exclude]`（TLE 的最后一道保险）是因为本工程不引用 `Colossal.Json`——为这颗按钮新开一条引用不值，
getter 已经堵住取值那一步；真要加，第七轮之后连着「存档键污染」一起判。
⇒ 门禁新增 **T2c**：源码级扫每个 `[SettingsUIButton]` 属性，没有 getter 就红（`scripts/verify.mjs`）。
放在门禁而不是回归壳，是因为 `ZoneSnapperSetting.cs` 引用游戏 DLL，T3 那层「零游戏引用」的壳编译不到它。

### ☆ 的账

- T3 断言 **1200 → 1208**（L4：名单 25 → 26、`WarningKey` 模板逐字、说明/弹窗正文各钉一条、繁简 `重置/重設` 不同形）。
- 实机哨兵 **21 → 22**：新增 `设置已重置`（带 `cleared=4/4 mapOk=N` —— `mapOk<4` 就是「存档清了、映射表没清」，本次会话那颗键还挂在老键上，重启即好）。
- LocaleKit 词条 **+4 行**：`group.KeysReset`、`ResetAllSettings`、`ResetAllSettings.desc`、`reset.confirm`，12 语种齐。
- 门禁段落 **+1**：T2c 按钮属性形状。

---

## 〇、本轮修订（v0.2.1，2026-09-23）—— T4 第六轮反馈：白名单收紧 / 跟随改成按键才写盘 / 拖拽落档真的写进去了 / 与邻接区域节点取齐

**本轮的开局账**（Playbook 规则 22：先看账再动手）：第五轮那份日志（v0.2.0）里有四个读数决定了本轮怎么打——
`闭合边已补写` 六轮一次都没打过、`follow=0` 依旧是 0、拖拽落档一个字节都没写（`followReject` 从会话开头就涨）、
玩家调吸附距离滑杆而游戏那头毫无反应。本轮 16 条改动全部是「代码在、证据链断了」或「口径定错了」这两类，
逐条修完并给每一条留了日志行 + 离线断言。离线门禁全绿：T1 构建 0 错误 / T2 部署产物 / T2b Harmony / T3 回归壳 **1208 条断言全过**（含 §☆ 那颗重置按钮带来的 8 条）。

### ① 闭合边补写（反馈 0）：不是「写不进」，是待补写环在**同一帧被我们自己丢掉了**

- **症状**：六轮实机里最后那条边永远是直的；日志只有 `闭合预览：环点数 …`，`闭合边已补写` 一次都没出现，
  而 `连续 3 次无法读写 Game.Areas.Node` 也没出现 ⇒ 不是游戏拒了我们的写，是**根本没走到写那一步**。
- **根因**：**帧序自杀**。玩家提交闭合的那一帧，区域工具离开 `State.Create`（闭合那一击把 state 置回 Default，
  FACT：ATS:3525）⇒ 主系统同帧走 Idle/`ClearSession`，把待补写环当垃圾丢掉 ⇒ 同帧 `ApplyTool` 相位的
  `ZoneSnapperApplySystem` 去取时拿到 null，静默跳过。两个系统各自都没错，错在「交接物」没有寿命。
- **改法**：给待补写环记龄——`SnapperState.PendingRingAge()`（按 `UnityEngine.Time.frameCount` 记登记帧）+
  `ZoneSnapperSystem.PENDING_RING_FRAMES = 8`：`ClearSession` **只在待补写环超过 8 帧时**才丢弃
  （Systems/ZoneSnapperSystem.cs:1094）。8 帧 ≈ 130 ms 的认领窗口：够同帧的 ApplyTool 先来取，
  又短到不会让上一局的旧环误写进新区域。
- **证据**：日志行 `闭合边已补写：区域节点 X → Y 个…`（ZoneSnapperApplySystem.cs:191）。
  ⚠ 「模组能写 `Game.Areas.Node`」这个命题**六轮仍未定案**——本轮只是把「走不到写」这一层拆掉了，
  第七轮清单第一步仍旧是盯这一行。

### ② 直线上不再多节点（反馈 1）：简化之前先派两个「哨兵」守住手动端点

- **症状**：玩家画一条基本是直的边，中间却被铺出一串自动节点。
- **根因**：`TraceKit.Finish` 把采样点列直接交给 `SimplifyKit`，RDP 的 `keep[0]/keep[last]` 落在**采样端点**
  而不是**手动节点**上 ⇒ 简化产物与手动端点对不齐，接缝处只好补点，直段也跟着遭殃。
- **改法**：送简化前把两个手动端点当**哨兵**插到点列首尾，让 `keep[0]/keep[last]` 恰好落在手动节点上，
  简化后再把哨兵剥掉归还 ⇒ 直线边交付 **0 个自动点**。
- **证据**：X9e 段顺带钉了拖拽那一路的同款判据（「直段街区拖一格 ⇒ 一个自动点都不补」，Tests.Follow.cs:385）；
  描边那一路由 T11 段与既有的 T9「完全直路的走线零钉」共同守着。

### ③ 两个叠在一起的粗虚线长方形（反馈 2）：长方形不是我们画的，但「粗」这一半是我们的锅

- **症状**：画区域时地上出现两个叠在一起的粗虚线长方形，玩家怀疑模组偷偷建了区域。
- **排查结论**（这条判据要留在账上，下一轮再出现先按它分诊）：本模组**没有任何创建区域实体的代码**——
  全仓库只有 `EntityManager.GetBuffer/SetComponentData/AddComponent(Updated)` 这类对**既有**实体的写入，
  写入点只有四处：`ZoneSnapperApplySystem.cs:175-180`（闭合边补写）、`ZoneSnapperFollowSystem.cs:429-435`（跟随重描）、
  `:497-503`（拖拽落档）、`:556-560`（撤销还原）；也**没有任何画虚线的代码**（全仓库 `dash` 零命中）。
  粗虚线长方形是**游戏自己画的地图瓦片边框**（FACT 27：`AreaBorderRenderSystem.cs:378-395` 只对带
  `Game.Areas.MapTile` 的 chunk 且非编辑器模式时 `dashedLines = true`）。
- **根因（「粗」的那一半）**：FACT 27 的另一半——`DrawEdge` 用 `m_SnapDistance` 同时决定**线宽**与**虚实段长**
  （`:544-552`，`num` 由 `m_SnapDistance * 1.25f` 量化）。我们上一轮把滑杆倍率写进了 prefab 的 `m_SnapDistance`
  却**没还**，游戏就一直按放大后的数画瓦片边框 ⇒ 倍率拉到 300% 时粗虚线方块按 3 倍变粗。
- **改法**：**借了要还**——新增 `RestoreAllPrefabSnaps(reason)`（Systems/ZoneSnapperSystem.cs:159），三个时机全额还原：
  ①离开创建态（reason=`tool-left-create`）②总开关关掉（`mod-disabled`）③系统销毁（`system-destroy`）；
  还原打日志 `吸附距离已还原 prefab：实体 #N（reason）`。
- **证据**：哨兵字面量 `吸附距离已写入 prefab` 在部署 DLL 里（T2）；实机判据是第七轮清单第三步
  「离开区域工具后城市边界的粗虚线恢复原粗细」。

### ④ 手动节点不再被模组吸附 + 滑杆真的生效（反馈 3）：两件事，一个共同的口径错误

**前半：手动节点被模组乱吸。**

- **症状**：玩家自己点下去的格被模组挪到路上；空地上点不出「贴着鼠标的小吸附」也一并被报上来。
- **根因**：旧口径把「手动节点也要吸」当成了功能。但玩家点的位置就是玩家的意图（§4.3 那条硬规矩本来只护住了跟随路，
  没护住绘制路），且游戏自己的节点吸附（`GetAvailableSnapMask` 那套，FACT：ATS:3235-3275）**一直在工作**——
  我们再吸一次就是双重吸附，玩家看到的「乱跳」有一半是我们贡献的。
- **改法**：`ZoneSnapperSystem.ManualNode(raw, tier, cfg, tuning)`（Systems/ZoneSnapperSystem.cs:502）——
  只有**城市边界（MapTile）**那一档还跑 FindSnap（那一档游戏自己完全不吸，我们不吸就没人吸），
  其余一律 `SnapKit.FreeNode(raw)` 原位落栈；命中/未命中计数也只对 MapTile 记。
- **证据**：X9e 段把这条钉进了拖拽拼接（「左右邻居写回的是它们**原来**的坐标」，Tests.Follow.cs:393）。

**后半：吸附距离滑杆（50%~300%）拉了没反应。**

- **根因（两层）**：①`IntSliderField` 的写入路径可能绕过 setter（游戏选项页对设置对象做反射写入，
  FACT：`AutomaticSettings.cs` 那套模板机制），我们的「改了才重算」根本收不到通知；②倍率只活在我们自己的
  `ResolvedTuning` 里，**游戏自己的数据一点没变** ⇒ 游戏内置的节点对齐仍按原版 `m_SnapDistance` 走。
- **改法**：①`ZoneSnapperSetting.Stamp()`（16 个字段做指纹）+ `PollSync()`（ZoneSnapperSetting.cs:339），
  三个系统的 OnUpdate **每帧对一次指纹**，不依赖 setter 被调到；②`ApplyPrefabSnap` 用
  `EntityManager.SetComponentData` 把倍率写进 prefab 实体的 `AreaGeometryData.m_SnapDistance`，
  原值缓存在 `s_prefabBase`，`minNodeDistance = desired * 0.5`（对齐 `AreaUtils.GetMinNodeDistance` 的口径，FACT 5）。
  prefab 实体靠**反射**从 `AreaPrefab` 上那个 `Entity` 类型的成员取（`AreaPrefab` 没有公开的 Entity/Index）。
- **证据**：日志 `吸附距离已写入 prefab：实体 #N 原版 X → Y`（Systems/ZoneSnapperSystem.cs:612）与
  `吸附距离已还原 prefab：实体 #N（tool-left-create）`（:174）；实机判据是清单第三步（滑杆 50%/300% 立刻生效，
  含游戏内置节点对齐；离开工具后粗虚线复原——与 ③ 是同一根链的两头）。
  ⚠ 「写进 prefab 的值会不会被游戏别处覆盖回去」**只有实机能答**，已列进 §一 未验证。

### ⑤ 选项页文字（反馈 4）：三个开关的说明删掉，下拉框的 key 拼错了

- **症状**：三个区域类型开关的说明文本玩家不要（标题已经说完了）；贴合模式下拉框显示的是
  `Options.TRACEMODE[…]` 这种 key 而不是中文。
- **根因（下拉框）**：枚举选项的 key 模板少拼了**设置 id 前缀**。游戏自己拼的是
  `"Options." + id + "." + 枚举类型名大写 + "[成员名]"`（FACT：`Game.UI.Menu/AutomaticSettings.cs:846-855`、`:906-913`），
  而我们只注册了不带 id 的那半截 ⇒ UI 查无此词、原样吐 key。
- **改法**：①删掉 `SnapDistrict.desc` / `SnapLot.desc` / `SnapSurface.desc` 三行（12 语种一起删），
  `LocaleKit.kLabelOnlyRows` 把这三项豁免出「每行必须有说明」的断言；②下拉框 key 修正为
  `LocaleKit.EnumKey(id, enumTypeName, memberName)` → `"Options." + id + "." + ENUMNAME.ToUpperInvariant() + "[member]"`，
  `ZoneSnapperSetting.kSettingId = "ZoneSnapper.ZoneSnapper.ZoneSnapperMod"`。
- **证据**：L4 段断言（Tests.Locale.cs：`"Options." + ID + ".TRACEMODE[FewestCorners]" == EnumKey(...)`）；
  实机判据是清单第二步（三个标签页文字正常、三个开关**没有说明文本**、下拉框显示中文）。

### ⑥ 自动跟随改成「按键才写盘」（反馈 5）：模组自己**绝不**碰存档

- **症状**：玩家改了一条路，视野外的已提交区域被动跟着变，「不太靠谱」。
- **口径重写**：路网/建筑变更那一路**不再写存档**——`netsUpdated` 只在 `m_netLatch` 去抖后打一行
  `路网/建筑变了：已提交区域不会自动改，要按「重算贴合区域」那颗键才写盘（第六轮反馈 5 的口径）`
  （Systems/ZoneSnapperFollowSystem.cs:237）。**唯一会批量改写存档的路径**只剩玩家按「重算贴合区域」键。
- **连带改动**：`FollowCommitted` 这个开关的语义从「允不允许自动跟随」变成「**允不允许那颗键改写已保存的区域**」，
  **默认值从 false 改成 true**（ZoneSnapperSetting.cs:218/:423）——关着的话那颗键什么都不会做，
  玩家只会以为模组坏了。关着时按键会打一行 `按了「重算贴合区域」，但选项页的「允许『重算贴合区域』键改写已保存的区域」
  是关的 ⇒ 这次什么都没写`（:254，一局只说一次，开关重新打开会复位 `m_keyBlockedLogged`）。
  标题与说明在 **12 个语种**里全部改口径，不再自称「自动跟随（实验性）」。拖拽那一路（⑦）**不受这个开关管**：
  拖拽是玩家亲手改的，落档写回不算「模组自作主张」。
- **证据**：L4 新增三条（标题含「重算贴合区域」且不含「自动跟随」/ 12 语种不再挂「实验性」/ 说明写清
  「绝不改存档、按键才写、能退回」三件事，Tests.Locale.cs:143-163）；哨兵 `不会自动改` 在部署 DLL 里（T2）。

### ⑦ 拖拽落档真的写进去了（反馈 6）：自检拿整环判自交，密点环几乎必拒

- **症状**：拖一个已闭合区域的格，淡色预览明明画出了贴合结果，松手后**存档一个字节都没变**。
- **根因**：老实现落档走整环 `FollowKit.Rebuild`，自检也拿**整环**判自交——已提交区域的环上有大量贴路密点，
  整环自检几乎必判 `WouldSelfIntersect` ⇒ `followReject` 从会话开头就涨、写盘永远到不了。
  「预览有、落档没有」的分裂就是这么来的：预览走的是局部两条边，落档走的是整环。
- **改法**：`ZoneSnapperFollowSystem.TryDragRetrace`（:457）+ 引擎侧新的纯函数 `FollowKit.LocalChain` /
  `FollowKit.SpliceLocal`（Engine/FollowKit.cs:223/:250，**拼接口径只有一处**，系统与离线壳共用）。
  三条硬规矩：**只重描被拖格的左右两条边**；其余每一格坐标逐个原样；左右邻居写回的是它们**原来**的坐标
  （不是重锚后的位置——否则就是模组挪了玩家没碰的格子，违反 ④ 前半那条口径）。自检只看**被替换的局部链**。
- **证据**：X9e 段（局部拼接：只换左右两条边、其余格坐标逐个全等、认不出被拖格 ⇒ 交回空表绝不猜、
  null/短环不抛）；日志 `拖拽落档重描：区域实体 #N 第 K 格，左边 X 点 / 右边 Y 点（回退 a/b），环 M → N 个节点`（:509）。

### ⑧ 与邻接区域节点取齐（反馈 7）：共线段的坐标**逐位照抄**，不是「贴得很近」

- **症状**：挨着已有区域画第二块，公共段两边的节点差着几十厘米，放大就能看出双缝。
- **改法**：`TraceKit.AdoptBorderNodes`（Engine/TraceKit.cs:851，在 `Finish` 的简化**之后**跑，:802）——
  我们每个自动点若在 **2 米**内找到某条既有区域边界的顶点、且两边方向大致平行（cos30°，比的是各自的进/出两段，
  这样拐角上的节点也认得出），就把坐标**替换**成那个顶点（逐位相同，不做插值、不再判曲率）；
  相邻两点认到**同一条**边界时，把它们之间的边界顶点也补进来（上限 24 个），
  并把节点预算按补进来的个数放宽——否则「超预算 ⇒ 退回直线」会把共线段变成斜线。
  只在 `UsedNetwork` 的走线上跑（手动路径没有取齐的对象）。
- **取齐链**：`TraceResult.Adopted`（Engine/TraceKit.cs:56）→ `SnapperState.AdoptedBorderNodes`（GameSide/SnapperState.cs:138）
  → 统计行的 `weld={36}`（Systems/ZoneSnapperSystem.cs:460）。
- **证据**：T10 段（与邻接区域边界重叠 ⇒ 坐标逐位照抄 + 补中间顶点 + `Adopted` 计数被报出来）。
  ⚠ `weld=` 在实机上是否真的上涨仍未证——取齐只在**绘制**那一路生效，跟随与拖拽重描那一路**不抓邻接边界**，
  理由是自吸回自己的旧边界会让跟随空转（已写进 README 已知限制）。

### ⑨ 叠置路网平局按最低的那条（反馈 8）：高架压在地面路上时，胜负原来交给遍历顺序

- **症状**：高架与地面路叠在一起的地方，市辖区有时吸高架上、有时吸地面，同一条路画两次结果不一样。
- **根因**：`P3.DistanceTo` 只看 X/Y ⇒ 叠置的两条路给出的距离**严格相等**，老口径的平局由四叉树遍历顺序裁决。
- **改法（两处，方向一致）**：①`PolicyKit.HeightTieCost(ge)`（Engine/PolicyKit.cs:501）=
  max(0, 中心线两端世界高度均值) × `HEIGHT_TIE_WEIGHT`（1e-3）——10 米高的高架只贵 0.01 米 ⇒
  **只打破严格平局，动不了真实路线选择**；加进 A* 的 `through`（Engine/TraceKit.cs:1237），
  **不加进 `geom`**（geom 是交付给下游的真实几何长度，掺了权重就是假长度）。②`SnapKit.Best` 在分数打平（±0.05）时
  改判「高度低者胜」。顺带修了 `WorldSampler` 的 Lift 源头——旧代码把 `m_ElevatedWidth`（**宽度**）当高度用，
  「双向六车道高架」的 Lift 会变成 24 米，让 `TraceKit.HeightChanged` 在平地上判出高度切换、白钉一个节点（FACT 30）；
  现在只在组合确实带 Elevated 位时用 `m_ElevatedHeightRange.max`。
- **证据**：O9 段（地面路零代价、10 米高架恰好 0.01、海平面以下夹到 0、null 不抛；两种插入顺序都判低者胜）。

### ⑩ 目标白名单（反馈 9）：步道 / 水道 / 埠头 / 内部路 / 摆件一律不是目标

- **症状**：产业区的角被吸到步道上去、边界沿着厂区内部小路描、树和桥墩也算「建筑轮廓」。
- **先把话说清楚**：游戏自己的区域吸附 job **不按网络类型过滤**（FACT 28：`AreaToolSystem.cs:440-508` 的
  `NetIterator.Iterate` 只查 `CheckComposition`，唯一排除的是隧道；`:651-663` 的 `Snap.ObjectSide` 对任何非圆形
  静态物体都贴边）⇒ **这份白名单是我们自己的口径，不是抄游戏的**；抄游戏的只有「中心线 vs 外沿」那一半
  （FACT：ATS:3235-3275）。所以白名单错杀/漏杀的责任全在我们，判据必须能离线断言。
- **改法**：引擎侧一组纯函数 `PolicyKit.IsTargetNet / NetAllowed / ObjectAllowed / TraceEdgeAllowed /
  NodeAllowed / AllowedEdgeOf`；三个新字段 `GraphEdge.OnWater`、`GraphEdge.Internal`、`ObjectRef.IsBuilding`
  由 `GameSide/WorldSampler` 填——OnWater/SubOwner 来自 `NetGeometryData.m_Flags`（FACT 29：
  `SubOwner = 0x1000000`、`OnWater = 0x2000000`，Game.Net/GeometryFlags.cs:31-32）；Internal 还可以由
  Owner 链走到建筑成立（走法抄游戏自己的：FACT 29 的 ReferencesSystem.cs:701-705），**判不出来时不算内部路
  = fail-open**（宁可漏排一条内部路，不可误杀一条真路）；IsBuilding = prefab 有 `BuildingData` 或几何标记带
  `HasLot`（FACT 29：Game.Objects/GeometryFlags.cs:28）。
- **落地位置（一处都不能少，少了就自相矛盾）**：`SnapKit.FindSnap` 的边循环（Engine/SnapKit.cs:261）、
  交叉口循环（交叉口候选必须至少接着一条合法边，锚点边用 `AllowedEdgeOf` 挑）、建筑轮廓循环；
  `TraceKit.ViaGraph` 与 `Continuation` 都改用 `TraceEdgeAllowed`（把原来的隧道禁令折进去）；
  `SnapKit.CrossesObstacle` 用**同一份**名单——否则会出现「步道不是目标、却能把产业区判成跨越了道路」的自相矛盾。
  `KindAllowed` 里 `NetNode` 从「两档都允许」改成**只在中心线档允许**（路口中心是中心线上的点，
  4 车道路上离人行道角点有十来米，外沿档收它就是吸错层）。`WorldSampler.CollectObjects` 在**抓取阶段**就跳过
  非建筑（否则一片树林会把 `OBJECT_LIMIT` 的名额吃光，把真建筑挤出去），并一局打一行
  `建筑轮廓白名单已生效：这一片 N 个静态物体不是建筑（树 / 摆件 / 桥墩之类），已排除在贴合目标之外；另有 M 个建筑拿不到轮廓`
  （GameSide/WorldSampler.cs:551）。
- **证据**：O8 段（道路/轨道三档共同目标、高架仍算路、步道/水道/分类不明一律不是、埠头与内部路即使挂 Road 标记也排除、
  隧道只在路缘档被禁、建筑档只认建筑、只有步道经过的端点不是交叉口候选）。

### ⑪ 路缘档绝不沿中心线描边（反馈 9/10 的底层修正）：定不出侧就不描

- **症状**：产业区的边界偶尔画到马路中间去。
- **根因**：路缘档的走线在某些边拿不到「贴哪一侧」，旧代码退回中心线描边——对市辖区那是正解，
  对路缘档就是把地块边界画到马路中间。
- **改法**：`TraceKit` 在 `side == SIDE_CENTRE` 且是路缘档时，先用 `SideTowardCentroid(ea, ctx)`
  （Engine/TraceKit.cs:627，质心离哪条外沿近就贴哪一侧；调用点 :247/:377）定侧；**定不出来就不描边**，
  交给续接/直线——宁可直连，也不能把地块边界画到马路中间。`Continuation` 里同口径加了一道。
- **证据**：T11 段（路缘档绝不沿中心线描边；没质心就不描）。

### ⑫ 规则冲突审计（反馈 10）：做了四件，剩两件要实机日志才能定

已落地的四条统一：白名单与跨越判据**同一份名单**（⑩）、`NetNode` 档位统一（⑩）、路缘档中心线禁令统一（⑪）、
叠置平局统一按最低（⑨）。**没做**的两条（不是忘了，是没有判据）：智能档（Smart）的旋钮数值调优、
续接中断点口径复核——两件都要等第七轮日志里的 `mode=` / `cont=` / `fb=` 分布才能定，离线调参是拍脑袋。

### ⑬ 跟随与拖拽的日志现在带区域实体号（反馈 2 的排查需要）

`跟随重描：区域实体 #N 节点 X → Y 个（沿路 a 条边，直线 b 条，挪位 c 格，类别 T）`（ZoneSnapperFollowSystem.cs:445）、
`拖拽落档重描：区域实体 #N 第 K 格…`（:509）。没有实体号，「哪块区域被改了」在日志里对不上号，
反馈 2 那种「两个长方形」的排查就只能靠猜。

### 台账变化

- T3 断言 **1126 → 1200**（新增 **X9e** 拖拽局部拼接、**O8** 目标白名单、**O9** 叠置平局按最低、
  **T10** 邻接边界取齐、**T11** 路缘档中心线禁令，**L4** 加三条 FollowCommitted 新口径）。
- 实机哨兵字面量 18 → **21**（新增 `拖拽落档重描`、`不会自动改`、`吸附距离已写入 prefab`）；门禁类型清单仍 30。
- `FollowCommitted` 默认 **false → true**，12 语种标题/说明全部改口径（不再挂「实验性」）。
- 统计行新增读数 `weld={36}`（照抄邻接区域节点坐标的格数）。
- 版本 0.2.0 → **0.2.1**；仍不发布（`PublishConfiguration.xml` 无 `<ModId>`、`AccessLevel=Private`）。

### 这一轮还没解释清楚的（别当成已解决）

1. **闭合边补写：修的是「走不到写」，不是「写不进」**——`闭合边已补写` 六轮从未出现，本轮拆掉了帧序自杀这一层，
   但「模组能不能写 `Game.Areas.Node`」仍要第七轮日志定案（清单第一步）。
2. **白名单会不会在真实存档上误伤**：尤其「内部路」判据 fail-open 的那一半（判不出来 ⇒ 按不是内部路处理）——
   厂区里那些既没 SubOwner 位、Owner 链又走不到建筑的路上，产业区照样会贴上去。判据：`建筑轮廓白名单已生效` 那一行
   的 N/M 两个数 + 玩家肉眼。
3. **`weld=` 是否真的在涨**：取齐的 2 米阈值与 cos30° 平行判据全是离线数字，实机上邻接边界顶点密度够不够、
   涨不涨、涨了之后双缝消没消，都要第七轮看。
4. **prefab 吸附距离写入/还原会不会被游戏别处覆盖**：我们只在自己这三处时机还原，游戏若在别处重写
   `AreaGeometryData.m_SnapDistance`（比如 prefab 重载），`s_prefabBase` 缓存的原值就过期了。
   判据：日志里 `吸附距离已写入 prefab` / `吸附距离已还原 prefab` 成对出现，且城市边界粗虚线复原。
5. **智能档旋钮与续接中断点**（⑫ 没做的两条）：等第七轮的 `mode=` / `cont=` / `fb=` 分布。
6. **`desync` / `pop` 的老账没动**：本轮 16 条改动没有一条碰手动栈同步链路，第五轮那两个数（15/76）如果第七轮还在，
   就照旧结论查「Cancel 之外还有谁在改那张表」。
7. **重置按钮的三件只有实机能定的事**（详见 §☆ 与清单第十一步）：①点完按钮**面板上的控件**会不会当场回到默认值
   （游戏的控件是拿 accessor 取值的，理论上会刷新，但我们没实测过一条「不是玩家自己改的」的刷新）；
   ②`InputManager.SetBinding` 递空 path 对我们那四颗动作到底认不认（`canBeEmpty` 默认 true 是反编译的口径，
   FACT 见 §☆，实机判据是 `mapOk=4`）；③清空绑定后 `shouldBeEnabled=false` 有没有真挡住幻影触发。
   ②失败不影响正确性（重启即好），③失败是**事故**，所以清单里把它写成优先回报项。

---

## 〇b、本轮修订（v0.2.0，2026-09-22）—— T4 第五轮反馈：口径重写（3 个工具 / 节点吸附距离 / 贴合模式 / 交点 / 续接），跟随的相位根因挖到底

**玩家反馈九条**（原文在会话里，这里只留可验证的口径）：① 预览不会自动消除（第二个节点没放就退出工具或按 ESC，
淡线和淡节点还留在地上）；② 总开关默认打开、区域类型开关"没生效"、生效类型只留三个并改名对应游戏内三个工具
（行政区域→**市辖区**、地块与产业用地→**专门产业区**、地表贴图区域→**表面区域**），另外两个移到新板块**"特殊吸附目标"**；
③ 贴合范围改成**"节点吸附距离"**、按三类各自单独设置、50%~300%、默认 100%=原版距离、
市辖区吸**中心线**、产业区与表面区吸**道路/轨道/建筑边缘**且"边缘"指**人行道外缘不是车道路缘**、含高架；
④ 术语重定义（**自动贴合路径 / 手动路径 / 交点 / 切换处**）+ **创建完成后所有自动节点都视为手动节点** +
**交点必放节点、没有交点就不放** + 切换处必放节点 + 同一段路两侧节点完全吻合 + 产业区要贴"那个产业区那一段"的节点；
⑤ 删掉"贴合优先级"板块，换成**"贴合模式"下拉框**；⑥ 删掉太空区域、地图瓦片改名**"城市边界线"**；
⑦ 实时预览移到第 2 页并改名**"其它设置"**，该页只留曲线精细度（默认 0）/自动跟随/实时预览，
描边默认允许且不显示，环形道路**沿环岛中心**，节点上限无限，删掉"自由点走弧线"换成**"节点续接"**；
⑧ 自动跟随：去掉"绘制过程实时跟随"，"按了重置键还是没法调整贴合区域，请修复，不稳定可以标实验性"，
"显示贴合提示"看不懂 ⇒ 改成**高亮被调整过的区域边界 + 被调整的节点闪烁一会儿后消失**，
并新增**"退回上一次自动跟随的调整（强烈建议设置）"**快捷键；⑨ 本地化必须用**游戏官方用词**。

按 Playbook 规则 22（先看账再动手），本轮同样是先读 v0.1.4 那一份日志再改代码。

### ⑧ 的根因（本轮最值钱的一条）：跟随系统挂在一个**大多数帧根本不存在**的相位上

v0.1.4 那份日志（第五轮反馈时玩家给的那段）里 `follow=0`，而 `snapHits`、`traceHit` 一直在动 ⇒ 模组活着、
投影链路活着，只有跟随那一条链路一次都没跑。查下来不是"判据太严"，而是**相位本身**：

- `ToolOutputSystem.OnUpdate` 只在 `applyMode` 是 Clear/Apply 的那一帧才建立 `ApplyTool` 这一拍
  （FACT：`Game/ToolOutputSystem.cs:20-30`）。也就是说 `SystemUpdatePhase.ApplyTool`
  **只在玩家按下左键提交的那一帧存在**，其余帧里挂在这个相位上的系统一次都不会被调度。
- 而跟随要盯的是"路网变了"——`Game.Net.UpdateCollectSystem.netsUpdated` 是在 **Modification5** 置位的
  （FACT：`UpdateCollectSystem.cs:389-413`，注册在 `SystemOrder.cs:192`），到 `ApplyTool` 那一拍时窗口已经过去；
  何况大多数帧根本没有 Apply。两件事叠起来 ⇒ 跟随系统每帧都被调度器跳过，`follow=0` 是**必然**而不是概率。
- `ToolSystem.OnUpdate` 每帧驱动 PreTool/ToolUpdate/PostTool（FACT：`ToolSystem.cs:251-261, 308-327`）⇒
  跟随必须搬到 **ToolUpdate**。搬过去之后它与主投影系统同相位，所以注册成
  `UpdateAfter<ZoneSnapperFollowSystem, ZoneSnapperSystem>(ToolUpdate)`，让"这一帧的跟随看到的是最终形状"。

同一条事实还解释了另一件旧事：`ZoneSnapperApplySystem`（闭合边补写）**必须**留在 `ApplyTool` ——
区域的 `Created` 标记在帧末 Cleanup 相位就被回收（FACT：`Game.Common/CleanUpSystem.cs:24-53`），
只有提交那一帧才既有 `Created` 又有 `ApplyTool`。所以"跟随挪走、补写留下"不是不一致，是两条相反的约束。

**这条约束目前只有反编译证据、没有实机证据**：v0.1.4 日志里 `闭合边已补写` 那一行五轮从未出现过
（只出现过 `闭合预览`）。已列进本轮实机清单第一优先项。

### ⑧ 的第二半：拖拽触发源不能问"`Updated` 标记"，要问工具的控制点表

跟随的第二个入口是"玩家拖了一个已存在区域的角点"。原来靠查 `Game.Areas.AreaUpdatedTag`/`Updated` 组件——
那是游戏在**提交之后**才打的标记，拖拽过程中根本没有。现在改成从 `AreaToolSystem` 的公开面取：
`GetControlPoints(out moveStartPositions, out dependencies)`（FACT：ATS:2973-2977）在 `State.Modify` 下
第 0 格就是被拖那一格，`moveStartPositions[0]` 给起点；进 Modify 的门槛是
`HasComponent<Area>(cp.m_OriginalEntity) && math.any(cp.m_ElementIndex >= 0)`（FACT：ATS:3440-3445）
⇒ `m_ElementIndex.x` 就是格号，`m_OriginalEntity` 就是那块区域。跟随拿这两样定位目标，
再等 `DRAG_SETTLE_FRAMES=2` 帧让游标稳定，才重描。

⚠ 游标那一格的 `m_ElementIndex` 恒为 `(-1,-1)`（FACT：ATS:3661-3671：游戏每帧用新的射线命中替换它），
所以"哪一格是被拖的"只能取**带 Area 的那一格**，不能按"表里第一格"猜。这条踩过一次，写进了 SnapperState 的注释。

### ① 的修法：生产端与消费端各一道，不是一句"记得 Clear"

`Idle(why)`（Systems/ZoneSnapperSystem.cs）——所有"这一帧我们什么都不做"的提前返回统一走它，
会话状态、拖拽快照、预览信箱一起撤。原来每条提前返回各写各的，只要有一格没人 Clear，
渲染端就会把**最后一帧**的淡色几何一直画下去（这就是玩家看到的现象）。

只在生产端修不够：日志显示工具可以停在"Create -ish"状态而生产端的 Clear 路径根本不执行
（`pvSkip=…not-a-vertex×2253` 就是那一段的痕迹）。所以渲染端另外加了**两道独立闸**：
`activeTool == AreaToolSystem && state ∈ {Create, Modify}` 才画，以及
`PreviewFeed.PublishedAtFrame`（`UnityEngine.Time.frameCount`）新鲜度闸 `STALE_FRAMES=4`。
两道闸都是"宁可少画一帧"的方向，不会留下残影。

### ② "区域类型开关没生效"——开关是好的，玩家看到的是**游戏自己的吸附**

这是本轮最容易改错的一条，先看账：v0.1.4 日志里 `pvSkip=snap-off×558` ⇒ 关掉的那一类确实被模组跳过了。
但玩家视角是"关了还是吸到路上"。原因是游戏自带的节点吸附一直在工作
（FACT：`AreaToolSystem.cs:3235-3275` `GetAvailableSnapMask`：District→`Snap.NetMiddle`，
Lot/Surface→`Snap.NetSide|Snap.ObjectSide`），**那不是你关掉的那个开关管的**。
所以本轮没有"修好一个坏开关"，而是把话说清楚：

- 每一项的 `.desc` 文案里明写"关掉之后游戏的区域工具按原版行为工作（游戏自己的节点吸附仍然会吸到路上）"；
- `LogOnceTierOff(tier)` 在第一帧跳过时打一行 `[ZoneSnapper] 类别 X 的开关是关的 ⇒ 本模组完全不介入该工具`，
  每个类别只说一句（Playbook：日志刷屏会淹掉下一次事故的证据）；
- 这一行进了 verify.mjs 的哨兵清单（`'的开关是关的'`），实机日志能直接判读。

### ③ 的取证：`m_Width` 含人行道，而不对称量在 `m_MiddleOffset` 里

上一版"贴路缘"其实贴的是 `m_Width/2`。本轮读了两处：

```
FACT：Game.Net/GeometrySystem.cs:526-530
      float2 o = m_Width * float2(0.5f, -0.5f) + m_MiddleOffset;
      → OffsetCurveLeftSmooth(centre, o.x) / OffsetCurveRightSmooth(centre, -o.y)
      写进 Game.Net.EdgeGeometry 的 m_Start.m_Left / m_Start.m_Right（同文件 :398/:405 的 Cut 拼法）
FACT：AreaToolSystem.cs:452-493  Snap.NetSide 消费的正是这四条 EdgeGeometry 曲线
旁证：Game.Zones/BlockSystem.cs:195 用 m_Width - m_MiddleOffset*2 当"建筑可占地宽度"的起点
      ⇒ m_Width 含人行道与隔音屏，m_MiddleOffset 才是"中线两侧的不对称量"
FACT：Game.Prefabs.EdgeGeometry 只有 Segment m_Start, m_End; Bounds3 m_Bounds ——**没有 m_StartWidth**
      （上一版任务 #23 记的"用 EdgeGeometry.m_StartWidth"是错的，本轮按第二份深查报告改掉了）
```

于是两侧各自 `|m_Width/2 + m_MiddleOffset|` 与 `|m_Width/2 - m_MiddleOffset|`（新 `GraphEdge.SetWidthsFromPrefab`），
`GraphEdge.LeftHalfWidth/RightHalfWidth` 落地，`WorldSampler.FillEdgeGeometry` 改为按侧取
`EdgeGeometry.m_Start.m_Left` 与 `m_End.m_Left` 拼一条完整左缘、缺数据时按**该侧自己的**半幅平移中心线
（`OffsetCopy`）。回归壳 W 段（14 条）钉的就是这道算术：以前它写在 GameSide 里，只有开游戏才看得见对错。

### ④⑤⑦ 的引擎侧：交点、模式、续接，各自都有一组可断言的数字

- **贴合模式**（反馈 5）：`PolicyKit.ModeKnobs(mode)` 是一表具体数字（绕行上限 / 转弯代价 / 换类代价 /
  许不许为"包住街区外侧"多搜一遍 / 许不许跨网络 / 拐点判定角度）。原来的三个开关之所以说不清谁赢，
  就是因为它们没有共同的代价尺度；换成模式之后"不切换网络"这一档就精确等于原来那个开关关掉（T2 段有对账断言）。
- **交点必放节点**（反馈 4）：`MarkCorners` 登记进 `TraceResult.Pins`，位置在 `Finish` 里、
  **在 `SimplifyKit.FitToBudget` 之前**——顺序反过来就是"简化先把角点抹掉，再按抹完的点说这里没有交点"。
  判据两条：单格转角 ≥ 阈值；以及**连续同向偏转的累计**越过阈值（直线接缓弯时头几米方向差很小，
  但它是"直线与曲线交汇处"）。阈值 = `Knobs.CornerDegrees × (1 − 0.8×曲线精细度)`，所以精细度 0 时 22°、
  100% 时 4.4°。T9 段用一个 30° 缓折（弦高偏差 2.59 m < 默认档容差 5 m）证明这件事：不钉就被 RDP 抹掉、钉住就留下。
- **切换处**（反馈 4 那句"相同或不同高度的道路或轨道"）：`ViaGraph` 原来只在 `KindChanged`（含 `LayerBits`）时钉接头，
  本轮补 `HeightChanged`（地面↔高架、或两条不同 `Lift` 的高架），T9 里有"纯高度切换、平面方向一点不拐"的夹具。
- **节点续接**（反馈 7，取代弧化）：`TraceKit.Continuation`——只有一端可描边时沿网络贪心走到走不动为止，
  每步三道闸（必须朝自由端前进 / 转角 ≤ 75° / 已走 + 剩下那段直线 ≤ 直线距离 × 绕行上限），
  贴出来不足 6 米就整条不续（免得边界上出现无缘无故的小折角）。**中断处那一格必须是真节点并登记进 Pins**：
  第一跳就被闸拦下时循环里那次 `Pins.Add` 根本不会执行（离线壳 T8 抓到，本轮补的），
  而反馈 7 的原话是"续接的节点可以是手动节点，也可以是续接路径上的节点"——说的就是这一格。
- **环岛**（反馈 7）："绕外围"那两遍搜索在 `ea.OnRing || eb.OnRing` 时直接不做 ⇒ 环岛沿它自己的中心线走。
- **节点上限**（反馈 7）：玩家侧那一项删了，代码里只剩 `HARD_NODE_CAP=4000` 这道存档侧硬保险
  （一条边走出几千个点意味着走廊或选路已经不对了，与其把这么大的点表塞进游戏的三角化，不如这条边退回直线）。

### ⑨ 官方用词与 12 语言表：能抄的抄到了，抄不到的这一栏是本轮**自己抓出来的假证**

`LocaleKit.kTable` 全部重写（**61 行 = 41 行标题/分组/下拉选项 + 20 行说明**，每行 13 格：key + en + 12 语言），
并补上 v0.1.0 欠的 **zh-HANT 独立一列**（那时繁体直接复用简体）。

**先说这一栏的教训**：本轮第一次"对账"给出的那张官方词表（三条出处
`Toolbar.SNAPPING_TITLE`、`ToolOptions.TOOLTIP[NetSide]`、`SubServices.NAME[Districts|…]`）**在本机安装里一条都不存在**。
我按它改了 `tab.snap`（简体换成「对齐」）、又在两条说明里引用了"游戏里这一档叫「快速对齐路边」"——
这些都是**没有证据的断言**。发现过程很偶然：本轮要把那份词表存成 `research/` 里的证据文件时，
写了个 `.loc` 解析器去复核，结果三条 key 全 0 命中。**教训：出处要能重跑，不能只写"我在反编译里读到过"。**
⇒ 现在词表由 `scripts/extract-official-terms.py` 现抽现生成（`research/locale-official-terms.md`），
存在性检索由 `scripts/probe-official-locale.py` 负责，L6 里加了两条守卫断言（见下）。

**证据在哪**：这份安装里**没有**明文 `StreamingAssets/Locale/l10n_*.txt`（全盘 `find -iname 'l10n_*'` 零命中 ⇒
v0.1.x 那条"游戏语言目录下有 l10n_zh-HANS.txt"的 FACT 也一并更正；结论没变——语言码确实是 `zh-HANS/zh-HANT`——
但凭据换成了 `Locale.cok` 的成员名与实机日志的 `activeLocaleId`）。官方文案在
`Cities2_Data/Content/Game/Locale.cok`（23.5 MB），而它**就是一个普通 ZIP**：`<lang>.loc` + `<lang>.loc.cid` 共 24 个成员、
全部 `STORED`（未压缩）。`.loc` 布局（实测）：`varint 版本 + varint(0) + 3×(varint 长 + 串)`，再 `varint 记录数`，
之后是 `[varint 长][lockey][varint 长][UTF-8 值]` 重复，基础游戏 **11027 条 / 11026 个非空 key**。
DLC 各有一份同结构的 `Content/<包名>/Locale.cok`（本机 12 份），**每份都是同样那 12 种语言**：
`en-US de-DE fr-FR es-ES it-IT pl-PL pt-BR ru-RU ja-JP ko-KR zh-HANS zh-HANT`（没有 `es-MX`）。

**抄得到的（四个工具名，`Assets.NAME[...]` 去掉尾部"工具"就是名词，12 格全对过）**：

- **市辖区**：ja **特区**（不是"地区"，`特区作成ツール`）、zh-HANT 行政區、it Quartieri、ru Районы、pl Dzielnice、ko 지구。
- **专门产业区**：en `Specialized Industry Area`（是 z）、ja **特化産業エリア**、zh-HANT 特殊工業功能區域、ko 특화 산업 구역。
  ⚠ 本轮先按那份假词表写成「産業特化エリア」——**词序反了**，`特化産業エリアツール` 才是游戏原文（L6 有一条就是钉这个）。
- **表面区域**：ja 地表エリア、zh-HANT 地面區域、ru Поверхности、ko **표면적**（我们原来写的"표면 구역"游戏里没有）、
  it Aree con superficie、es Zonas de superficie、de Oberflächenbereiche。
- **地图区块**：游戏里**没有**"城市边界"这个词（`Assets.NAME[Map Tiles]` = 各语言自己的"地图区块"），
  所以那一档写成 `玩家词（官方名）`，12 种语言每格都带着自己那一层的官方名
  （en Map Tiles / de Kartenfelder / fr Carreaux de carte / es **Casillas de mapa**（上一版多打了个 del）/
  it Quadranti della mappa / pl Pola mapy / pt Quadrados do mapa / ru Клетки карты / ja マップタイル / ko 지도 타일）。

**抄不到的（所以不许再声称"官方"）**：`SNAPPING` 这个 key 在 12 种语言的 key 与值里 **0 命中**，
en 值里 `Snapping` 0 次、zh-HANS 值里 `吸附` 0 次、`对齐` 只有 1 次（`Assets.NAME[UXSnapSFX]` = UX对齐音效，一个音效名）；
`ToolOptions.*` / `Toolbar.ELEVATION_TITLE` / `Toolbar.UNDERGROUND_MODE_TITLE` 也全是 0 命中，
4.28 GB 的 `Blob.cok` 里扫不到「快速对齐 / Snapping」明文 ⇒ 那层工具选项文案在压缩资源里，本机读不到。
⇒ 三处撤回：① `tab.snap` 简体从「对齐」改回**「吸附」**，与全表另外 15 处「吸附」一致，且不再标"官方"；
② 两条 `.desc` 里"游戏里这一档叫「快速对齐…」"删掉（L6 有守卫：整张表里再出现「快速对齐/快速對齊」就红）；
③ 四条滑杆标题的动词标注为**本表自译**。副作用：L3 的繁简同形豁免名单从"空"变回**只有 `tab.snap` 一条**，
理由也换了（不再是"官方繁简相反"，而是"无证可依据 ⇒ 繁简统一用玩家词"）。

**另外两个游戏里确实没有的概念**（这次是实测 0 命中，不是推测）：`curb`/`kerb` 在 12 种语言里 **0 次**
（`sidewalk` 有 31 次，zh-HANS 一律译「人行道」）⇒ "人行道外缘不是车道路缘"只能我们自己说清；
也没有"高架道路/隧道"标签，游戏给的是 `Common.ACTION[Enter|Exit|Leave Underground Mode]`（地下视图 / 地面视图 / 离开地下）。
⚠ 反馈 6 那条的副作用仍然成立：`Game.Areas.AreaType` 的 `Space` 在 UI 上对应**空域**（直升机坪那类），
不是外层空间——别和 `Game.Zones.AreaType`（住宅/商业/工业）混。

**两处故意不跟官方**（玩家点名要的词优先）：**节点吸附距离**（反馈 3 原话）与**城市边界线**（反馈 6 原话）。
L6（**31 条**）钉的就是"能抄的这张表 + 抄不到时我们怎么写"，改任何一个词都会在这里红。
L4 的账跟着重签：上屏设置项名单 28 → **25** 项，`mode.*` 五行按游戏的枚举模板
（FACT：`AutomaticSettings.cs:906-913` `enumType.Name.ToUpperInvariant() + "[" + names[i] + "]"`）只注册标题；
L3 断言行数下限 **61**。

### 台账变化

- T3 断言 **953 → 1113**（新增 O5 模式表、O6 精细度与交点阈值、O7 每类档位、T8 续接八条契约、
  T9 交点与切换处、W 每侧独立半幅宽、**L6 官方用词 31 条**（含两条"没取证的词不许冒充官方"守卫）；
  删掉弧化 P4、交叉口加成开关 N2、路缘开关 N3/N4/N5 那些行）。
- 门禁类型清单 26 → **30**（加 `FollowFlashFeed`、`LocaleKit`、`TraceMode`、`ModeKnobs`）；
  实机哨兵 11 → **18** 条（预览依赖、预览作废、跟随相位、跟随高亮、退回跟随、贴合模式、类别关掉）。
- 选项页：标签页 `贴合 / 描边与节点 / 快捷键` → **`吸附 / 其它设置 / 快捷键`**（第一页这个名字繁简都用玩家词「吸附」——
  我们**取不到**游戏那一层 UI 文案的证，见上面 ⑨ 与 FACT 26，所以不冒充官方）；上屏设置项 28 → 25；
  快捷键动作 3 → **4**（新增 `CycleSnapMode`、`UndoLastFollow`，删掉 `TogglePriority`）。
- 系统相位：`ZoneSnapperFollowSystem` **ApplyTool → ToolUpdate**（`UpdateAfter<…, ZoneSnapperSystem>`）。
- 新文件：`GameSide/FollowFlashFeed.cs`（跟随改动的闪烁信箱，`MAX_FLASHES=8`、`LIFE_SECONDS=4`）；
  `scripts/extract-official-terms.py` + `scripts/probe-official-locale.py`（官方词表现抽现生成 / 存在性检索），
  产物 `research/locale-official-terms.md`（12 语言逐格原文 + 命中次数）与 `research/locale-probe{,2}.txt`、
  `research/locale-blob-grep.txt`（`Blob.cok` 明文扫描，结论：那层 UI 文案读不到）。
- 版本 0.1.4 → **0.2.0**；仍不发布（`PublishConfiguration.xml` 无 `<ModId>`、`AccessLevel=Private`）。

### 这一轮还没解释清楚的（别当成已解决）

1. **`闭合边已补写` 五轮从未出现在实机日志**——只有 `闭合预览`。要么提交帧没跑到那段、要么写了但日志行没打出来。
   这一条不查清，"最后一边是直的"这个症状就没法说修好了。本轮清单第一项就是它。
2. **`desync=15 / pop=76`**（第四轮那份账）：右键撤销与我们手动栈不同步的次数还不为 0。
   Cancel 补丁只有一个打上时才会这样，但 `[Hook] 补丁已安装 2/2` 是有的 ⇒ 说明另有路径（游戏自己清的表）。
3. **`NotOnNetwork×501` 是最大的回退项**（第五轮那份账里 `traceHit=83 / fallback=522`）。
   节点续接就是冲着它去的，但它能把多少 `NotOnNetwork` 转成"部分贴合"，本轮只有离线证据。
4. "同一段路两侧完全吻合"仍是**确定性论证**而不是实机比对（两块区域分别压在公共段两侧时的外围选择）。
5. 曲线精细度、五种贴合模式的具体手感——离线只能钉数字，不能钉"玩家觉得对不对"。

---

## 〇c、上一轮修订（v0.1.4，2026-09-19）—— T4 第四轮实机：「随机不贴」查清了，预览上线，海岸线下线

**玩家反馈三条**：① 「有的路会触发自动贴合，有的又不会，很随机，不知道什么原因你查一下」；
② 「在玩家还在移动下一个准备放置的节点，或拖拽已存在的节点还未放置时，实时预览放置后会贴合的结果，
预览显示的节点用颜色更淡的显示」；③ 「海岸线那个开关我完全没发现效果，而且意义其实并不大，可以去掉」。

照第四轮定下的规矩（Playbook 规则 22：先看账再动手），本轮开局就是读日志，不猜。

### ① 的根因：走廊跟着光标跳，先前那一格贴的路掉出了快照

本机 `Logs\ZoneSnapper.log`（v0.1.3，21:37–21:56，零 `[ERROR]` 零 `[WARNING]`）最后几行的账：

```
tier=District radius=220.0 … traceHit=40 fallback=146 … rebind=52 resnap=45 lost=25 stale=0
fb=NotOnNetwork×121,NoPathWithinBudget×20,WouldSelfIntersect×3 lastMs=0.55
```

- `stale=0` ⇒ 上一轮的锚点重绑**没有失效**：没有一个锚点下标越界。这条先排除掉「重绑没修好」。
- 但 `resnap=45 / lost=25` ⇒ 70 个锚点**签名找不到**，只能按位置重认，其中 25 个连重认都没命中 ⇒ 保持自由点。
- 「签名找不到 + 位置也吸不到」只有一种解释：**那条路这一帧压根不在快照里**。
- 为什么会不在：`WorldSampler.CorridorBox(a, b, …)` 的走廊只由**当前这两点**（上一格 + 光标）算出来，
  光标每动一下走廊就整体平移/缩放一次。玩家沿一条路点三格再往前走，第一格贴的那条路就可能掉出走廊 ⇒
  描边 `NotOnNetwork` ⇒ 那一条退回直线。**同一局里同一条路，什么时候贴、什么时候不贴，取决于光标此刻在哪** ——
  这就是「很随机」。`radius=220` 是玩家把倍率拉到顶（想让它在吸），反而让走廊跳得更凶。

修法：`PolicyKit.PlanStroke`（新）——一次绘制内的走廊**只增不减**，且**必须包住已经放下的每一个角**：

```
raw = 上一帧的 raw ∪ 所有已落档角点 ∪ 本次两端         capture = raw ⊕ max(1.5r, r + 最长边×0.15)
超过 STROKE_LIMIT=2500 m 时按中心收缩，并只警告一次（那确实是大到画不出来的尺寸，不是 bug）
```

`EnsureWorld` 的闸同步改掉：只有「当前快照**不包含**某一端点」才重抓（原来是「框不相交就重抓」，
等于走廊一收缩就换一份快照 ⇒ 亲手制造了下标漂移）。`m_strokeRaw` 在 `ClearSession` 里归零，
所以每一笔新绘制都从干净状态开始。离线钉在 X11e（单调性 + 覆盖所有角 + 超限时 `Clamped` 为真）。

### ② 实时预览：新增一层 `Engine/PreviewKit.cs` + 第四个系统（Rendering 相位）

关键取证（这轮才挖到，两条都是公开的，不需要 Harmony）：

- `AreaToolSystem.GetControlPoints(out NativeList<ControlPoint> moveStartPositions, out JobHandle)`
  （FACT：ATS:2973）——**第二个出参就是拖拽期的起点表**，之前一直被我们当 `out _` 丢掉。
- 进 `State.Modify` 的门槛（FACT：ATS:3440）是 `HasComponent<Area>(cp.m_OriginalEntity) && math.any(cp.m_ElementIndex >= 0)`，
  而游戏自己就按 `m_Nodes[cp.m_OriginalEntity][cp.m_ElementIndex.x]` 取被拖格、按 :845/:846 取左右邻格。
  ⇒ 任务 #16 卡着的「被拖的是第几格没有可靠来源」当场解开：**`m_ElementIndex.x` 就是格号**，
  第三段（松手后只重描左右两条边）现在有据可做，已另立任务。
- Modify 态 `index = math.select(0, Length-1, state==Create)`（FACT：ATS:3678）⇒ 表里第 0 格就是跟着鼠标动的那一格，
  且已经过游戏自己的吸附（:3681）⇒ 我们在它之上再贴一次，不抢游戏的活。

实现口径（三条都是硬约束，写在 `PreviewKit` 类注释里）：

1. 预览**一个字都不写进**控制点表与区域数据。原因不是洁癖：倒数第二格↔游标那一格要过游戏的最小间距校验
   （FACT：ATS:3509-3511），预览点占进表里会把玩家最后一次点击静默吃掉 —— 这正是 v0.1.1 闭合边折返线的形状；
   而且左键会把表里**全部**点收进区域，预览点就会变成真节点。
2. 淡色因此是**技术上必然**而不是审美选择：`ControlPoint` 没有颜色通道，所以走
   `Game.Rendering.OverlayRenderSystem` 的公开 `Buffer`（`GetBuffer(out deps)` :602、`DrawLine` :201、`DrawCircle` :133，
   颜色是 `UnityEngine.Color`、alpha 有效），注册成 `UpdateBefore<预览系统, OverlayRenderSystem>(SystemUpdatePhase.Rendering)`
   （FACT：SystemOrder.cs:688 / UpdateSystem.cs:173）。OverlayRenderSystem 每帧把实例计数归零 ⇒ 停止提交就是消失，
   不需要擦除代码。⚠ `SystemUpdatePhase` 枚举里 `Rendering` 排在 `ToolUpdate` **之前** ⇒ 预览最坏晚一帧上屏（≈16 ms）。
3. 每帧都算，但带预算闸 `BUDGET_MS=4`：上一帧整步超预算就沿用上一帧那套几何（预览晚一帧不伤人，卡手会）。
   实测 `lastMs≤0.55`，离预算很远。

第二个入口（拖拽）直接复用 `FollowKit.Around`：把「拖到哪」当成存档环里那一格的当前值复制一份喂进去 ⇒
预览与松手后的重描是**同一段代码**，不会出现「看着贴上了、松手又弹开」。存档环只读（传进去前复制）。
另外两处口径：预览折线**含两端**（缺一头就是「线头悬在指尖前一米」）；
预览的回退原因与真正落档那次描边**必须同一个**（T3 的 P4 就是把这条钉成断言，而不是钉「结果一定好」）。

选项页新增两行：`ShowLivePreview`（默认开）+ `PreviewFadePercent`（10–90%，默认 35）——
12 语言标题与说明都补齐，L 段名单 27 → 28 项。

### ③ 海岸线整条删除（不是关掉，是删掉）

游戏里没有现成的海岸线折线，我们每帧按 8 米网格采水面/地形再跑等值线（`IsoKit` 的来历）——
这是全模组**单价最高**的一档，而玩家的结论是「完全没发现效果」。所以：
`ModConfig.TraceShorelines`、选项行、`WorldSampler.CollectShorelines`（66 行）、`WorldSnapshot.Shorelines`、
`SnapKind.Shoreline`、`TraceResult.UsedShoreline`、描边里的海岸分支——一起删。
`SnapKind` 的编号 1 **保留不复用**（旧日志里的数字不能被误读成别的档），`AnchorCode.Shore/IsShore/SHORE_MIN`
留着（`SHORE_MIN` 还是 `IsObject` 的区间边界）；等值线那套 `IsoKit` 只有产品侧不用了，
仍由回归壳 C9/Iso 段守着（它同时也是 v0.1.x 弦高/采样口径的回归证据）。
统计行里海岸线相关的账一并消失 ⇒ 少一档少一分噪声。

### 台账变化

- 版本 **0.1.3 → 0.1.4**（横幅与 xml 一致，门禁 [0] 守着）。本轮同样**不发布**，只本地部署。
- T3：**953 条断言全绿**（上一轮 899）。本轮新增两组：**X11e**（`PlanStroke` 的单调性、覆盖所有已放角、
  超限时 `Clamped` 为真）与 **P1–P7**（预览的含两端契约、四道闸门、闭合那一帧与落档口径一致、
  以及「预览没有资格改动手动栈/存档环/快照」）。海岸线那三条老断言换成「删除后不抛、不再吐 -(2e6+s) 编码、
  已删档吃 default 权重 4.0」的守护断言。L 段因为多了两行设置项而变长（名单 27 → 28 项）。
- T2 类型名 22 → 26（`PreviewKit`、`PreviewFeed`、`ZoneSnapperPreviewSystem`、`TraceContextUtil`），
  哨兵字面量 10 → 11（新增 `预览已上屏`）。
- `TraceContextUtil` 从 `Systems/` 搬进 `Engine/TraceKit.cs`：预览要用同一份口径造上下文，
  留在游戏侧就够不着；复制一份必然漂移，症状正是「已落档的边贴合、预览那条边乱贴」。
- 新增第四个系统 `ZoneSnapperPreviewSystem`：它只画不写，注册失败/绘制异常都只关掉预览。

### 这一轮还没解释清楚的（别当成已解决）

- `desync=135 / commit=121 / pop=105 / adopted=9`：这一轮玩家撤销了 105 次，且自愈反复触发
  （「表里比我们多 1 格」一天里出现了 4 次以上）。**这轮没动这条链路**——单调走廊把「换快照」的频率降下来之后
  它有可能自己变好，但也可能是一个独立的失同步（谁在我们的槽位上写点）。第五轮先看这三个数。
- `follow=0` 依旧：跟随系统一整局没改写任何区域，且**那行「拿不到鼠标射线命中点」也没出现** ⇒ 三个可能里
  「拿不到命中点」基本排除，剩「玩家没开那个默认关的开关」与「`state==Modify` 判定没认出来」。
- `闭合边已补写：…` **在四轮日志里一次都没出现过**，而 `闭合预览：环点数 3…` 出现过 ⇒
  「我们到底写没写进 `Game.Areas.Node`」到这一轮仍是未证。第五轮清单里这一步单独做一次完整闭合并盯这一行。
- `NoPathWithinBudget×20`：不是走廊的锅（走廊这轮修了），是搜索预算/绕行上限的账，样本太少先不猜。
- 预览的淡色在**雪地/夜间**够不够看得见、`LIFT=0.35 m` 在高架路上会不会把预览抬到路面之外——只有实机能答。
  玩家已经能自己调 `PreviewFadePercent`，所以这一条的代价是「可能要调一次」。

---

## 〇d、更早一轮修订（v0.1.3，2026-09-19）—— T4 第三轮实机：模组活着，描边从来没跟上过

**玩家反馈一句话**：「这次测试完全没效果，无论怎么设置根本就不会自动贴合！」

这次不用猜——日志就在本机（`AppData\LocalLow\Colossal Order\Cities Skylines II\Logs\ZoneSnapper.log`），
读回来是**决定性**的。逐条对完，结论和玩家的观感并不矛盾：

```
Zone Snapper v0.1.2 loading / [Hook] 补丁已安装 2/2 / 跟随系统就绪 / 语言源已注册 12 份 / 选项页已注册   ← 全都活着
tier=District radius=96.0 prefab=32.0 minNode=16.0 … snap=2907 miss=1243 traceHit=17 fallback=119 …
                                                  desync=30 adopted=4 shrink=15 follow=0 …
（整份日志里没有一条 [ERROR] / [WARNING] ⇒ 不是崩，也不是版本没部署）
```

- **游标贴合在工作**：`snap=2907`（命中率 70%，剩下 30% 是点在了路上没有的东西旁边）。
- **描边基本全灭**：`traceHit=17` 对 `fallback=119`。玩家看到的「贴合」几乎就是「边界沿路走」，
  所以他的判断「完全没效果」是对的 —— 落点会吸，但边界永远是他自己那根直线。
- 日志里那行 `闭合预览：环点数 3（闭合边描出 0 个点，回退原因 NotOnNetwork）` 就是病灶的名字。

### 根因：锚点记的是「下标」，而快照每次重抓都是一份新下标表

`ManualStack` 跨帧持有玩家点过的每一格，每格是一个 `PlacedNode`，里面除了位置还记
`Kind / GraphNode / Edge / Arc / Side` —— 其中 `Edge`、`GraphNode` 是 **`WorldSnapshot.Edges/Nodes` 这两个数组的下标**。
而 `EnsureWorld` 的口径是「走廊不够大就重抓」：玩家每多点一下、走廊就长大一点，`WorldSampler.Capture`
就会重建一份快照（四叉树遍历顺序、走廊边界都变 ⇒ **同一个实体的下标会变**）。之后：

1. `RetraceStaleEdges` 拿着**旧下标**在**新快照**上描边；
2. `TraceKit.Trace` 的越界防御（`a.Edge >= world.Edges.Count`）与 `IsTraceable` 直接判 `NotOnNetwork` ⇒ 退回直线；
   不越界时更坏 —— 那条下标现在是**另一条路**，描出来的是鬼线（然后被自交闸拦下，还是退回直线）。

为什么前两轮没暴露：v0.1.1 的「第一点落不下」把测试挡在了第二步之前；v0.1.2 的靶心是「贴错对象」，
玩家报的是「贴到中线了」——**贴到了**说明游标那一步是对的，我们就顺着去改档位，没去看 `traceHit/fallback` 这一对。
（这一对我自己写的统计行里就有，第三轮清单里我还让玩家对比它 —— 玩家没报，因为要他一边画一边盯日志不现实。）

### 修法：`Engine/AnchorRebind.cs`（新层，X11 段 40 条断言）

每格锚点本来就带 `Signature`（GameSide 用 `Entity.Index + Entity.Version + 该实体的参考坐标` 算的，
FACT：`WorldSampler.SignatureOf`）。同一条路在两次抓取之间签名不变 ⇒ **靠签名把锚点搬回新下标空间**：

- 认出来了 ⇒ 只改 `Edge/GraphNode/Arc`，**位置一格都不许动**（那是玩家点下的几何）。这条每个分支都单独断言。
- 认不出来（那条路真被改/拆了）⇒ 才退回「按位置重新吸附」（`SnapKit.FindSnap`，与玩家新点那一格同一套档位口径），
  仍然把位置按回原值。半径内什么都没有 ⇒ 保持自由点（那一条边走直线）。
- 签名与下标都对得上时**不许重新吸附**：第一版实现把「resolved」和「moved」混成一个条件，
  X11b 当场把它钉住（半径内有两条路时每次重抓都会把锚点换一条 ⇒ 需求 4 的确定性先没了）。
- 调用点：`EnsureWorld` 里在 `MarkAllStale()` **之前**（先算后绑等于白算一遍）。

### 顺带修的第二条：接管/自愈把点记成「自由」= 自己把贴合永久关掉

`ProjectKit.ShouldAdopt`（编辑/重建路径开局）与 `MissingTail`（连续 15 帧被闸挡下的自愈）都是
`m_stack.Push(new PlacedNode(pos, SnapKind.Free, -1, -1, 0, 0))` —— 位置照单收是对的（不能擅自挪玩家存好的几何），
但锚点留成 Free ⇒ `IsTraceable(Free)` 恒假 ⇒ **这一圈每一条边**都 `NotOnNetwork`。
日志对得上：`adopted=4` 之后 `traceHit` 就再也不涨。现在两处都过 `AnchorRebind.Attribute`：位置原样、锚点照认。

### 新增的可判读性（这一轮的教训：不能再靠猜改一整轮）

统计行末尾再加四个字段，一次游玩就够定位：

```
rebind= 搬回下标的次数      resnap= 签名认不出、按位置重认的次数
lost=   重认也没命中的次数   stale= 锚点下标已不属于当前快照的次数（修好了就该恒为 0）
fb=     退回原因分布，如 "NotOnNetwork×112,DetourTooLong×4"（GameSide/FallbackTally.cs，按枚举实际取值算长度）
```

`stale=` 是给这次这个根因装的哨兵：它 >0 就说明还有没被 `AnchorRebind` 覆盖的重抓路径（比如描边前另一处换了快照）。

### 台账变化

- 版本 **0.1.2 → 0.1.3**（两处一致，门禁 [0] 守着）。本轮同样**不发布**，只本地部署。
- T3：**899 条断言全绿**（上一轮 859；本轮 +40：X11 搬下标 13、X11b 认不出/幂等 11、X11c 交叉口与边界 6、X11d 整栈与接管口径 10）。
- T2 类型名 20 → 22（新增 `AnchorRebind`、`FallbackTally`），哨兵字面量 9 → 10（新增 `锚点重绑`）。
- 新代码：`Engine/AnchorRebind.cs`、`GameSide/FallbackTally.cs`、`WorldSnapshot` 的签名索引、
  `ZoneSnapperSystem` 的重绑调用点与接管/自愈两处属性化。`Engine/` 依旧零游戏类型。

### 这一轮还没解释清楚的（别当成已解决）

- `desync=30 / shrink=15`：描边一稳定，投影点数的来回振荡会小很多，**但这条我没有独立证据**。
  下一版看 `desync=` 是否跟着掉下来；不掉就单独查「谁在我们的槽位上写了别的点」。
- `follow=0`：跟随系统一帧都没改写过任何区域。三种可能：玩家没开那个开关（默认关）、`state==Modify` 没认出来、
  或者视野盒拿不到命中点。第四轮清单里有一条专门问这个（那行 `跟随：拿不到鼠标射线命中点…` 有没有出现）。
- 玩家这轮**没提**选项页文案 ⇒ 既不能算通过也不能算没通过，第四轮第一步仍然要看一眼。

---

## 〇e、更早一轮修订（v0.1.2，2026-09-19）—— T4 第二轮反馈：贴合目标搞错了对象

**用户提的六条**：① 选项页满屏是本地化 key（前两个模组踩过同一坑）；② 市辖区的贴合在按**最短**连线走，
要的是**最外围路段**（不能穿中间小路，也不能绕大圈），且类型变化处必须有节点、直路两端就够、
弯道相邻两点的连线不许越出道路边缘（拿不到路宽按最小路宽 3 米）；③ 市辖区贴**中心线**，
专门产业区/表面规划/填埋/地皮表面贴**道路边缘或同一建筑边缘**，前两者不许跨越道路或建筑、
后两者跨越段保持直线；有两种模式（依次点节点 / 拖拽已存在的节点，后者看**左右邻居**）；
这四种要严丝合缝；④ 一模一样的路段，两边区域的自动节点要完全吻合，不因重叠物换路径；
⑤ 高架也要适用；⑥ 自动跟随没效果。

### 一句话根因：**「贴路缘」从来没真的贴过路缘**

`WorldSampler.SidePolyline` 用 `(m_Left.a + m_Right.a) / 2 − 中心线起点` 当偏移量。那个中点**就是中心线本身**，
差值恒接近 0 ⇒ `SideLine ≈ Line`。于是第 ② ③ 条的所有表现都对得上：产业区看着贴上去了，
但贴的是马路中线，玩家看到的是「一半路面被划进地块」；而离线壳里**没有任何一条断言**钉住
「产业区贴的点必须不在中心线上」——两轮全绿、实机两轮都不对。修完之后第 ③ 条才有意义可谈。

第 ⑥ 条同样是「代码不存在」而不是「代码坏了」：`cfg.FollowCommittedAreas` 这个开关**从没被任何系统读过**，
选项页上有它、说明里写着「会改写已保存区域」，背后零实现。台账里原本写着「已提交区域跟随未接线」，
所以这不是意外 —— 教训是**选项页上每出现一个开关，就要有一条断言钉住「有系统读它」**（已补，见 T3 的 O 段）。

### 本轮改出的七个真缺陷（每个都留了断言位）

1. **`SnapKit.OfferSide(Best best, …)` 少了 `ref`**：`Best` 是 struct ⇒ 每一个路缘候选都改在副本上被丢掉，
   编译通过、跑起来不报错、只是「贴不上」。N3 第一次就是这么抓出来的（期望 NetSide，实际贴到 30 米外的区域边界）。
   **这一类「静默丢候选」的失败模式**已写进注释：热路径上任何 struct 参数只要要写，就必须 `ref`。
2. **贴合目标按类别分档**（第 ③ 条）：照抄游戏自己的掩码表（FACT：`AreaToolSystem.cs:3235-3275 GetAvailableSnapMask`
   —— District 只给 `Snap.NetMiddle`，Lot/Surface/Space 给 `NetSide|ObjectSide`，MapTile 什么都不给）：
   市辖区**只**贴中心线，外沿档**只**贴左右缘线/建筑轮廓，且**缺数据时不许退回中心线**
   （把地块角点吸到马路中心 = 半条马路划进地块，比不吸更错）。
3. **绕行的「最外围」不能用惩罚项做**（第 ② 条）。试过 `OUTER_PENALTY` 0.6 与 6.0 两版都不成立：
   中间那条小巷本来就几乎与两端连线重合，罚轻了换不掉、罚重了整个街区都绕。
   现在的做法是**朝弦的两侧各搜一遍**（`sideFilter ±1`，落在弦 ±2 米内的边两侧都放行），
   再用「走线中间点到质心的平均距离」定胜负，绕行比那道闸（用纯几何长度）仍然封顶 ⇒ 不会绕大圈。
4. **路口转角把整条边判死**。游戏的左右缘线一路采到**路口中心**，十字路口的两条内侧缘线在路口里十字相交 ⇒
   拼出来的走线在每个角上自交 ⇒ `Rejects` 判 `WouldSelfIntersect` ⇒ **一到路口就退回直线**。
   现在按路宽在转角处两侧各剪 halfWidth（`JunctionTrim`），X 变成贴着路口的斜切；
   中心线档不剪（相邻两条边的中心线本来就在节点上精确相接，剪了反而啃角）。
5. **「左/右」是每条边自己的属性，整条走线不能共用一个 Side**（`FollowKit` 第二次重描抓到）：
   绕街区一圈时同一条内侧缘在北边那条路上叫 LEFT、到西边那条路上变成 RIGHT ⇒ 共用一个就把边界整体
   甩到马路对面，表现是「跟随越跑越坏」。现在逐边定侧，判据与贴合**同一条**（离质心近的那条），
   这也是第 ④ 条「两边区域的节点完全吻合」能成立的前提。
6. **共线时把类型变化那个角删掉了**（第 ② 条）。`TraceKit` 的钉节点登记只看回溯出来的 `edges` 序列，
   而锚点挂在边中间时该边的**两个**接头都会被播种、搜索常从对侧接头出图 ⇒ `edges` 是空的
   （整条走线 = 两头各沿所属边走一段），换类点恰好落在没被比较的那一环 ⇒ 钉数 0 ⇒ RDP 把角抹平。
   现在按「实际走过的边序列」（含两头的接入段）逐对比较。`KindChanged` 比两层：`Kind` + 游戏的 `LayerBits`
   （普通道路 ↔ 公共交通道路在 Kind 里同为 Road，只有 Layer 位分得开）。
7. **跨越判据漏掉「正好从格点/路口正中穿过」**：逐段严格相交会看不见「折线顶点正好落在线段上、
   折线从它两侧穿过」这一种（相邻两段的叉积必有一个是 0）⇒ 同样这条线挪开 1 厘米判跨越、正对着就不判。
   `GeoKit` 补了顶点闸（垂距归一到 EPS + 开区间 + 前后顶点异侧三个条件）。

### 新增的一层：`Engine/FollowKit.cs`（第 ③ 条的第二种模式 + 第 ⑥ 条）

落档之后的区域只剩一圈坐标 —— 「这一格吸在哪条边的哪一侧、弧长多少」游戏不存，所以跟随与拖拽改形
都必须**从位置反推锚点**再重描。绘制中、拖拽后、提交后是同一段逻辑，只写一遍放这里；两套必然漂移，
而漂移的表现是「画的时候好好的，改一下就全乱了」。

两条硬规矩（都是「半死不活比完全不工作有害得多」的具体形式）：
- **不许拖走玩家的几何**：反推的锚点离原位置超过贴合半径 ⇒ 这一格按自由点处理，一个毫米都不动；
- **自检不过整圈不写**：重描出的环自交或退化 ⇒ `Rejected`，调用方保持原样（三角化失败会让区域隐形、
  连带建筑不归属，见 `ZoneSnapperApplySystem` 的同类护栏）。

`Rebuild` 的幂等性是这一层的验收核心，断言口径是**形状**而不是点数（X9/X10）：把结果喂回去重描三遍，
每一格必须原地不动（`Moved == 0`）、两遍的曲线互为最近点距离 0；同时 `GeoKit.CurvesAgree` 按形状判
「这块区域其实没变」⇒ 不重写存档、点数也不会一轮轮往上涨。**逐点比序号是错的**（重描会把长边切得更密）。

顺带一个真性能雷：`AreaGather.Intersect` 原来 `return true` ⇒ 画一块市辖区要把全城几万块区域走一遍。
区域四叉树和网络四叉树一样**没有按包围盒查询的重载**，裁与不裁完全由这个回调决定（上一轮已在 `NetGather`
上犯过同一错，这轮在第二棵树上复现 —— 已抽成 `WorldSampler.AreaEntities`，两处同一口径）。

⚠ 口径修正（写完代码回头核对文档时发现的一处「写了但没接」，别当已完成）：
- **游戏侧只用到了 `Rebuild`**：拖拽落档与改路跟随都走整圈重描。`Around`（只重描左右两条边）有断言、没接线，
  卡在「被拖的是第几格」这个下标在 ApplyTool 相位没有可靠来源 ⇒ 详见下面 §一 已知限制 1。

本地化这一条**复核过，没有类似的洞**（因为它是本轮第一优先，值得逐条对上）：
注册循环用的就是游戏自己那一份 `GetSupportedLocales()`，而游戏的语言下拉同源（FACT：`Game.Settings/InterfaceSettings.cs:266`）
⇒ 「玩家能选的语言」必然在我们要注册的语言里；`LocaleKit.Text` 任一格空都退回英文列（`Engine/LocaleKit.cs:93-101`），
而 L 段断言钉住「12 列每格非空」⇒ 不会退回成 key。可用于对账的两行日志：
`语言源已注册 N 份，当前语言=…`、`词条 key 前缀 id=… name=…`（面板上若还看到 `Options.OPTION[...]`，
拿这个 id 前缀一比就知道是「id 拼错」还是「词条缺」）。

### 高架、隧道、宽度来源（第 ② ⑤ 条的数据侧）

- 宽度/类别/高架/隧道全在 prefab 上：边 → `Game.Net.Composition` → prefab 的 `NetCompositionData.m_Width`，
  `NetGeometryData.m_MergeLayers` 给 Layer 位，`CompositionFlags.General` 的 `Elevated=0x10000` / `Tunnel=0x20000`，
  另有 `Game.Net.Elevation.m_Elevation`。**高架不参与任何闸门**（第 ⑤ 条要它照常贴），**隧道排除在外沿档**
  （游戏自己贴路缘时就把 Tunnel 组合排除，FACT：`AreaToolSystem.cs:501-508`）。
- 道路两侧缘线是真从 `EdgeGeometry` 的贝塞尔采的（`m_Start`/`m_End` 各存一半，且 `m_End` 被 Invert 过两次 ⇒
  朝向必须**判**出来，不能假设）。
- 建筑轮廓：`ObjectUtils.CalculateBaseCorners(transform位置, 旋转, ObjectGeometryData.m_Bounds)`，
  有 `BuildingData.m_LotSize` 时游戏自己把 bounds 换成 `lotSize * ±4`（FACT：`AreaToolSystem.cs:651-663`），
  `GeometryFlags.Circular` 排除。静态物体走公开的 `SearchSystem.GetStaticSearchTree` + `AddStaticSearchTreeReader`，
  根建筑沿 `Game.Common.Owner` 上溯到带 `Game.Buildings.Building` 的那一个。

### 本地化（第 ① 条）

代码与断言在上一轮末就齐了（`Engine/LocaleKit.cs` 是 12 语言文案的唯一来源，27 条设置项 + 3 个快捷键动作
各自有标题与说明；L1–L5 五段断言钉住 key 模板、行数、以及「取词绝不返回 key 本身」）。
本轮**刻意没有新增任何设置项**，所以商店侧不需要新词条 —— 键模板与根因（`ModSetting.cs:303-371` 的
`Options.SECTION[id]` / `Options.OPTION[id.name.Prop]`，`id = assemblyName.modNamespace.modClassName`，
miss 时 `UILocalizationManager.cs:19-29` 直接 `data.Set(key)` 把 key 当文案显示）写在 §〇d 那节里。
**这一条只有实机能定案**：清单里它是第一项。

### 台账变化

- 版本 **0.1.1 → 0.1.2**（两处一致，门禁 [0] 守着）。本轮同样**不发布**，只本地部署。
- T3：**859 条断言全绿**（上一轮 742；本轮 +117：X1 定侧 10、X2 跨越 12、X3 描边沿线 8、X4 外围选路 8、
  X5 换类钉节点 6、X6 建筑档 5、X7 弦高与曲率 12、X8 编码 6、X9/X9b/X9c/X9d 跟随 45、X10 形状判定 9，
  其余为 N3/N5 的改档与夹具修正）。
- T2 的类型名清单 17 → 20（新增 `FollowKit`、`ZoneSnapperFollowSystem`、`AnchorCode`），
  实机哨兵字面量 7 → 9（新增 `跟随系统就绪`、`跟随重描`）。
- 新增第三个运行期系统 `ZoneSnapperFollowSystem`（ApplyTool 相位、排在闭合边补写之后）：
  拖拽落点那一路只要模组开着就走；路网变更那一路要 `FollowCommittedAreas` 打开或按快捷键，
  且范围限制在**视野盒**内（拿不到鼠标射线命中点就这一轮不做，不猜中心）。
- 统计行末尾多了三个字段：`follow= / followSkip= / followReject=`（第 ⑥ 条的账，原来只有每块一行的
  `跟随重描…`，没有总量 ⇒ 实机没法判「三次 F6 有没有在爬」）。字段是现成的 `SnapperState` 计数器，
  加进去不引入新逻辑；`fallback=` 仍是总数，退回原因只在代码里分开（`TraceFallback` 八个值）。

---

## 〇f、更早一轮修订（v0.1.1，2026-09-18）—— T4 第一轮实机反馈：模组把玩家的区域工具搞坏了

**用户报的三件事**：① 点行政区/产业区域/地皮表面区之后**放不下第一个节点**（但移动游标时看得出有吸附迹象）；
② 垃圾场填埋区能放节点，但**完全没有自动贴合**；③ 选项页里**看不到**本模组，却弹出**快捷键冲突**警告。
外加一条口径要求：**快捷键默认全部留空**，由玩家自己设。

三条症状是**同一个根因的三个面**，不是三个 bug。日志把链条钉死了（不是猜测）：
`ZoneSnapperApplySystem.OnCreate` 里写 `ComponentType.ReadWrite<DynamicBuffer<Game.Areas.Node>>()`
⇒ 当场 `ArgumentException: Unknown Type` ⇒ **OnLoad 从第 6 步断掉** ⇒

- 第 5 步的 Harmony **一步都没走**（日志里没有 `[Hook]` 那行）⇒ 手动栈永远是空；
- 第 6 步的前半句已经把 `ZoneSnapperSystem` 注册进去了 ⇒ 它带着空栈每帧跑，
  把「0 槽投影」写进 `m_ControlPoints` ⇒ **玩家刚点下的那一格被抹掉**（症状 ①）；
  游标贴合是它自己每帧算的、不依赖栈，所以「移动时有吸附迹象」——这条观察正是最有力的证据；
- 第 3、4 步（选项页注册、快捷键绑定）在断点**之前**已经跑完 ⇒ 绑定进了全局按键表（撞别的模组的键 ⇒ 症状 ③ 的警告），
  但模组整体被游戏判为初始化失败 ⇒ 选项页不上屏（症状 ③ 的另一半）。

**「半死不活比完全不工作有害得多」**——这句从本轮起是本项目的硬口径：一个装坏的模组必须退化成
「什么都不碰的原版手感」，而不是「每帧改坏游戏状态」。落到代码上是四道独立的闸。

### 本轮改的五处（每处都有断言位，都在 T3 里）

1. **类型写法**：查询里的缓冲区一律用**元素类型** `ComponentType.ReadWrite<Game.Areas.Node>()`。
   根因（这轮把 Unity.Entities 反编译看完才闭环）：TypeManager 的哈希表在启动时一次性建好，
   **构造出来的泛型 `DynamicBuffer<T>` 没有 StableTypeHash**，只有元素类型有（从游戏自己那句
   `AddBuffer<Game.Areas.Node>` 扫出来的）；拿不到 hash 的类型 `TypeManagerInternal.cs:186-198` 直接 return 0
   ⇒ `GetTypeIndex` 抛 Unknown Type。游戏自己全程用元素形式（`GuideLinesSystem.cs:2651` 查询、
   `RequiredComponentSystem.cs:1341` 取用、`AreaToolSystem.cs:3433` 建表），已上线的 AccessAnarchy 也是。
   **官方兜底口**（万一将来非要构造泛型）：`TypeManager.AdditionalComponentsRegistry.AddType(typeof(DynamicBuffer<Game.Areas.Node>), 2882194395u)`。
2. **顺序 + 兜底**：Harmony 安装**挪到注册系统之前**；两个系统各自 try/catch，副系统起不来只降级闭合边；
   任一失败面 ⇒ `SnapperState.Broken = true`，两个系统第一行就返回（`ZoneSnapperMod.cs` 第 5、6 步）。
3. **总闸 `ProjectKit.MayWrite`**：要减点必须先证明「表里这些格全是我们上一帧自己写的」。
   这一条单独就能挡住症状 ①，与 ② 的顺序修复互为冗余。断言：P2b。
   ⚠ **这条闸第一版是「投影只加不减」，那是错的，而且是另一种把工具搞坏的方式**：右键撤销的净效果
   = 弹掉一个手动节点 **连同那条边的全部描边点**（需求 3），玩家把「简化程度」往粗调同理 ——
   都**必然要少写几格**。一刀切禁减的连锁后果：删掉的角还留在表里 ⇒ 每帧撞闸 ⇒ `desync` 疯涨 ⇒
   自愈把游戏剩下的描边点当「玩家新点的节点」收进栈 ⇒ 环彻底乱掉。
   现在的判据是「减点要出示证据」而不是「禁止减点」；P2b 里专门加了一组**正当减点**的正例，
   把「修闸修过头」这一类也钉住（这类「安全机制反过来咬掉正当功能」的坑，比不设闸更难在实机上看出来）。
   代价是 `ToolUpdate` 里只在真的要减点时才 `Positions()` 分配一次（稳态零分配）。
   新增观测位 `shrink=`：只应在右键或调滑杆时增长，且与 `pop=` 同量级。
4. **开局接管 `ProjectKit.ShouldAdopt` + 自愈尾巴 `MissingTail`**：我们一个手动节点都没有而表里已有点 ⇒
   照原样收进栈（**不重新吸附**，那些点是玩家已经存好的几何）。这既接上了 ② 的填埋区/编辑重建路径
   （v0.1.0 那道 `recreate != Entity.Null ⇒ 返回` 的闸把整类绘制排除在外了，已删），
   也让「漏收一次事件」从「这一局永久不再写」变成「半秒后接着贴合」（`blocked 15 帧 → 收尾巴`）。
   断言：P2b/P2c。
5. **落点证据 `ProjectKit.GameAcceptedCommit`**：收到 `Apply` 事件 ≠ 游戏真的落了点 ——
   `State.Create` 那一支的最小间距 `if` **没有 else**（FACT：ATS:3509-3511 → 3567），距离不够时一个节点都不加，
   而 Harmony 后缀是无条件跑的。不拦的后果：District 的门槛是 32 米，玩家挨着点两下就把上一个手动节点
   **重复收进栈** ⇒ 环里自重叠、那条边退化、右键要按两下才掉一个看得见的节点（直接违背需求 3）。
   判据回到证据：`表的格数 > 我们上一帧写进去的槽位数` 才算落点。断言：P2d。

### 本轮读到的新机制（都带 file:line，都是上一版理解错的）

- **第一击其实发生在 `State.Default`**：选中工具后表里只有游标；点下去走 ATS:3476-3494 ⇒ `m_State = State.Create`、
  `Clear()`、`Add(value)`（= 玩家那一格）、再 `Add(raycast)`（= 新游标）。所以我们的入口闸读到的 state
  已经是 Create、`len==2`、`cps[len-2]` 就是第一个手动节点 —— 与投影口径自洽，但**理解要按这条走**，
  否则容易误以为「Create 从一开始就在」。
- **`State.Modify` 的 Apply 也会跳回 Create**（ATS:3568-3592）：拖角点没拖动就 `m_State = State.Create` 并重新灌两点。
  这是 `ShouldAdopt` 的第二个正当来源，不是 bug。
- **闭合那一击之后 `m_State = State.Default`**（ATS:3525）+ `recreate` 分支会直接把 activeTool 交回 ObjectToolSystem
  （ATS:3533-3543）⇒ 我们的入口闸会在同一帧清会话，不需要自己判「区域建完了」。
- **最小节点间距**：`AreaUtils.GetMinNodeDistance(areaData)` = `m_SnapDistance*0.5`，没有 prefab 时退回类别表
  Lot 8 / District 32 / MapTile 64 / Space 1 / Surface 0.75（FACT：ATS:3458 与 Game.Areas 同类方法）。
  需求 7 的档位基准现在**全部取自游戏自己的量**（prefab 的 `m_SnapDistance` + 这张表），玩家滑杆只是乘数；
  `PolicyKit.Resolve(..., gameMinNodeDistance)` 会把我们的间距**抬到不低于游戏那道闸**（断言 O4）。
- **模组侧写 `Areas.Node` 到底行不行，仍未证实**：`EntityManager.GetBuffer<Game.Areas.Node>()` 走的是
  `DynamicBuffer<T>` 那条类型索引，而它在我们 OnLoad 的时刻确实还没注册（就是本轮的根因）。
  区域是游戏同一帧先建的 ⇒ 轮到补写时索引多半已经在，但这条只有实机能定性，所以补写系统加了
  「连续 3 次异常 ⇒ 本局停用补写 + 只留一行证据」的闸：宁可闭合边退回直线，也不能让日志被同一条异常刷满。

### 又一次「断言自己错了」（本轮唯一一条 T3 失败）

P2b 我写「表里点数比我们多 ⇒ 不许写」时给了 6 个点，而被比的投影是 13 槽（3 手动 + 10 描边）——
`6 < 13` 其实是「我们要减点」的合法场景，期望值写反了。**口径**：凡「多于/少于」类断言，
长度一律从被比较对象算出来（`p.Slots.Count + 1`），不写死常量。上一轮同类错误已经犯过三次
（`20+100+20` 写成 120、直线走廊当绕行比样本、闭环用首尾差判零长），这是**第 4 次**，规则升级成硬口径。

### 台账变化

- 版本 **0.1.0 → 0.1.1**（`kVersion` 与 xml 两处一致，门禁 [0] 守着）。本轮不发布，只本地部署。
- T3：**643 条断言全绿**（上一轮 601；本轮 +42：P2b 正当/事故两侧 20、P2c 自愈尾巴 9、P2d 落点证据 7、O4 间距钳制 6，
  其余为长度与形状辅助断言）。
- T2 的实机哨兵字面量从 2 条扩到 7 条：`补丁已安装`、`选项页已注册`、`系统就绪`、`不介入`、`接管游戏已有的`、`自愈`、`闭合边已补写`。
  统计行新增 `slots=` / `list=` / `rejected=` / `shrink=` 四个数：`desync>0` 时光看 `manual=` 猜不出谁和谁脱节。
- 快捷键默认 `BindingKeyboard.None` ×3（玩家的明确要求）。注释里记了为什么危险：绑定了但模组初始化失败
  ⇒ 键还在、动作没了，卸载都找不回原绑定。
- 一次性探针工程 `tools/isoprobe/` 已删（结论全部转成 T3 的 I 段与 C9 段断言）。

---

## 〇g、更早一轮（v0.1.0，2026-09-18，全离线）

> 本节全部保留：里面那四条引擎缺陷与需求 9 的实测账**仍然有效**（修法没被回退，断言还在 T3 里跑）。
> 唯一被推翻的是最后那句「T4 一次都没跑过」——实机第一轮的结果见上面 §〇d。

**结论先说**：贴合 + 描边 + 手动栈投影 + 闭合边补写 + 选项页 + 快捷键全部实现；
离线四道门禁 T1/T2/T2b/T3 **全绿**（T3 = 601 条断言，0 失败，跑完 < 4 秒）；
**T4 实机一次都没跑过** —— 所有「游戏里会怎样」的说法都是未验证项，不是事实。

### 本轮 T3 逼出来的四个真缺陷（都已修，且每条都有断言钉住）

按「如果没有离线壳，这条要付几轮实机」排序。四条全是**引擎层**缺陷，游戏 DLL 一行都没参与。

1. **`TraceKit.ViaGraph` 只能从锚点边的「较近接头」进出图** ⇒ 结构上找不到全网最短路。
   方形环实测真值 160 m，老实现绕出 240 m，绕行比虚高 1.5 倍 ⇒ 能描的边被 `MaxDetourFactor` 判成超预算，
   玩家视角就是「明明有路它偏偏画直线」。修法：a 边的**两个**接头都做源、b 边的两个都做汇
   （`AnchorEnds`/`AnchorEndCosts` + 多源播种 + 多汇早停）。断言：T2b 那一段。
2. **`AppendSpan` 把「各小段自己的两端」当成描边端点去做最小间距过滤** ⇒ 路网每个接头处那颗顶点
   被相邻两段各丢一次，**拐角整体消失**。折尺路 (7,0)→(20,0)→(20,20)→(23,20) 交出去的是
   (18,0)(20,2)(20,18) 这种被削掉角的斜线。修法：过滤只针对本条描边的 a/b（`endA`/`endB`）。
   断言：T2/T3 的「拐角保住了」。这条与 `PushMid` 里那句贪心抽稀是同一类错 —— 抽稀是 SimplifyKit 的活，
   在密采样点列上贪心跳点必然连拐角一起跳。
3. **`IsoKit` 的交点从「本单元先列出的那个角点」量起** ⇒ 同一条网格边被相邻两个单元从反方向插值，
   数学同点、位上差 1 个 ULP（60 与 59.999999999999964 并存），缝合哈希分家 ⇒ 一条闭环被打断成 6 条碎线。
   一次性探针数出来：32 个悬挂端点**全部**是 fpMiss、missing=0（拓扑本来是对的，纯粹是浮点没对上）。
   修法两条：交点一律**从湿端量起** + t≈0/1 时直接给格点本身；`Stitch` 的桶键从异或哈希改成
   (ix,iy) 打包 + 扫 3×3 邻域 + 距离不合格就不接（老代码那个三元表达式**没匹配上也照样 Append**）。
   断言：I 段（一座岛 ⇒ 一条闭环、周长 627.9、度≠2 的端点为 0）。
4. **`StoreKit.Load` 进门先 `_values.Clear()`** ⇒ 「读到空 ⇒ 保持原值」这条口径永远不成立
   （内存里已经没值可保持了），而且读盘中途出岔子会把内存清成半套。修法：先解析进临时表再整体替换，
   空值那行走「继承内存旧值」；并补 `Remove(key)` 作为「真的要删」的正当出口。断言：Z4。

另外把两处**判定顺序**改了：`Rejects`（绕行比/自交/退化）现在跑在 `Finish`（简化）**之后**，
判的是真正交给游戏的那条折线。理由一半是诚实（玩家看到的就是简化后的形状），一半是性能
——`SelfIntersects` 是 O(n²)，未简化的长走廊按 2 米采样能上千点；同时加了 `RAW_CAP=512` 的抽稀闸。
断言：C9 的耗时闸（闸门失效时离线就红，不用等实机卡帧）。

### 需求 9 的实测账（R=200 m 四分之一圆弧路，弧长 314 m，游戏侧 2 米采样 = 158 点）

| 类别 | 简化滑杆 | 容差 | 交出去的节点 | 离真圆弧最大偏差 |
|---|---|---|---|---|
| 地块（prefab 12 m） | 0 | 0.4 m | 19 | 0.25 m |
| 地块 | 0.5 | 3.8 m | 10 | 3.60 m |
| 地块 | 1 | 7.2 m | 7 | 3.79 m |
| 行政区（prefab 45 m） | 0 | 2.7 m | 11 | 1.00 m |
| 行政区 | 0.5 | 28.3 m | 5 | 15.03 m |
| 行政区 | 1 | 54.0 m | 5 | 15.03 m |

**读数口径**：滑杆 0 时「19 个点、最大偏差 0.25 米」= 形状上与原模组的密集放点无差别，但点数已是它的 1/8；
行政区一档滑杆 0.5 与 1.0 结果相同（5 点 / 15 m）⇒ **滑杆在 0.5 以上饱和**，这是 RDP 容差与弧的弦弓高
（R=200 的四分之一圈弦弓高 58.6 m）撞在一起的自然结果，不是 bug，但意味着滑杆上半段是浪费的 ——
实机手感调参时如果要保留「再糙一点」的余地，容差上限应当按走廊弧长收一口（`min(tol, arcLen·k)`），
这条等 T4 的手感反馈再决定，不要现在猜。这组数字已经以断言形式钉在 T3 的 C9 段（点数单调、预算守得住、耗时闸）。


### 关键机制（都带证据，证据路径见设计文档 §一）

1. 节点式区域绘制工具在这款游戏里**只有一个**：`Game.Tools.AreaToolSystem`。
   行政区/地块/地表贴图区/太空区/地图瓦片都是它的 `AreaType`。
   用户列的「产业区域、地皮贴图、垃圾场填埋区」= `Lot` / `Surface` / 特殊产业 `Lot`，
   **不是一个接一个的工具**，所以需求 2 的覆盖面是一处接入而不是 N 处。
   反证：`ZoneToolSystem` 是网格刷/框选、`TerrainToolSystem` 是笔刷、`WaterToolSystem` 是拖半径 —— 都没有节点概念。
2. 控制点表 `NativeList<ControlPoint>` 有公开读写口 `GetControlPoints(out moveStart, out deps)`（ATS:2973）
   ⇒ **投影可以纯数据完成**，Harmony 只用来收「左键提交 / 右键撤销」两个事件。
3. 表的不变量：最后一格 = 游标（ATS:3678）；提交时校验倒数第二格到游标的距离 ≥ `m_SnapDistance*0.5`（ATS:3509-3511）。
   ⇒ **描边点必须插在它指向的手动节点之前**，形状 `[m0,T,m1,…,mN,cursor]`。
   ⇒ **闭合边的描边不能进实时投影**（会让最后一击被静默吃掉），只能在区域创建后从数据层补写。
4. 区域边界改写的动作面就是 `DynamicBuffer<Game.Areas.Node>` + `Game.Common.Updated`
   （游戏自己的 ApplyAreasSystem.cs:175/182 就这两下）。`Updated` 是单帧标记 ⇒
   补写系统必须跑在同帧 `ApplyTool` 相位、`UpdateAfter<…, ApplyAreasSystem>`。
5. `AreaFlags.Slave` 区域的 Node 会被游戏从 Owner 覆写 ⇒ 跳过，改了白改。
6. 三角化失败（自交/超节点数）会 `triangles.Clear()` ⇒ 区域隐形 + 四叉树空 + 建筑不会归属。
   所以**自交检查与节点预算不是加分项，是护栏**，改写前必须自检不过就不写。
7. 游戏里**没有**海岸线折线/等高线代码，`NetToolSystem.SnapShoreline`(1811-1866) 是按格遍历湿/干、
   阈值 `y > 0.2f` 现算 ⇒ 本模组的 IsoKit 用同一个 0.2 阈值提等值线，才不会出现「游戏能贴我们贴不上」。
8. 这个 Entities 版本（1.3.10）的 `EntityManager` **没有** `TryGetComponent/TryGetBuffer`
   ⇒ 一律 `ComponentLookup<T>`/`BufferLookup<T>`，且它们只有 `Update(SystemBase)`，
   没有 `UpdateLookup()/Dispose()` ⇒ 句柄由系统 `GetComponentLookup<T>()` 建并每帧刷新，不能自建。
9. `Temp` 组件在 `Game.Tools` 命名空间（不是 Game.Common，反编译里 `__Game_Tools_Temp_RO_ComponentLookup` 可证）。
10. 0Harmony 2.2.2（编译期）的 `Harmony` 类只有 `UnpatchAll(string)`；2.3.3（运行期）上它是 obsolete-as-error
    ⇒ 卸载改成逐个 `Unpatch(original, patch)`，两份 API 都有，也绝不碰别人的补丁。

### 试过但不可用的路（别再试第二遍）

- **参照模组的源码拿不到**：ModId 148238 状态 `removedByUser`，其更新日志里「Initial release to Github」对应的
  `github.com/EjafiSoftware/Subdivisions` 已 404。机制只能靠商店页文本 + 更新日志 + 游戏反编译重建。
- 「把闭合边也插进实时投影」——会被游戏的最小间距校验挡掉最后一击，见上面第 3 条。
- 「用 `SortedSet` 当 Dijkstra 优先队列」——net48 没有 `System.PriorityQueue`，`SortedSet` 同代价会互相顶掉 ⇒ 自带 `MinHeap`。
- 「按文件哈希判断产物是不是本次编的」——MVID 每次构建都变（Playbook 步骤 4 已记，本轮再确认一次）。
- 「断言里的期望值靠注释口算」——本轮 3 条失败是**断言自己错了**：`20+100+20` 写成 120（真值 140）、
  把一条全直线的走廊当「绕行比 &gt; 1」的样本（直线走廊的绕行比恒等于 1，那组断言其实什么都没验）、
  以及「不许有零长段」用首尾点距离判（闭环本来就该首尾同点，两条断言互斥）。
  **口径：Cost/弧长类期望值一律从几何重推一遍再写死；判退化用总长，不用首尾差。**
- 「marching squares 的交点谁算都一样」——差 1 个 ULP。同一个格点的高度**正好等于**阈值时，
  相邻单元从相反方向插值，缝合就断（游戏里水面是一张平面，这种点成片出现，不是理论边界）。

---

## 一、验证状态台账

### 已验证（机器证据）

| 项 | 凭据 |
|---|---|
| T1 构建 0 错误、后处理跑完、三平台 Burst 桩生成 | `dotnet build -c Release` 输出 + `ZoneSnapper.dll processing complete` |
| T3 引擎层无游戏回归壳 **1274** 条断言全绿 | `node scripts/verify.mjs` → `PASS 1274 / FAIL 0`（零游戏 DLL 引用、零联网、零额度，跑完 < 1 秒）。第八轮之后**新增了 J 段（路口归并，11 条组 + `Clamped` 计数两条）与 T12（接入点模型；T10/T11 已被第六轮的邻接取齐与中心线禁令占住）**，并把 T7/T8/X3/X9c/X9e/X11/O8 按新口径改判（见 §☆☆☆ 的账） |
| 四个**工具名**在 12 种语言里逐格对过游戏官方词条 | L6 段 31 条断言（`tests/t3/Tests.Locale.cs`）；期望串取自 `research/locale-official-terms.md`，那份表由 `scripts/extract-official-terms.py` 从 `Cities2_Data/Content/Game/Locale.cok`（普通 ZIP，成员 `<lang>.loc` 未压缩）现抽现生成 ⇒ **出处能重跑**，这是 ⑨ 那一栏踩过坑之后加硬的口径 |
| 「游戏里没有 Snapping / 快速对齐 / curb / kerb 这些词」 | `scripts/probe-official-locale.py` + `research/locale-probe*.txt`：`SNAPPING` 在 12 种语言的 key 与值里 0 命中，`curb`/`kerb` 全语言 0 次，`sidewalk` 31 次；`Blob.cok` 明文扫「快速对齐/Snapping」0 命中（`research/locale-blob-grep.txt`）⇒ 这些只能我们自己说，且**不许**标成官方词 |
| 部署产物是本次的 | `scripts/verify.mjs` T2：**32 个类型名**在 `#Strings`(latin1) + `0.3.0` 在 `#US`(UTF-16LE 双 parity) + mtime + **27 条**哨兵字面量 + **2 条反向哨兵**（prefab 吸附距离的借与还不许回来）；另有 **T2c** 源码级扫按钮形状（带不带 getter 由「有没有确认弹窗」决定，见 §☆ ④） |
| `0Harmony.dll` 真的是 2.3.3 那份 | T2b：sha256 == `~/.nuget/packages/lib.harmony/2.3.3/lib/net48/0Harmony.dll` |
| 上传内容形状 | `Mods\ZoneSnapper\` 7 件（dll/pdb + 三平台桩 + 0Harmony） |
| 版本号两处一致 | verify.mjs [0]：横幅 `kVersion` == xml `<ModVersion>` |
| 「查询里用元素类型」这条硬口径 | 实机反证：v0.1.0 用 `DynamicBuffer<T>` ⇒ Unknown Type 把 OnLoad 打断（日志 + TypeManagerInternal.cs:186-198） |
| 本轮的贴合规则**本身**成立（外沿选路、逐边定侧、类型变化钉点、弦不越路缘、转角剪角、跟随幂等） | T3 新增 X1–X10：`tests/t3/Tests.Targets.cs` + `tests/t3/Tests.Follow.cs`，在离线人造路网上逐条断言（含「同一区域连重描三次形状一模一样」这条硬闸） |
| 相交检测不再漏「折点正好压在弦上」那一类 | T3 X2：`SegmentCrossesPolyline` 补了 vertex-through 分支，这正是上一版 `WouldSelfIntersect` 误报的来源 |
| **第三轮实机「完全不贴合」的根因判定** | 本机 `Logs\ZoneSnapper.log`（2026-09-19 16:05–16:30）：`snap=2907 / traceHit=17 / fallback=119`、零 `[ERROR]`，且有一行明文 `回退原因 NotOnNetwork` ⇒ 「模组没跑起来」被排除，病灶锁定在锚点下标失配 |
| 锚点重绑本身成立（搬下标 / 认不出才重认 / 位置永不动 / 幂等） | T3 X11–X11d 共 40 条（`tests/t3/Tests.Rebind.cs`）：夹具是同一条路在两份快照里下标不同（0↔1），断言「不重绑就会拿错边」+「重绑后描边恢复」+「连做两次账不再涨」 |
| **第四轮「有的贴有的不贴，很随机」的根因判定** | 本机 `Logs\ZoneSnapper.log`（v0.1.3，2026-09-19 21:37–21:56）：`stale=0` 但 `resnap=45 / lost=25`、`fb=NotOnNetwork×121` ⇒ 「签名找不到 **且** 按位置也吸不到」只剩一种解释：那条路不在这一帧的快照里。走廊只由「上一格 + 光标」两点算出 ⇒ 光标一动走廊就平移，早期角点贴的路掉出快照 |
| 单调走廊本身成立（只增不减 / 包住所有已放角 / 超限收缩并置 `Clamped`） | T3 X11e（同文件）：连推三次角点，第二帧的 `Capture` 框必须包含第一帧那一整框，且 `Raw` 覆盖全部角点 |
| 预览的四道闸门与「含两端」契约、且预览改不到任何已落档数据 | T3 P1–P7（`tests/t3/Tests.Preview.cs`，30 条）：包括「预览给出的回退原因 == 落档那次描边的回退原因」这条一致性，和「跑完两次预览后手动栈/存档环/快照逐字段原样」 |
| **人行道外缘那道算术**（`|m_Width/2 ± m_MiddleOffset|`）逐档正确，脏输入被消毒、两侧之和恒等于整幅宽 | T3 W 段（14 条，`Tests.Targets.cs`）：算式本轮从 `GameSide/WorldSampler` 挪进 `Engine/WorldModel.cs` 的 `GraphEdge.SetWidthsFromPrefab`，所以它第一次变得可离线断言。同一夹具还钉了「内外两侧各自那条缘线」（吸与描都按各自的半幅，不再统一 `m_Width/2`） |
| **交点必放节点、没有交点就不放**（反馈 4 的那两条） | T3 T9 段：一条 30° 缓折（整段弦高偏差 2.59 m **小于**默认档容差 5 m）——不钉就被 RDP 抹掉、钉住就留下，两档对比 ⇒ 证明的是交点规则本身，不是采样碰巧；另钉「完全直路的走线零钉」「精细度 100% 时 6° 缓折才算交点（阈值 22°→4.4°）」「道路→铁路与地面→高架两种切换处各只留一格」 |
| **节点续接的六条契约**（反馈 7） | T3 T8 段：①只有一端在路上也能贴出那一段 ②转角 75° 闸（锚点在边中间、路口在它前面时也算，这是本轮补的洞）③两端都自由 ⇒ 手动路径零点 ④贴不出 6 米 ⇒ 整条不续接 ⑤绕行预算把 Cost 连末段直线一起算 ⑥交换两端后走线严格反序 ⑦签名/UsedEdges 齐备且幂等 ⑧正常描边不被标成续接 |
| **五种贴合模式互不串味**（反馈 5） | T3 O5 段：五组数字（绕行上限/转弯代价/换类代价/搜外围/切网络/拐点角）两两不同、各自方向正确、且"换模式不许动吸附距离"；`NoNetworkSwitch` 那一档与旧"跨网络类型开关关掉"行为等价（T2 段有对账断言） |
| **三类各自的节点吸附距离**（反馈 3：不再三层相乘） | T3 O 段：默认 100% ⇒ 半径正好等于 prefab；调一档不牵连别档；50%~300% 内线性单调；上下限 8/260 与 NaN 消毒 |
| **拖拽落档的局部拼接**（第六轮反馈 6：只换左右两条边） | T3 X9e 段（`tests/t3/Tests.Follow.cs`）：新环 = 原环 + 两段重描点、其余每一格坐标逐个全等、左右邻居写回**原来**的坐标、直段拖一格零自动点、认不出被拖格 ⇒ 交回空表绝不猜、null/短环不抛；拼接只有 `FollowKit.LocalChain`/`SpliceLocal` 一处，系统与离线壳共用 |
| **目标白名单**（第六轮反馈 9：步道/水道/埠头/内部路/摆件一律不是目标） | T3 O8 段（`tests/t3/Tests.Policy.cs`）：道路/轨道三档共同目标、高架仍算路、分类不明不是目标、埠头（OnWater）与内部路（Internal）即使挂 Road 标记也排除、隧道只在路缘档被禁、建筑档只认建筑、只有步道经过的端点不是交叉口候选、`CrossesObstacle`（跨越判据）与目标判据**共用同一份名单** |
| **叠置路网平局按最低高度**（第六轮反馈 8） | T3 O9 段：`HeightTieCost` 地面路零代价、10 米高架恰好 0.01（`HEIGHT_TIE_WEIGHT`=1e-3 只打破严格平局）、海平面以下夹到 0、null 不抛；`SnapKit.Best` 分数打平（±0.05）判低者胜，**两种插入顺序都过**（老口径的胜负由四叉树遍历顺序决定） |
| **与邻接区域边界取齐**（第六轮反馈 7） | T3 T10 段（`tests/t3/Tests.Trace.cs`）：重叠 ⇒ 坐标**逐位照抄**（不是「贴得很近」）+ 相邻两点认到同一条边界时补中间顶点（上限 24）+ 节点预算随之放宽 + `Adopted` 计数被报出来（实机靠统计行 `weld=` 判读） |
| **路缘档绝不沿中心线描边**（第六轮反馈 9/10） | T3 T11 段：路缘档 `side==SIDE_CENTRE` 时先按质心定侧（`SideTowardCentroid`），定不出来**不描边**交给续接/直线；`Continuation` 同口径 |
| **FollowCommitted 新口径的文案**（第六轮反馈 5） | T3 L4 新增三条（`tests/t3/Tests.Locale.cs:143-163`）：标题含「重算贴合区域」且不含「自动跟随」、**12 语种**不再挂「实验性」、说明写清「绝不改存档 / 按键才写 / 能退回」三件事 |
| **一个路口只有一个代表点**（第八轮反馈 5，两张截图） | T3 **J1–J11**（`tests/t3/Tests.Junction.cs`）：2~5 个成员节点归并成一个路口、中心点=**主接头**平均（偏北 8 米那格不参与，所以它没把中心拽走）、`NodePoint` 对成员给出中心而非自己、走线两侧连过来都拐在同一格且重描两次逐格全等、20 米外的另一个路口不被并进来、纯步道/直路分段点/两腿在地下的那种"路口"根本不成立、**直穿路口时那个中心点不占一格**（反馈 5 的第二半，靠的是既有"直线不补点"契约）；
  同一格到底"本来就在中心"还是"被挪过来的"由 `TraceResult.Clamped` 分开数（J9 期望 0、J10 期望 1 ⇒ 统计行 `junc=` 的来源） |
| **接入点模型：你那格不动、走线自己接进网络**（第八轮反馈 2/3/6/8/10 的共同修法） | T3 **T12** + T7/T8/X3/X9 改判后的断言：两端都自由 ⇒ 照样贴（`Attached` 计数与"接入点真的在缘线/中心线上"）、**关掉续接后同一条走线逐格全等**（这条就是反馈 2 第一句的反证）、四分之一弯路绕行比恰 1.414 仍放行、跨街区对角线仍沿网络、附近真没路才 `NotOnNetwork` 零点；`StuckOnTarget`（Kind 对但坐标不在那条几何上 ⇒ 视为需要接入点）另有 X3/X11 两处正面钉住 |
| **地下物体一律不是目标 / 跨越只判结果**（第八轮反馈 3 / 8+9） | T3 O8 重写：隧道与下沉段（`LowElevation<-0.5`）三档都既不能吸也不能描、高架照给、地下建筑不给；X2 仍期望 `CrossesObstacle` 但**理由换了** —— 判的是交付折线自己逐段有没有横跨马路（`ResultCrosses`，只对路缘档生效），不再是"两点连线压到了东西" |

### 未验证（下次照此执行，别外推）

> 这份账是逐轮累加的：下面写于 v0.2.x 的那些条目里的「下一轮」现在一律指**第九轮**（清单 §二，v0.3.0）。
> 第八轮把口径反掉的三条（1、1b、13）已就地改写，没改写的仍然成立 —— 但别把「第六轮」当成还没发生的时间，
> 那是过去的编号，未来的那一跑叫第九轮。

1. **T4 全套**：见下面的第九轮清单（§二）。**v0.3.0 一次实机都没跑过**，本轮四处改动（接入点 / 路口唯一中心点 /
   地下排除 / 跨越只判结果）全部只有离线证据。能拿到的最近一份真机账仍是 v0.1.4 那次：
   `traceHit=83 / fallback=522`、`fb=NotOnNetwork×501`、`pvSkip=…not-a-vertex×2253`、`pvSkip=snap-off×558`、`follow=0`。
   凡涉及「某一条边贴没贴」「预览还在不在」「跟随动没动」的观察，都要在 v0.3.0 上重取。
   本轮新加的四间账 `attach=` / `despike=` / `under=` / `junc=` 是**第一次有实机读数**，
   判据分别写在 §二 第一步、第七步、第五步、第三步。
1b. **`NotOnNetwork` 会不会真的降下来**：第八轮之前它是冲着"续接"去的，现在主力换成了**接入点**（`attach=`）——
   续接只剩兜底。判据是 `traceHit` 与 `attach` **一起**涨、`fb=` 里 `NotOnNetwork` 不再占绝大多数、`cont=` 基本趴着不动。
   三种失败形状分别是：`attach=0` 且 `NotOnNetwork` 仍一片 ⇒ 接入点模型在真实数据上没吃到输入（`StuckOnTarget`
   的容差或 `Attach` 的候选集在真路网上不成立，是我的下一步）；`attach` 涨但 `traceHit` 不涨 ⇒ 接进去之后仍然描不通；
   `attach` 涨、`traceHit` 涨、玩家仍说不贴 ⇒ 观感问题在形状不在通路（先要日志与一张截图）。
   旧口径里「那些边的两端本来就都不在路上 ⇒ 该走手动」这句仍然有效，但它现在**不再是**不回退的理由。
1c. **`desync=15 / pop=76`**（第五轮那份账）：右键撤销与我们手动栈不同步。`[Hook] 补丁已安装 2/2` 是有的
   ⇒ 说明Cancel 之外还有谁在改那张表（游戏自己在 Apply/Cancel/模式切换里都会 Clear，FACT：ATS:3067-3075、:3502）。
   本轮没动这条链路，第六轮如果它自己掉下来 ⇒ 是相位改动的下游。
1d. **`follow=0` 的相位判定只是反编译证据**：`ToolOutputSystem` 只在 Clear/Apply 那一帧建立 `ApplyTool`
   （FACT：`ToolOutputSystem.cs:20-30`）+ `netsUpdated` 在 Modification5 置位（FACT：`UpdateCollectSystem.cs:389-413`）
   ⇒ 逻辑上"永远不跑"。**但本轮没有实机复测**：第六轮第一件事就是让玩家开着跟随改一条路，
   看 `跟随重描` 那一行有没有出现（它出现了才算根因确认，否则是另一个原因）。
2. ~~Harmony 能否真的打上~~ —— 已答：**2/2**（v0.1.1 起每轮都是）。清单第一步不再单独问，仍要求「不是 2 就把日志发回来」。
3. **模组侧能不能真的读写 `Game.Areas.Node`：六轮下来仍未定案**。整段日志里既没有 `闭合边已补写：…`
   也没有 `连续 3 次无法读写 Game.Areas.Node`。第六轮修掉了「帧序自杀」那一层（待补写环被同帧 ClearSession 丢掉，
   见 §〇 ①），**第七轮仍把它列成第一优先项**：点一个完整闭合的区域，
   只盯这一行。它不出来 ⇒ 「最后那条边是直的」这个症状就没修，而且跟随/拖拽写盘这条路的地基是空的。
3b. **预览的三道消费侧闸会不会误杀**：本轮把预览从"生产端 Clear"改成"生产端 + 消费端都闸"
   （`activeTool`/`state` + `Time.frameCount` 新鲜度）。判据是正常绘制时 `预览已上屏` 与 `pv=` 一直在动，
   而退出工具/ESC 后**立刻**出现一行 `预览已按「工具不在/几何过期」作废` 且地上不留东西。
   反过来如果绘制中途预览时常闪断 ⇒ `STALE_FRAMES=4` 太紧（预算闸 `BUDGET_MS=4` 时也会晚帧），要放宽。
4. **`ZoneSnapperFollowSystem` 搬到 ToolUpdate 之后与主系统的先后**：注册成 `UpdateAfter<跟随, 主系统>` ⇒
   同一帧里跟随看到的是主系统刚投影出的最终形状。若实机看到"跟随与绘制互相抢着改同一块区域"（边界逐帧抖动），
   就是这条顺序不对，`SnapperState` 里已有 counters 可判。
5. **`state == Modify` 与"被拖格号"的探测在实机对不对**（拖拽预览与拖拽重描的共同入口）：
   本轮改成两条路（`moveStart[0]` 的 `m_ElementIndex` → 任一带 Area 的条目 → **按位置就近认环上格**），
   并且把两个命中率分开计数（`dragIdx=` / `dragPos=`）。第五轮那份账里 `not-a-vertex×2253` 说明
   前两条经常失败——如果第六轮 `dragPos=` 占多数 ⇒ `m_ElementIndex.x` 在实机上并不是区域节点缓冲的格号，
   那 ATS:3440/:842 那条读法要重新审。
6. **重描的幂等性在真路网上是否成立**（离线是人造路网）：判据仍是「同一区域连按三次重算键，边界每次一模一样」。
7. **外沿档与预览的每帧代价**：第五轮 `lastMs≤0.55` 是"没有续接、没有交点钉、每类还要探两侧缘线"之前的账，
   本轮每格多了 `MarkCorners`（O(n) 方向差）与续接的贪心走线 ⇒ 第六轮看 `lastMs=` 有没有进毫秒级。
8. `listDeps.Complete()` / `deps.Complete()` 每帧一次的代价（大城市 + 长走廊）——Playbook 点名过这类卡点。
9. **太空区本轮彻底不管**（反馈 6）：`PolicyKit.TierEnabled(Space)` 恒 false。
   公园、散步区、公交线路那类点链工具仍不做（玩家口径）。
10. **第 ④ 条「公共路段两边节点完全吻合」**：离线能证明「同侧 + 中心线档」必然一致，
    「两块区域各在公共段**两侧**」时最外围是各自相对自己质心算的，理论上会选不同路线 ⇒ 实机要专门看这一种。
11. 10 种非中英语言仍是人工翻译、未经母语者校对；本轮 `zh-HANT` 从"复用简体"改成独立一列（v0.1.0 欠的账），
    L3 段现在钉"繁简同形只允许 `tab.snap`"。
12. **删改设置项的后果**（本轮第一次成规模发生）：`RadiusMultiplier`/`DistrictBonusPercent`/`Simplification`/
    `MaxNodesPerEdge`/弧化三项/`LiveRetrace`/`ShowSnapMarkers`/`SnapSpace`/`TogglePriorityBinding` 全没了，
    新增三条吸附距离与曲线精细度。预期是"旧键被忽略、新项回默认值"，且**选项页必须照常打得开**
    （`StoreKit` 的读盘容错在这条上被压过一轮：解析失败 ⇒ 用默认值，绝不抛）。
    第六轮第一步顺手确认这两点，并把结果写进 README 已知限制 8（已写，待实机确认措辞）。
13. **模组侧不再写游戏的吸附距离**（第八轮反馈 3/10 反掉了第六轮那条）：`吸附距离已写入 prefab` /
    `吸附距离已还原 prefab` 这两行**现在不该再出现**（门禁 T2 把它们钉成反向哨兵：部署 DLL 里搜到就算坏）。
    判据反过来三条：① 日志里这两行一次都没有；② 滑杆仍然管**我们**的吸附半径（`snap` 随 50%/300% 变）；
    ③ 离开区域工具后城市边界的粗虚线**保持游戏自己的粗细**，也不再出现「第二个粗虚线长方形」（§〇 ③ 那个症状的根因）。
    如果玩家仍然报告虚线变粗或多出第二个框 ⇒ 说明借用不是我们引入的（或另有来源），那是重新取证的任务，
    不许把 prefab 那条路再改回去。
14. **白名单是否真的把埠头与厂区内部路排除**（第六轮反馈 9）：看一行
    `建筑轮廓白名单已生效：这一片 N 个静态物体不是建筑…`（N = 被排除的非建筑、M = 拿不到轮廓的建筑）+
    肉眼确认产业区不再吸到步道/埠头/厂区内部路上。**误杀真路**也要报：「内部路」判据是 fail-open 的
    （SubOwner 位或 Owner 链走到建筑，判不出来按「不是内部路」处理）⇒ 漏排是预期内的，误排不是。
15. **`weld=` 是否在挨着已有区域画时上涨**（第六轮反馈 7）：判据是画第二块时统计行 `weld=` 从 0 变正、
    公共段双缝消失。不涨 ⇒ 2 米阈值或 cos30° 平行判据在真实顶点密度下太紧，调阈值别改结构。
16. **拖拽落档重描是否出现**（第六轮反馈 6）：拖一格已闭合区域、松手后日志应有
    `拖拽落档重描：区域实体 #N 第 K 格，左边 X 点 / 右边 Y 点（回退 a/b），环 M → N 个节点`。
    只有 `followReject` 涨而没有这一行 ⇒ 局部链自检之外还有第二道闸在拒，把日志发回来。


### 已知限制（按影响排序）

1. **落档之后的重描是整圈**——第五轮反馈 4 把定义定了以后，这是**正确行为**而不是待修的偏差：
   「区域创建完成后所有自动节点都视为手动节点」⇒ 重描的输入就是那一整圈手动节点，没有「哪几个是我们吸的」需要区分。
   代价仍然是玩家能看到的那种：**拖一个角时，其余落在吸附距离内的角点也会被重新吸附**。
   （v0.1.4 之前这条被记成「预览只做两条边、写盘没做」的任务 #16；本轮该任务作废，理由就是上面那句。）
   预览那一侧仍只画被拖角点的左右两条边 —— 那是诚实的范围，不是没做完。
1b. **预览与游戏自己的橡皮筋并存**：淡色折线画在游戏那条直线**之上**，不是替换它
   （要替换就得改 `CreateDefinitionsJob.Edit()` 写进 Temp 预览实体的节点缓冲 —— 那是把 Harmony 补丁面
   扩到 Burst job 内部，风险高于收益）。观感是「一条直的 + 一条淡的弯的」，而淡的那条才是点下去的结果。
1c. **跟随是实验性功能**（反馈 8 玩家自己给的口径：「如果不稳定可以标注实验性功能」）：
   默认关、选项页标题里写着「实验性」、且**只有单级退回**（`UNDO_LIMIT=24` 批，但界面只承诺"退回上一次"）。
   退回只覆盖本局内存里的批次，跨存档不做 —— 想要"跟随之前先备份"就得写存档组件，那是 P3 那件事。
2. 已提交区域的跟随**不区分「哪些点是人当年手动放的」**：手动节点没有存进存档（`StoreKit` 仍未接线），
   所以跟随按整圈重算。挡坏事的三道闸：默认开关关着 + `CurvesAgree` 形状相同就不写盘 + 单级退回键。
3. 「编辑/重建已有区域」（填埋场、特殊产业用地）走的是**接管**：开局把游戏已有的控制点原样收下、
   **位置不重新吸附**（那些点是玩家存好的几何，我们擅自挪就是改存档）。锚点会认（v0.1.3 起，
   `AnchorRebind.Attribute`：只记「挂在哪条线的哪个弧长」，不动位置），所以接管的这一圈也能沿路描边。
4. 第 ④ 条「同一路段两侧区域节点完全吻合」在**同侧/中心线档**下由确定性保证（T3 X10），
   两块区域分居公共段两侧时「最外围」各自相对自己质心计算，理论上可能选到不同路线 ⇒ 未实机验证。
5. 「两点真曲线」数据结构不支持，用自适应简化的折点逼近（外沿档靠弦不越路缘这条硬界把密度顶上去）。
6. 世界网格快照在 `ApplyTool`/`ToolUpdate` 相位抓，与游戏自身渲染刷新节拍可能差一帧。
7. `OverlayRenderSystem` 现在画两种东西：**预览**（淡青线 + 淡黄点）与**跟随改动的高亮**
   （被改区域的绿色边界环 + 被挪动节点闪烁 4 秒）。「每个节点贴到了什么」的品红/黄/青图例仍没做 ——
   那一档要的是每个已落档节点一个标记，数量能到几百个，得先想清楚上限与淘汰策略（闪烁那档已经用了
   `FollowFlashFeed.MAX_FLASHES=8` 的上限，图例没有）。
8. 快捷键默认空 ⇒ 需求 6 的「快捷键页」有四个动作，但玩家不设就用不了（这是玩家明确要的取舍）。
   本轮新加的两颗（切模式、退回跟随）里，**退回跟随不设等于没有** —— 它是实验性功能的唯一逃生口，
   所以那一项的说明里写了「强烈建议设置」。
9. 只覆盖**三个开关**背后的区域类型：市辖区 / 专门产业区（含垃圾场填埋）/ 表面区域（含地皮表面区），外加可选的城市边界线；
   太空区按反馈 6 删除（`TierEnabled(Space)` 恒 false）；公园、散步区、线路等点链型工具按玩家口径不做。
10. `Engine/CurveKit.cs` 与 `IsoKit`（在 `WorldModel.cs` 里）在产品代码里**已无调用者**，仍被回归壳测着。
    要么下一轮连着 C/C2/I/I2 段与门禁类型清单一起删掉，要么明确保留 —— 现在这种状态会让下一个人以为还有弧线功能。

### 下一步计划（按价值排序）

- **P-实机（本轮唯一的第一优先项）**：跑第七轮 T4 清单（下面 §二）。顺序照清单：
  ①`闭合边已补写` 到底出现没有 → ②跟随在新相位上跑不跑 → ③预览退出工具后是否立刻干净 → ④续接把多少 `NotOnNetwork` 转走（看 `cont=`）。
  这四条都是"日志一行字定生死"的判据，不要靠观感下结论。
- **P1 修**：按 T4 日志/截图修实际手感（三类各自的吸附距离默认、五种贴合模式的旋钮、曲线精细度默认、
  `SAME_EPS`、预览 `LIFT` 与淡出度、续接的 6 米/75° 两个闸），别凭感觉改默认值。
- **P2**：任务 #19 —— 闭合边的「绕外圈」被 `ViaGraph` 的侧向过滤挡在搜索之外，导致这种形状退回斜线。
  第五轮的环岛改动（`ringCase` 不搜外围）可能已经顺带改变了它的形状，先复测再动手。
- **P3**：已提交区域的跨存档手动节点记忆 —— 存档侧用 `ISerializable` 的 mod 组件（证据：`ComponentSerializerLibrary.Initialize`
  遍历 `TypeManager.GetTypeCount()` 且 `ModManager.AfterLoadAssembly` 会 `SerializerSystem.SetDirty()`，
  所以 mod 组件**会**自动进存档；`IDefaultSerializable` 可给系统自己开一段存档区）。
  键的选取：Entity 数值跨会话不稳 ⇒ 用 `NameSystem` 的区域名 + `SaveInfo.sessionGuid`，注意未命名行政区会被
  `RandomLocalizationIndex` 随机本地化名（会重名，不能当唯一键）。
  有了它，「跟随退回」才能跨会话，限制 2 才谈得上「只重描我们吸过的点」。
- **P4**：`OverlayRenderSystem` 的吸附标记图例（品红/黄/青）——通道本轮已打通，剩「几百个标记的预算与淘汰」。
- **P5**：扩展到点链型工具（`NetToolSystem` 修路、`RouteToolSystem` 线路闭环、`ObjectToolSystem` 的 Line/Curve）。

---

## 二、T4 实机验收清单 v0.3.0（给用户执行，第九轮）

**这一轮要验的是「贴合到底活没活」**：第八轮那十条里有一条根因（手动节点不许吸附之后，正常描边这条路被自己关掉了），
它同时解释了「关掉续接就不贴」「开着续接就强行断在路口」「太远就直连」「产业区几乎看不到路径」。
修法是一个模型（**接入点**：你那格不动，走线自己长出吸附节点接进网络），所以每一步都既看地上的形状、也看日志里那一间账。

日志：`%USERPROFILE%\AppData\LocalLow\Colossal Order\Cities Skylines II\Logs\ZoneSnapper.log`
统计行现在多了四间账：**`attach=`**（长出的接入点总数）、**`despike=`**（拆掉的尖刺接缝）、**`under=`**（因为是地下的被排除掉多少）、**`junc=`**（拐在路口的哪一格被挪到了路口中心点）。
**不对劲就把日志整段发回来，不用你解读。**

### 第〇步：装的是新包（30 秒）

1. 选项 → 模组。日志应有 `Zone Snapper v0.3.0 loading`（**必须是 0.3.0**）、`[Hook] 补丁已安装 2/2 个`、
   `选项页已注册`、`ZoneSnapperFollowSystem 已注册：ToolUpdate 相位…`。少任何一行 ⇒ 后面不用测，发日志。
2. 快捷键页四颗键下面：先「把所有设置恢复默认值」，再应当看到新的**「关于」板块**三行
   —— 模组版本 `0.3.0` / 作者 `yuexian` / 三个按钮。按钮点一下应当开浏览器，日志写
   `关于页已打开链接：请我喝杯咖啡`（没弹出但日志有 ⇒ 是系统/浏览器拦的，不是模组没接上）。

### 第一步：关掉「节点续接」以后还得贴（反馈 2 的第一句，本轮最重要的一条）

1. 选项里把**节点续接关掉**。用市辖区工具在一条贯通的路旁点 3~4 格，**每格都故意点歪几米**（不要正压在路中间）。
2. 期望：边线仍然沿这条路走，路口处拐一次，你点的那几格**一个都没被挪走**。
   日志 `attach>0` 且在涨、`cont=0`（能连通时不该再走续接那条支路）。
3. 失败形状：开着/关着都只是一串直线连起来 ⇒ `attach=0`，把统计行发回来（那是接入点根本没长出来，不是判据严）。

### 第二步：远与弯都不该成为门槛（反馈 6）

1. 仍开着默认「智能」模式，沿一条大弯路（或四分之一的 L 形路口）在**首尾各点一格**，两点相距越远越好测。
2. 期望：走线沿两条路贴出来，拐点在路口上；**不是**一条直线跨过街区。
3. 顺带验一条反向的：在**没有路**的空地上点两格（离任何路都远）——这两点之间应当就是直线，
   日志 `fallback`/`fb=NotOnNetwork` 涨而 `attach` 不涨。该直连的时候老实直连。

### 第三步：路口只在一个点上拐弯（反馈 5，看你那两张截图）

1. 找一个**有人行道/辅路的大十字路口**（就是你截图里那种：中心、偏上偏下偏左偏右、人行道处都有一圈点）。
2. 让边界从这个路口拐一次（比如沿北侧那条路画到东侧那条路）。
3. 期望：拐点是**路口的中心那一个点**，不再停在偏心几米的那一格上；同一条边界反复重描（按一次「重算贴合区域」）
   **不抖**——每次都拐在同一个坐标。
4. 反向期望：如果这条路只是被拆成几段的**直路**、边界直穿路口过去，那这个路口**不该**多占一格节点。
5. 日志这一格的凭据是 `junc=`：拐过路口那一次它应当**涨**（数的是"被挪到中心点的格数"）。
   画了一整圈带路口的边界而 `junc=0` ⇒ 归并没有在真实数据上并出路口（那种路口跨度比我们 15 米的口子还宽），
   这是我要回去调常数的信号，不是你操作的问题 —— 顺带把那块的截图一起发回来最好。

### 第四步：产业区 / 表面区域（反馈 8，两条都要）

1. 切「专门产业区」工具，在街区里贴着房子画几格，其中**故意点歪**、也故意点一格到马路对面一侧。
2. 期望两件事同时成立：
   - **能看到贴合路径**（沿人行道外缘或建筑轮廓），不再是"几乎不贴"；
   - **绝不沿马路中间走**：任何一段都不该压在车行道上（半条马路被划进地块那种形状）。
3. 「表面区域」工具重复一遍。它比产业区松（允许跨，跨的那段不贴），但同样不许贴到路中间。

### 第五步：地下物体一律不算（反馈 3）

1. 城市里有隧道/下穿通道的地方，用三类工具各画一次，让鼠标经过隧道口。
2. 期望：隧道与下沉段**既不是吸附目标也不是贴合路径**（以前市辖区会吸到隧道的中心线上去，这一轮撤了）；
   高架路**照旧**能贴（第五轮那条要求不变）。
3. 日志统计行 `under>` 应当是个非零的小数（走廊里有隧道才会有）；旁边那条
   `建筑轮廓白名单已生效：… N 个在地下（第八轮反馈 3）` 是建筑那一侧的凭据。

### 第六步：连线压到别的区域，但贴出来的路径干净 ⇒ 要能给建（反馈 9）

1. 挨着一个已有区域画第二块，让你自己那两个节点之间的**直线**跨过旁边那块区域（或跨过一条街）。
2. 期望：只要自动贴合出来的那条线本身干净（不自我打结、不横跨马路/楼），**就能建**，
   不再是"既不贴合也不给创建"。
3. 失败形状：预览里贴合线明显绕得好好的，但按下去落不回那个形状 ⇒ 发日志（看 `rejected=`/`despike=` 有没有在动）。

### 第七步：拖拽与预览必须一致（反馈 4 + 3/10）

1. 拖一个已闭合区域的角点：**往街区里拖 8 米左右**（正好是本轮会出尖刺的那个位置），松手。
2. 期望：松手后地上的形状 = 松手前那圈淡色预览；**你拖到的那一格留在你松手的地方**（不再被吸回路缘），
   它左右两条边重描，其余每一格坐标一个毫米都不动。
3. 日志 `拖拽落档重描` 那一行要出现；`despike=` 若涨说明左右两边本来会在同一个坐标各接一次（本轮拆掉了）——
   不涨也正常（那种形状没出现）。**如果边界在你没碰的地方自己挪了，立刻发日志。**

### 第八步：稳定性总账（反馈 4 的「非常不稳定」）

连续画 5~6 块不同形状（含一个环岛、一个 L 形路口、一段挨着已有区域的边），然后把统计行发回来。
我们只需要看这几个数的比例：`snap` / `traceHit` / `fallback` / `fb=…`（哪条闸挡的）、`attach` / `cont` / `despike` / `under` / `junc`、
`pv`（预览上过屏几次）与 `lastMs`（一帧耗时）。
`traceHit` 与 `attach` 一起涨、`fb=NotOnNetwork` 不再占绝大多数 ⇒ 根因修上了；
`attach=0` 而 `fb=NotOnNetwork` 仍然一片 ⇒ 这一轮的模型在真实数据上没吃到输入，那是我的下一步，不是你的问题。

### 第九步：顺手确认的两件旧事

1. **模组绝不写游戏的吸附距离了**（第八轮撤掉了第六轮那条）：滑杆仍然管**我们**的吸附半径；
   离开工具后城市边界的虚线**不该**变粗，也不再出现「第二个粗虚线长方形」（第六轮 §〇 ③）。
2. 「重算贴合区域」按键才写盘、改路不自动改存档（第六轮那条没动）；改完之后那颗「退回上一次自动跟随」仍要能退。

---

## 二b、T4 实机验收清单 v0.2.1（第七轮，已被上面那份取代，留档）

**为什么还要你跑**：本轮 16 条改动 + 出机前追加的那颗重置按钮（§☆），证据全部只到「离线断言（1208 条）+ 反编译」这一层。
其中五件事只能实机看：闭合边到底写没写进去、prefab 吸附距离写进去会不会被游戏覆盖、
白名单在真实存档上会不会误伤、`weld=` 涨不涨、拖拽落档那一行出不出来。

日志位置：`%USERPROFILE%\AppData\LocalLow\Colossal Order\Cities Skylines II\Logs\ZoneSnapper.log`
（模组目录那一行之后就是它自己那段。**任何一步不对劲，把日志整段发回来就行，不用你解读。**）

### 第一步：启动与版本行（30 秒）

1. 启动游戏，进主菜单 → 选项 → 模组。日志里应有：
   - `Zone Snapper v0.2.1 loading` ← **版本号必须是 0.2.1**，不是就说明部署的还是旧包
   - `[Hook] 补丁已安装 2/2 个`（**不是 2 就到此为止，把日志发回来**）
   - `语言源已注册 12 份，当前语言=…`
   - `选项页已注册`
   - `ZoneSnapperFollowSystem 已注册：ToolUpdate 相位…`
2. 少了任何一行 ⇒ 装的不是本次的包或装载失败，把日志发回来，后面不用测。

### 第二步：选项页文字（反馈 4）

1. 打开 选项 → 模组 → Zone Snapper，三个标签页 **吸附 / 其它设置 / 快捷键** 的文字都正常（没有整行英文、没有 key）。
2. "吸附"页上那**三个区域类型开关**（市辖区 / 专门产业区 / 表面区域）应当**只有标题、没有说明文本**
   —— 说明这一轮删掉了，还显示着就是旧包。其余每一项仍是「人话标题 + 一行说明」。
3. 点开**贴合模式**下拉框：五个选项应当显示中文（智能 / 路径最短 / 节点最少 / 交点最少 / 不切换网络）。
   看到 `Options.TRACEMODE[…]` 这种 key ⇒ key 模板还没修对，这是硬故障，发日志。
4. "其它设置"页应当看到改过口径的那一行：**「允许「重算贴合区域」键改写已保存的区域」，默认开**，
   标题与说明里**不再有「实验性」「自动跟随」字样**。

### 第三步：吸附距离滑杆真的管用了（反馈 2/3）

1. 用市辖区工具开始画（点一个节点即可），把它的「节点吸附距离」从 100% 拖到 **50%**，再拖到 **300%**：
   吸附手感应当**立刻**变，包括游戏内置的节点对齐那一档；日志出现
   `吸附距离已写入 prefab：实体 #N 原版 X → Y`。
2. 然后**离开区域工具**（切到道路工具或按 ESC）：日志出现
   `吸附距离已还原 prefab：实体 #N（tool-left-create）`，并且**城市边界那种粗虚线方块恢复原粗细**。
   粗虚线一直粗着 ⇒ 「借了没还」还活着（FACT 27：线宽与虚实段长都正比于 `m_SnapDistance`），发日志。
3. 顺手确认地上**不再多出第二个粗虚线长方形**；如果还有，先看日志里有没有「已还原」那一行再报
   （排查结论见 §〇 ③：长方形本身是游戏画的瓦片边框，模组没有任何创建区域实体或画虚线的代码）。

### 第四步：手动节点与直线（反馈 1/3）

1. 在**空地**上点节点：节点应当落在**你点的位置**，不被模组吸到附近的路/步道上去
   （唯一例外是打开「城市边界线」那一档时；游戏自己对齐的手感仍在，那是原版的活，不是模组的）。
2. 画一条基本是直的边并落档 ⇒ 那条边应当**只有两端两个手动节点**，中间不再多出一串节点。
3. 节点还被乱挪、或直线仍多点 ⇒ 报「哪个工具、哪一档、大概离路多远」，这三个信息能直接分诊。

### 第五步：拖拽一格，松手后真的落档（反馈 6）

1. 画一块贴路的区域并闭合，然后**拖动它的某一格**到新位置，松手。
2. 看日志：应有 `拖拽落档重描：区域实体 #N 第 K 格，左边 X 点 / 右边 Y 点（回退 a/b），环 M → N 个节点`
   —— 这一行六轮从未出现过，是「落档真写了」的唯一证据。
3. 看形状：**只有被拖那一格的左右两条边**重新贴合，其余格一个都不动（包括落在吸附距离内的）。
4. 松手没变化、日志里只有 `followReject=` 在涨 ⇒ 局部自检之外还有闸在拒，发日志。

### 第六步：改路之后不自动改存档，按键才改（反馈 5）

1. 画一块贴路的区域并闭合，然后去**改它旁边那条路**（拆一段 / 移一段 / 加岔路）。
2. 判读：区域边界**不应当自动变**；日志只有一句
   `路网/建筑变了：已提交区域不会自动改，要按「重算贴合区域」那颗键才写盘（第六轮反馈 5 的口径）`
   （连续改路也不刷屏，去抖过的）。边界自己动了 ⇒ 那条自动路径没掐死，是硬故障。
3. 给快捷键「重算贴合区域」设一颗键，按它 ⇒ 视野内的区域沿新路重描，日志出现
   `跟随重描：区域实体 #N 节点 X → Y 个…`，那块区域描一圈绿色高亮、被挪的节点闪几秒；
   「退回上一次自动跟随的调整」键仍然有效。
4. 再把「允许「重算贴合区域」键改写已保存的区域」**关掉**试一次：按键应当什么都不做，
   日志出现一句 `按了「重算贴合区域」，但选项页的「允许『重算贴合区域』键改写已保存的区域」是关的 ⇒ 这次什么都没写`
   （一局只说一次，这是刻意的）。

### 第七步：挨着已有区域画第二块（反馈 7）

1. 找一块已提交的区域，**沿着它的边界**画第二块，让公共段走一段直线距离。
2. 判读：公共段两边的节点应当**重合**（放大看没有双缝）；统计行里的 `weld=` 从 0 变正。
3. `weld=` 一直是 0 ⇒ 2 米阈值或平行判据在真实顶点密度下太紧，报公共段大概多长、两块是什么类型。

### 第八步：高架与地面路叠在一起（反馈 8）

1. 找一段高架压在地面路上的地方，用**市辖区**在**地面路**附近点节点。
2. 判读：节点吸到**低的那条**（地面路）；同一段画两次结果相同（旧版的胜负由遍历顺序决定，两次能不一样）。
3. 想吸高架就点到高架附近去——平局只在「两条严格等距」时按高度裁决，不影响你主动选哪条。

### 第九步：三类的吸附目标 + 白名单（反馈 9）

1. **市辖区**：只吸**道路/轨道中心线**（含高架）；步道、水道不吸。
2. **专门产业区 / 表面区域**：只吸**人行道外缘**与**真建筑的轮廓**；
   步道、水畔、**埠头**、**厂区内部路**、树/摆件/桥墩都**不再是目标**。
3. 日志应有一行 `建筑轮廓白名单已生效：这一片 N 个静态物体不是建筑（树 / 摆件 / 桥墩之类），已排除在贴合目标之外；
   另有 M 个建筑拿不到轮廓…`（一局一次）。
4. 路缘档的边界**绝不应画到马路中间**；定不出贴哪一侧时它保持你画的直线（宁可直连）。
5. **误杀也要报**：哪条「真路」不吸了，报路的类型与位置——「内部路」判据是 fail-open 的，
   漏排是预期内的，误排不是。

### 第十步（六轮悬案）：闭合那条边到底写进去没有（反馈 0）

1. 用市辖区沿一条路点 4~6 个节点，**最后把首尾连起来闭合**。
2. 立刻看日志找 `闭合边已补写：区域节点 X → Y 个…`。
   本轮拆掉的根因是「帧序自杀」：待补写环在提交那一帧被 `ClearSession` 丢掉（§〇 ①），现在有 8 帧认领窗口。
3. 判读：
   - 出现 ⇒ 「模组能写 `Game.Areas.Node`」六轮以来第一次被证实，跟随/拖拽写盘的地基是实的；
   - 只有 `闭合预览：环点数 …` 而没有「已补写」⇒ 发日志，这仍是下一轮头号任务；
   - 出现 `连续 3 次无法读写 Game.Areas.Node` ⇒ 也是有效结论（游戏拒了我们的写），发回来。
4. 顺手看闭合后的边界：最后那条边是沿路弯的，还是直直切过去。

### 第十一步（出机前追加的那颗按钮）：快捷键页最下面的「重置」

1. 先**故意**把设置弄成认得出的样子：三档「节点吸附距离」拖到 50% / 300%、贴合模式换成「交点最少」、
   关掉「表面区域」，四颗快捷键**至少配一颗**（比如给「重算贴合区域」配一颗键）。
2. 到「快捷键」页，四颗键下面那个分组（标题「重置」）里点**把所有设置恢复默认值**：
   应当**先弹确认框**（文案：确定要把本模组的所有设置项恢复成默认值吗？快捷键也会被清空…）。
   不弹就直接改了 ⇒ 弹窗那条链路没接上，发日志。
3. 确认后判读：
   - **面板上**：三档滑杆、贴合模式、区域类型开关应当**立刻**回到默认（100% / 智能 / 三个全开）。
     数字没动但**关掉面板重开一次**就是默认值 ⇒ 只是刷新时机问题，算通过，请在反馈里提一句是哪种；
   - **快捷键**：四颗都显示未绑定。仍显示老键名 ⇒ 发日志，重点看 `设置已重置` 那一行的 `mapOk=`；
   - **手感**：立刻去画一块区域，吸附距离应当是原版手感（不再是你上次拖的 300%）；
   - **那颗旧快捷键必须失效**：按原来那颗键，什么都**不该**发生。
     ⚠ 按任意键都触发某个功能 = 「幻影触发」回来了（见 §☆ ③ 第三条），这是这颗按钮唯一可能弄坏键盘的失败方式，
     请优先把这一条的结果发回来。
4. 日志里应有：`[ZoneSnapper] 设置已重置：所有选项回到默认值，四颗快捷键已清空（需要重新配）cleared=4/4 mapOk=N`。
   `mapOk=4` 最干净；`mapOk<4` 表示有几颗只清了存档、本次会话的映射表没清掉（重启即好，但仍请报回来）。
5. 重新配一颗键、按它确认生效之后，再回来看第 3 条那三个「面板上」的判读——
   **同一会话里刚配的键要重启才生效**这条老限制本轮没动（它和这颗按钮无关，是 OnLoad 才注册句柄），
   如果你实测发现刚配的键当场就能用，也告诉我，那说明我把这条限制记错了。

### 判读用的一行账（想让我们少问一轮就发这个）

每 300 帧打一行，长这样（数字是示例）：

```
tier=Lot mode=Smart radius=12.0 prefab=12.0 minNode=6.0 spacing=2.0 manual=5 auto=18 slots=23 list=24
snap=412 miss=9 traceHit=57 fallback=12 commit=6 rejected=0 pop=4 shrink=0 adopted=2 hooks=2/2 desync=0
follow=3 followSkip=1 followReject=0 undo=1 rebind=14 resnap=2 lost=0 stale=0 fb=NotOnNetwork×6,DetourTooLong×3
pv=812 pvSkip=…0 dragIdx=9 dragPos=1 cont=7 weld=12 lastMs=0.61
```

- `weld=` 是本轮新加的：**照抄了多少格邻接区域节点的坐标**（逐位相同才算），与第七步对着看；
- `followReject=` 本轮之后应当**基本不涨**：拖拽自检改成只看局部链之后，「密点环必判自交」那个来源没了；
- `snap=`/`miss=` 现在只对城市边界那一档计数（手动节点不再跑我们的吸附，§〇 ④）；
- `mode=` / `radius=` / `dragIdx=` / `dragPos=` / `cont=` 的读法与上一轮相同。

### 不用测的（本轮刻意没做）

- 「路网变了自动改写存档」：这条路径**已经掐掉**（反馈 5），只剩按键那一条，别再等它自己动。
- 智能档（Smart）的旋钮数值调优、续接中断点口径复核（反馈 10 没做的两条）：要等这份日志里的
  `mode=` / `cont=` / `fb=` 分布才能定，离线调参是拍脑袋。
- 太空区域、公园 / 散步区 / 公交线路那类点链工具：口径不变，仍不做。

日志路径：`%USERPROFILE%\AppData\LocalLow\Colossal Order\Cities Skylines II\Logs\ZoneSnapper.log`

---

## 三、本文件夹清单（谁是什么）

```
ZoneSnapper.csproj           官方工具链工程（net48；带 Harmony 2.2.2 编译 + 2.3.3 运行替换）
ZoneSnapperMod.cs            IMod 入口（顺序硬约束：LoadSettings 先于 RegisterKeyBindings；第五轮加了「补丁必须先于注册系统」）
ZoneSnapperSetting.cs        选项页（吸附/其它设置/快捷键三页）+ 四个快捷键动作 + 词条 key 模板对账
Engine/                      纯 BCL 层：GeoKit WorldModel SnapKit PolicyKit TraceKit SimplifyKit
                             ProjectKit StoreKit InputKit FollowKit AnchorRebind PreviewKit LocaleKit   ← 回归壳直接编这套源文件
                             （CurveKit 已无产品调用者，仅作为已测纯几何留着，见 §一 限制 10）
GameSide/                    WorldSampler（ECS↔引擎）、SnapperState（volatile 镜像 + 日志口 + 跟随退回栈）、
                             FallbackTally（回退原因计数）、PreviewFeed（预览的发布/消费信箱）、
                             FollowFlashFeed（跟随改动的高亮/闪烁信箱，上限 8 块、活 4 秒）
Systems/                     ZoneSnapperSystem（ToolUpdate：投影 + 会话 + Idle 统一出口）
                             ZoneSnapperFollowSystem（ToolUpdate：已提交区域跟随 + 拖拽落档重描 + 单级退回。
                             ⚠ 相位在 v0.2.0 从 ApplyTool 挪过来，理由见 FACT 21，别挪回去）
                             ZoneSnapperApplySystem（ApplyTool：提交那一帧补写闭合边，必须留在这一拍）
                             ZoneSnapperPreviewSystem（Rendering，先于 OverlayRenderSystem：淡色预览 + 跟随高亮，不写任何数据）
Patches/                     AreaToolPatches（唯一补丁点：Apply/Cancel 两个 postfix）
Properties/PublishConfiguration.xml   发布元数据（本轮不发布；无 ModId 是刻意的）
scripts/verify.mjs           离线门禁（版本一致性 + T1 + T2 产物身份 + T2b Harmony sha + T3）
scripts/extract-official-terms.py  官方词表抽取器：解析 `Locale.cok` 里的 `<lang>.loc`，
                             输出 `research/locale-official-terms.md`（L6 那些期望串的证据；**要能重跑**）
scripts/probe-official-locale.py   官方文案存在性检索：按 key 片段与值片段全库 grep，
                             用来判"游戏里到底有没有这个词"（0 命中 = 不许冒充官方）
scripts/*.py（其余三个）      本轮批量改/核对 12 语言表用的一次性脚本（check-locale-table.py 值得留：
                             它逐行数格子，是 L3 段之外的第二双眼睛；另两个已经用完、内容全部进了
                             `LocaleKit.cs` 并被 L1–L6 钉住 ⇒ 该删。但**这个目录还不是 git 仓库，删了就没有回退**，
                             所以先留着，等建库那一轮连同 §一 限制 10 的 CurveKit/IsoKit 一起去留去）
tests/t3/                    无游戏回归壳（T3.csproj + Check/Harness/Fixtures/Tests.*，1208 条断言）
research/                    1.6.2f1 反编译树、参照模组元数据快照（不进 git）、
                             locale-official-terms.md 与 locale-probe*.txt / locale-blob-grep.txt（⑨ 的证据）
设计文档.md                  机制蓝图（所有 file:line 证据都在这，FACT 1～32）
README.md                    用户视角
开发笔记.md                  本文件
```

一次性脚手架已清理：v0.1.4 那轮用于批量改源码的 `research/patch_*.cjs` 三个脚本、以及定位 IsoKit 断线
与需求 9 数字的一次性探针工程 `tools/isoprobe/` 都已删除（Playbook 步骤 10：临时脚本用完就清，
并在文档里留一句「现在根目录应该只有 N 项」）。探针的结论没有丢：全部转成了 T3 断言（I 段 + C9 段），
下次回归由门禁守着，不靠那份副本。
第五轮新增的三个 `scripts/*.py` 是批量写/核 12 语言表用的，其中两个（append-locale-descs、find-locale-defects）
已完成使命，留着一轮再删（本轮 LocaleKit 是它们写出来的，删早了对不上账）。
现在根目录应当只有：`Engine GameSide Library Patches Properties Systems research scripts tests
README.md ZoneSnapper.csproj ZoneSnapperMod.cs ZoneSnapperSetting.cs 开发笔记.md 设计文档.md`
（+ 构建产物 `bin obj`，都在 .gitignore 里）。
