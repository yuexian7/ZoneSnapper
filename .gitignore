# Zone Snapper —— 公开仓库不收的东西
#
# 仓库范围（作者 2026-09-25 定的口径）：源码 + 离线回归壳 + 脚本 + 三份文档 + 调研证据。
# 下面每一条都是"为什么不收"，不是随手写的通配符。

# —— 构建产物与本机状态 ——
bin/
obj/
Library/
*.user
.vs/

# —— 反编译的游戏源码：那是 Colossal 的程序集产物，不是我们的可分发物 ——
# research/decompiled/ 下 2258 个 .cs 是调研材料（标准流程步骤 1 的反编译产物）。
# 文档与代码注释引用它一律写成 research/decompiled/<文件>:<行> 这种指针，
# 需要看原文的人自己按步骤 1 重跑一次 —— 这样"官方断言可重跑取证"这条不依赖仓库里存着它。
research/decompiled/

# —— 发布快照（二进制副本，仓库里放一份等于把每个版本的重传产物都存着）——
releases/

# —— 实机日志：只在本机，含玩家城市名，绝不进仓库 ——
Logs/
*.log
# 例外：这份是 1.6.2f1 反编译跑批的调研凭据（版本对应关系靠它），不是运行日志
!research/decompile-1.6.2.log

# —— 任何本机状态/凭据（Paradox 会话 token 在 AppData 下，本来就不在这儿，写死防手滑）——
state/
*.pdxsdk*

# —— 开发期调研产物：整目录不入库（照 AccessAnarchy 那份 .gitignore 的口径）——
# research/decompiled/ 是 Colossal 程序集的反编译产物（2258 个 .cs），research/verify/ 是它的单文件摘录，
# 两者都不是我们的可分发物。文档与代码注释引用它们一律写成 research/decompiled/<文件>:<行> 这种指针，
# 需要原文的人按标准流程步骤 1 自己重跑一次反编译。
# 唯一例外：research/locale-official-terms.md 由 scripts/extract-official-terms.py 现抽，
# 那份表是 README/设计文档「官方用词」栏的出处 ⇒ 留在仓库里，改文案时能重跑比对。
research/
!research/locale-official-terms.md
